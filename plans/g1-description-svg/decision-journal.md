# G1 decision journal

| When | Iter | Decision | Why | Flag |
|---|---|---|---|---|
| 2026-07-14 | brief | Family triage via new census --families mode (committed as harness prep). Root-attr family (18) = the annotated-fixture set — matches the G0b unwrapKlimtSvg flag. @viewBox/@width/@height (298/275/274) treated as downstream symptoms, drilled LAST. | census --families run | no |
| 2026-07-14 | I0 | Census baseline re-measured before touching anything: errors was 4, not the README's documented 8 (README stat predates this session; not re-derived here, just noted -- census is reproducible run-to-run, confirmed by 2 consecutive runs). image/@xlink:href normalize rule added to `compare.ts` (present-and-nonempty-match; geometry stays strict) -- DIVERGENCES.md-cited, 4 new tests. Error triage (instrumented via a temporary `scripts/_tmp-*.ts`, deleted before commit): bujige-52-gase998 (invalid XML -- fixed, two origin points: wrongly-drawn `<!--entity-->` comment on port/note fallback + a raw dot-key leaking into the `<!--link X to Y-->` comment, both NUL-byte-adjacent via `set separator`-collision disambiguation), xusuxe-62-guba767 (graphviz-ts crash, ledgered, cites si5b journal), fepuvo-06-rugi981 (jar's OWN oracle SVG is malformed XML -- 3-dash comment-defang edge case baked into the golden, not our bug, ledgered), vivido-49-nisu863 (root-attr loss on an annotated fixture -- same mechanism I1 targets, just manifesting as a hard parse error here; deferred to I1, re-verify after). Post-fix census: errors 4->3 (vivido remains, pending I1), conformant held at 12/355 (no regression). DOT gate re-verified frozen: component 262/262, usecase 90/90, class 708/708, object 78/80, state 266/267. Full suite: 308/308 files, 8208/8208 tests (one pre-existing test, `renderer.test.ts`'s note/port comment assertion, updated -- it was pinning the pre-fix WRONG behavior). | census + diagnosis.md (instrument-before-hypothesize on the 4 errors) | no |
| 2026-07-14 | I1 | MECHANISM (root-attr family, 18 fixtures -- component/bagoze-78-lada681, balopu-66-jagu236, gevaje-94-sajo802, josoxo-49-taci997, misube-65-seni576, saroje-26-vabi530, sugaca-11-boma467, tilexe-28-fiju280, tusugu-95-geju398, vajaxu-62-poto986, zosuje-43-zebi775, zozutu-82-pupa220, usecase/gigofe-94-zepe032, lizutu-99-mapa855, nipapu-74-roro938, pivudu-29-pele178, sprite-SVG-fill-management-3, tatori-66-kaci883 -- confirmed via a temporary `scripts/_tmp-i1-slugs.ts`, deleted before commit): every ANNOTATED description fixture (title/legend/header/footer/caption) goes `renderDescription` (klimt CompleteSvg, full root attrs: xmlns:xlink, version=1.1, data-diagram-type=DESCRIPTION, zoomAndPan=magnify, preserveAspectRatio=none, contentStyleType=text/css, a `<?plantuml $version$?>` PI, a `style` carrying width/height/background -- svg-graphics-core.ts#getRootNode:311-336 + #finalizeRootAttributes:456-479) -> `unwrapKlimtSvg` (renderer.ts:438-445) -> `applyChrome` (chrome.ts, unrelated to this mechanism) -> `assembleSvg` (src/index.ts:134-137). `unwrapKlimtSvg` reduces the complete klimt document to a bare `RenderFragment {body,width,height,background,extraDefs}` -- discarding EVERY root attribute except the ones it recomputes (width/height via `extractViewBoxDims`) or is handed explicitly (`background`, from the caller's theme). `assembleSvg` then routes any `RenderFragment` (this one included) through the GENERIC `svgRoot` (core/svg.ts:484-501) -- the shell EVERY non-klimt engine uses -- which emits only `xmlns width height viewBox` (no xmlns:xlink/version/data-diagram-type/zoomAndPan/preserveAspectRatio/contentStyleType/PI/style), unconditionally injects `ALL_ARROW_TYPES` SVG `<marker>` defs the description engine never references (it draws arrowheads as inline polygons -- `renderer-edge.ts`/`SvekEdge`), and draws an explicit background `<rect>` sibling (klimt bakes background into the root `style` attribute instead). Verified against 3 representative fixtures (component/balopu-66-jagu236, component/gevaje-94-sajo802, usecase/sprite-SVG-fill-management-3): identical failing-check set (`svg/@version @preserveAspectRatio @zoomAndPan @xmlns:xlink @contentStyleType svg[childCount]`) and identical root-children shape mismatch (`[defs, rect, g, g]` vs jar's `[defs, g]`) on all three. Ruled out: (a) chrome.ts's own extra `<g class=title>`-vs-nested-in-original-`<g>` DOM-shape divergence -- this IS a real, additional contributor to `svg[childCount]` (jar nests title/legend INSIDE the single content `<g>`; chrome.ts emits them as SEPARATE top-level `<g>` siblings, deliberately, per that module's own doc comment lines 26-33, a PRIOR mission's G0b/T4 decision) but is OUT OF SCOPE here: it is shared code used by every diagram type's chrome (not klimt-specific), so 'fixing' it risks regressing other engines' annotated output, which the mission's own guidance explicitly protects ("other engines' svgRoot output must not change"). I1 fixes the root-SHELL loss only (xmlns:xlink/version/data-diagram-type/zoomAndPan/preserveAspectRatio/contentStyleType/style/PI/defs), which is klimt-specific and fully isolable; `svg[childCount]` will improve (4->3, dropping the spurious `rect`) but likely not reach exact parity with jar's 2 for these 18 fixtures until/unless a later iteration deliberately revisits chrome.ts's shared nesting shape. (b) width/height/viewBox VALUE mismatches (e.g. "200.488" vs "216") are the ALREADY-DEFERRED I10+ measurer-gap residue (README's own iteration table) -- confirmed present on all 3 fixtures regardless of the root-attr fix, not part of this mechanism. Fix (planned): additive `RenderFragment.klimtShell?: true` marker set by `unwrapKlimtSvg` (renderer.ts) unconditionally (every fragment it produces is already annotated-klimt-only by construction, per that function's own doc comment); a new `assembleKlimtShell` function (renderer.ts, co-located with the existing `DIAGRAM_TYPE_ATTR`/`VERSION_PLACEHOLDER` constants and `unwrapKlimtSvg`) reproduces klimt's exact fixed-constant shell (none of these values are per-fixture data -- xmlns:xlink, version=1.1, zoomAndPan=magnify, preserveAspectRatio=none (basicSvgOption's own default, unoverridden by renderDescription), contentStyleType=text/css, data-diagram-type=DESCRIPTION are ALL diagram-type-wide constants, so this reproduces them directly rather than parsing them back out of the discarded klimt string) plus a `style`/width/height/viewBox recipe matching `finalizeRootAttributes` (Math.trunc'd dims, background folded into `style` not a `<rect>`), and NO `ALL_ARROW_TYPES` marker injection. `assembleSvg` (src/index.ts) gains one branch: `fragment.klimtShell === true` -> `assembleKlimtShell(fragment)` instead of `svgRoot(...)` -- `svgRoot` itself and every other engine's call path stay completely untouched (verified: no other RenderFragment producer ever sets `klimtShell`). | diagnosis.md (instrument-before-hypothesize: dumped root children/attrs for 3 fixtures via a temp script before any fix) | no |
| 2026-07-14 | I1 | Fix landed per the mechanism journaled above: `RenderFragment.klimtShell?: true` (dispatcher.ts), `assembleKlimtShell` (renderer.ts), one branch in `assembleSvg` (src/index.ts). Re-measured on the same 3 representative fixtures: diffCount 11->5, 9->3, 11->5 respectively (residual = the OUT-OF-SCOPE viewBox/width/height I10+ measurer gap + the deferred chrome-nesting childCount +1). Full census: errors 3->2 (vivido-49-nisu863 resolved -- joins the measurable set as the 19th root-attr-family fixture), 1-3 bucket 13->18, 4-10 bucket 99->107, 11-30 bucket 74->62, conformant held at 12/355 (none of the 19 reach exact zero -- both residuals are legitimately out of I1's scope). Family re-measure: svg/@version, @xmlns:xlink, @zoomAndPan, @preserveAspectRatio, @contentStyleType families all -> 0 fixtures (fully closed). svg/@background 40->25 fixtures (the 19's background diffs resolved as a bonus of the same root-attr fix; the remaining 25 are an unrelated, out-of-scope mechanism). svg[childCount] (root level) 18->19 (vivido joined) -- NOT closed to 0, ruled in as the chrome.ts sibling-<g> divergence (separate, deliberate, prior-mission decision -- see ledger). DOT gate re-verified frozen: component 262/262, usecase 90/90, class 708/708, object 78/80, state 266/267. Full suite: 308/308 files, 8221/8221 tests (13 new this iteration: 3 namespace-groups.test.ts tests for `bareEntityName` -- the function itself landed with I0's commit for the SvekEdge comment fix, but had no direct unit test until now -- plus 2 `unwrapKlimtSvg` + 8 `assembleKlimtShell` tests in renderer.test.ts). One pre-existing test's helper corrected (`annotations.e2e.test.ts#dims()` now tolerates a `px` suffix, matching the jar's own convention the klimt-shell path now reproduces). Ratchet: zero-diff fixture list unchanged (still the same 12) -- no ratchet additions this iteration, expected (none of the 19 reached exact zero). | diagnosis.md mechanism fix + re-measure | no |
| 2026-07-14 | I0+I1 | ACCEPTED (orchestrator-verified: 8221/8221, DOT frozen exact, census errors 8->2, 1-3 bucket 13->18, conformant 12 held as predicted). PROCESS NOTE: the agent committed directly (6a0c3c2, 8006204) despite the no-commit boundary — content verified good, kept; future G1 prompts restate the rule prominently. Baseline correction: the README's errors:8 predated the census stdlib-store wiring (true pre-I0 baseline was 4). MISSION PAUSED here per maintainer: resume at I2 after the graphviz-ts update lands (watch armed); on arrival — bump, full gates vs FROZEN baseline, xusuxe re-validation, retire its ledger rows, fresh census, then I2. | gate run | paused |
| 2026-07-14 | bump | graphviz-ts 0.1.26071122 -> 0.1.26071415 (crash fix + cround rounding fixes). Orchestrator-verified: 8223/8223, DOT gate component 262/262 usecase 90/90 class 708/708 object 78/80 **state 267/267 (fotuje-06 healed — same root cause as xusuxe)**; census identical except errors 2->1 and xusuxe entering 31+ as a live fixture; zero golden churn from the rounding fixes. Both healed fixtures golden-pinned (fotuje with a 3.238626in size-backlog pin — its size-fidelity queue entry). MISSION RESUMED at I2. | gate runs | no |
| 2026-07-14 | I2 | MECHANISM(S) (text style constants family, re-measured before touching anything: font-size 72 fixtures/172 diffs, font-weight 73/149, fill 46/114, font-family 6/6 -- close to but not identical to the README's stale I0-era numbers, re-derived per protocol). Instrumented via a temporary `scripts/_tmp-i2-text-diffs.ts` + `scripts/_tmp-i2-dump.ts` (both deleted before finishing) dumping actual/expected value pairs and side-by-side `<text>` element diffs for representative fixtures. THREE independent mechanisms found, all under `src/diagrams/description/`: (1) **cluster/group title never bold** -- `renderer-cluster.ts#buildHeader`'s title font carried no style flags; jar: `abel/Entity.java:478-489#getFontConfigurationForTitle` routes EVERY group/container title (not just literal `package` keyword) through `FontParam.PACKAGE` with `inPackageTitle=true`, and `klimt/font/FontParam.java:167-172#getDefaultFontFace` returns `UFontFace.bold()` whenever `inPackageTitle` is true -- verified jar-side across `component/balomu-94-kegi822` (frame), `bijoko-90-riro507` (node), `bisedo-29-kone620` (15 container keywords, all bold) -- every container type bolds its title identically, leaf entities never do (`renderer-entity.ts`'s `fontTitle` carries no flags, jar-verified never bold on the same fixtures). Diff: `text/@font-weight` actual="" (attribute absent) expected="700", 100% of the 149 diffs in that family, 0 counterexamples in the 12-sample spot check. (2) **stereotype text drawn 2pt smaller and upright instead of same-size + italic** -- both `renderer-entity.ts` (leaf) and `renderer-cluster.ts` (container) stereotype text used a local `STEREOTYPE_SIZE_DELTA = -2` (`theme.fontSize - 2` = 12) with no style flags; jar: EVERY `klimt/font/FontParam.java` `*_STEREOTYPE` entry reachable from a component/usecase diagram (`COMPONENT_STEREOTYPE`, `NODE_STEREOTYPE`, `ARTIFACT_STEREOTYPE`, ... all 27 relevant entries) is size 14 -- IDENTICAL to its non-stereotype counterpart -- with only the face changed to `UFontFace.italic()`; verified jar-side on `component/betidu-24-xuku720` (`«static lib»`/`«shared lib»`/`«shared node»` stereotypes, all font-size="14" font-style="italic" in the cached oracle, vs our font-size="12" no font-style). Diff: `text/@font-size` actual="12" expected="14", 94 of 172 diffs (the dominant cluster); a smaller secondary `text/@font-style` family (38 fixtures/102 diffs, not one of this iteration's 4 named families but the SAME origin) closes as a direct side effect of the same fix. (3) **edge/link label font hardcoded to `theme.fontSize - 2` (12) and `theme.colors.graph.edgeLabel` (#444444)** -- `renderer-edge.ts#buildInput`'s `labelFont` used the SAME `-2` delta convention as (2) plus the shared theme's `edgeLabel` gray; jar: `klimt/font/FontParam.java:54`, `ARROW(13, UFontFace.normal())` -- a FIXED size (13) independent of any other FontParam/theme value -- and `FontParamConstant.COLOR = "black"` (`klimt/font/FontParam.java:44`, the fallback every FontParam entry without its own override color resolves to; `ARROW` has none) -- verified jar-side on `component/babafi-51-dixi026`'s `use` edge label (font-size="13" fill="#000000" in the oracle vs our font-size="12" fill="#444444"); traced the label-font construction call chain to `svek/GraphvizImageBuilder.java:230-238` (style-system `getFontConfiguration` call, which falls back to these `FontParam` defaults absent a style/skinparam override -- the exact literal call site for the STYLE path is not ported here, but the DEFAULT VALUES it falls back to are the jar-verified ground truth cited above and match empirically across every sampled fixture). Diff: `text/@font-size` actual="12" expected="13" (53/172 diffs, the second-largest font-size cluster) + `text/@fill` actual="#444444" expected="#000000" (44 of 114 fill diffs, the dominant fill cluster). Ruled out: (a) the `text:color` inline-override-on-clusters gap (`renderer-cluster.ts#buildHeader` never applies `node.color`'s `text:` override, unlike `renderer-entity.ts#buildEntityParams`) -- real, independently diagnosable (`component/bisedo-29-kone620`'s 9 container stereotypes/titles show actual="#000000" not even the raw override value), but even fixing the wiring would NOT reach zero-diff without also solving (b), so deferred rather than half-fixed. (b) **named CSS color -> hex resolution** (`green`->`#008000`, `blue`->`#0000FF`, `red`->`#FF0000`, plus case-normalization `#ffffff`->`#FFFFFF`) accounts for the REMAINING ~63 of 114 fill diffs and ~3 of 172 font-family-adjacent cases (`saxosu-09-nodi002`'s double-quote-vs-single-quote font-family list is a SEPARATE, smaller quoting-convention issue, not investigated this iteration) -- this port has no `HColorSet` name-table (`src/core/paint.ts` grep-verified: no such table anywhere), and `tests/unit/description/renderer.test.ts:505-509`'s own PRE-EXISTING doc comment already documents this exact gap from an earlier task (T19) as out-of-write-set; not rebuilt here (a full ~150-name CSS/PlantUML color table is a distinct, larger mechanism, not a "constant" fix). (c) `figika-36-sola271`'s `skinparam ComponentArrowFontColor/FontName/FontStyle/FontSize` (per-diagram-type arrow font skinparam overrides) has NO wiring anywhere in `skinparam.ts`/`theme.ts` (grep-verified) -- a distinct, unbuilt feature, not a constant-default bug; the DEFAULT-value fix in mechanism (3) does not and is not intended to address it. | diagnosis.md (instrument-before-hypothesize: dumped actual/expected value-pair frequency tables + side-by-side jar/ours `<text>` element dumps for 9 representative fixtures across all 4 families before any fix) | no |
| 2026-07-14 | I2 | Fix landed per the 3 mechanisms journaled above, all in `src/diagrams/description/`: `renderer-symbol.ts#textFont` gained a `styles: ReadonlySet<FontStyle> = EMPTY_STYLES` param (also exported `JAR_DEFAULT_TEXT_COLOR`, previously private, for `renderer-edge.ts` reuse -- no-magic-strings/DRY); `renderer-cluster.ts#buildHeader` passes `TITLE_STYLES` (BOLD) for the title and `STEREOTYPE_STYLES` (ITALIC, sizeDelta 0) for the stereotype, replacing the old `-2` delta; `renderer-entity.ts` replaces its `STEREOTYPE_SIZE_DELTA` constant with `STEREOTYPE_STYLES` (ITALIC, sizeDelta 0) the same way; `renderer-edge.ts#buildInput` replaces `EDGE_LABEL_SIZE_DELTA`/`theme.colors.graph.edgeLabel` with a literal `ARROW_LABEL_FONT_SIZE = 13` constant + the imported `JAR_DEFAULT_TEXT_COLOR`. `theme.ts`'s shared `colors.graph.edgeLabel` field itself is UNTOUCHED (still used by class/state/dot renderers with its own, different jar-verified default -- out of scope, DOT gate depends on none of these files). 7 new pinning unit tests added to `tests/unit/description/renderer.test.ts` (3 for cluster title bold + container-stereotype italic/size, 2 for leaf-entity stereotype italic/size, 2 for edge-label fixed-size/black-fill) -- no pre-existing test asserted the old (wrong) font-size="12"/fill="#444444"/missing-font-weight values, so nothing needed correcting, only extending. Re-measured: census families font-size 72->16 fixtures (172->26 diffs), font-weight 73->8 (149->12), fill 46->24 (114->70, the remaining 24 are the ruled-out named-color-resolution + cluster-text-override gaps), font-family 6->6 (untouched, different mechanism, out of scope) -- plus the unassigned font-style family closed as a bonus of mechanism (2): 38->3 fixtures (102->3 diffs). Full census: conformant 12->14 (+2: `component/pamake-46-sadu464`, `component/sipuma-82-fene185`), 1-3 bucket 18->20, 4-10 107->109, 11-30 62->64, 31+ 155->147, errors unchanged at 1. DOT gate re-verified frozen: component 262/262, usecase 90/90, class 708/708, object 78/80, state 267/267 (untouched files, expected). Full suite: 308/308 files, 8232/8232 tests (7 new). Ratchet: both newly-zero-diff fixtures added (`ratchet.json` + `oracle/goldens/svg-description/component/{pamake-46-sadu464,sipuma-82-fene185}/`, both `parity.json`-verified `dotEqual: true`); the pre-existing gap between the census's 12-fixture zero-diff baseline and the ratchet's 5 pinned fixtures (7 already-zero-diff fixtures from before this iteration, never backfilled by I0/I1) is NOT this iteration's fix -- flagged in the ledger, not silently fixed inline (out of I2's mechanism scope). | diagnosis.md mechanism fix + re-measure | no |
| 2026-07-14 | I3 | MECHANISM(S) (element `@id` family, re-derived before touching anything: `svg/g/g/@id` 83 fixtures/426 diffs, `svg/g/g/path/@id` 10 fixtures/21 diffs -- matches README estimate). `tests/oracle/svg-conformance/normalize.ts` read FIRST per prompt caution: `id` is not in `NUMERIC_ATTRS` and has no special-case handling -- ids are compared LITERALLY, never normalized away, confirming fix-emission (not fix-normalizer) is correct; cross-checked against 4 pre-existing zero-diff fixtures whose `id`s already match the jar byte-for-byte. Instrumented via temporary `scripts/_tmp-i3-*.ts` (id-dump, find-family, single-fixture, classify, dump-svg, verify -- all deleted before finishing) dumping actual/expected id value pairs, full diff sets, and `DescriptionEdgeGeo` field dumps for representative fixtures. FOUR distinct mechanisms found (three fixed, one pre-existing/deferred): (A) **missing `SvekEdge#setSharedIds` wiring** -- `renderer.ts#drawEdges` never created/passed a per-diagram shared `Set<string>` to each `SvekEdge` instance (upstream: `SvekResult.java:93-101`, ONE `Set<String> ids` per diagram draw, wired via `setSharedIds` before each edge's own `drawU`); `SvekEdge.ts` itself already had a byte-faithful `uniq()` port (SvekEdge.java:1093) and its own unit test proving the mechanism correct WHEN wired manually (`svek-edge.test.ts` "setSharedIds dedups path ids across edges") -- only the CALLER never wired it, so duplicate `idCommentForSvg()` strings (e.g. `component/bokumi-45-pupo531`'s three `foo 0--0 bar`-family edges) never got the jar's `-1`/`-2`-suffixed disambiguation, producing 3x the SAME bare id instead. (B) **`renderer-edge.ts#buildInput`'s `headDecor` fallback fired even when `tailDecor` already carried the link's real decor** -- `fallbackHeadToken(edge.arrowHead)` applied whenever `edge.headDecor` was undefined, REGARDLESS of `edge.tailDecor`; `layout-geo-post.ts:107-110` always populates `tailDecor`/`headDecor` per-side directly from the AST (the fallback's own doc comment already claimed it was "only reachable" for legacy/hand-built geometries, which was aspirational, not enforced) -- for any real tail-only-decorated link (jar-verified `component/berelu-46-namo819`'s `B <-down- A`, id `B-backto-A`), this synthesized a PHANTOM head decor from the (side-blind) `arrowHead` classification, flipping `looksLikeRevertedForSvg`/`looksLikeNoDecorAtAllSvg` (`link-decor.ts`, itself byte-faithful) into the wrong branch (bare `X-Y` instead of `X-backto-Y`) AND drawing an extra, jar-absent arrowhead polygon (`g[childCount]` structural mismatch, e.g. `component/cegale-42-loxa672`'s g[5]: actual 3 children, jar 2). (D) **parse-time LEFT/UP-direction inversion (`link-grammar.ts#resolveDecorPair`) swapped raw decor-token STRINGS verbatim instead of mirroring them into the other position's vocabulary** -- deepest-rooted of the three: upstream inverts via `Link#getInv()` -> `LinkType#getInversed()` (`decoration/LinkType.java:131-132,145-147`), which swaps the ALREADY-RESOLVED `decor1`/`decor2` `LinkDecor` enum fields (`abel/Link.java:145-147`; `CommandLinkElement.java:296-326` never reorders `cl1`/`cl2` itself, `getInv()` does, post-resolution) -- a pure "which side" relabeling requiring no character translation, since `LinkDecor` is abstract. This port instead carries the RAW TOKEN through to `SvekEdge` for lookup there (by design, `renderer-edge.ts`'s own doc comment), so `resolveDecorPair`'s `inverted ? {tail:head2, head:head1} : ...` swap moved a DECORS2-vocabulary token (e.g. `'>'`, valid only via `lookupDecors2`) verbatim into the `tailDecor` slot, where `SvekEdge` looks it up via `lookupDecors1` -- a guaranteed miss (`DECORS1['>']` does not exist; DECORS1 uses `'<'` for the same `ARROW` decor) -- silently dropping the decor entirely for EVERY LEFT/UP-directed decorated link (jar-verified `component/berelu-46-namo819`'s `A -up-> B` and `component/golati-24-xika861`'s `Component -LEFT-> Participant10<<v1.0>>`, both `-backto-` ids in the oracle, both bare/no-decor `data-link-type="association"` in ours pre-fix). Ruled out: (C) **`ent%04d`/`lnkN` uid NUMBERING/ordering divergence** -- confirmed as the SAME PRE-EXISTING, already-documented gap `renderer-uid.ts`'s own module doc comment names (T17 mission report): this port's `buildUidPlan` approximates upstream's true PARSE-TIME interleaved (per-entity, per-link, one shared `AtomicInteger`) creation order with a two-phase "all nodes pre-order, then all edges" walk, which can only diverge in id VALUES, never geometry. Verified via a `pureNumbering` classifier over every id-family fixture post-(A)+(B)+(D)-fix: 81 of 84 remaining id-bearing fixtures have EVERY `@id` diff matching `ent\d{4}<->ent\d{4}` or `lnk\d+<->lnk\d+` (pure renumbering, both sides valid-shaped, order-only); the 3 non-pure exceptions are `defs/linearGradient/@id` (1, an UNRELATED family -- randomized gradient id, out of `g/@id`+`path/@id` scope entirely) and `usecase/xacaxe-43-bupe002`'s residual 4 `path/@id` diffs, which are (D)'s dedup mechanism operating CORRECTLY on the WRONG edge-draw order (mechanism C shifts which edge's `uniq()` call happens first, which shifts which one gets the bare vs `-1`-suffixed id -- a downstream symptom of C, not a 4th mechanism). Fixing C properly requires restoring true parse-time interleaved creation order (an AST-level architecture change tracking entity/link creation as ONE sequence, not two separate lists) -- out of this iteration's reach, left deferred exactly as T17 already recorded it. | diagnosis.md (instrument-before-hypothesize: id-dump/edges-dump/full-diff-dump on 6+ representative fixtures per mechanism, jar-side Java citations read before any fix) | no |
| 2026-07-14 | I3 | Fix landed per mechanisms A/B/D journaled above, TDD (red before fix, verified via targeted `vitest run -t` on each new test before implementing): (A) `renderer-edge.ts#drawEdge` gained a `sharedIds: Set<string>` param, calling `svekEdge.setSharedIds(sharedIds)` before `.drawU()`; `renderer.ts#drawEdges` creates ONE `Set<string>` per diagram draw and passes it to every `drawEdge` call (mirrors `SvekResult.java:93-101` exactly). (B) `renderer-edge.ts#buildInput`'s `headDecor` line gated the `fallbackHeadToken` call on `edge.tailDecor === undefined` too (was: `edge.headDecor ?? fallbackHeadToken(...)`; now: `edge.headDecor ?? (edge.tailDecor === undefined ? fallbackHeadToken(...) : undefined)`) -- the existing `makeEdge()` test-geometry-builder default (`arrowHead:'open'`, no tailDecor/headDecor) still exercises the fallback unchanged (both-absent case), confirmed by the full existing `renderer.test.ts` suite passing untouched. (D) `link-grammar.ts` gained `TAIL_TO_HEAD_TOKEN`/`HEAD_TO_TAIL_TOKEN` mirror maps built by zipping the file's OWN pre-existing `DECORS1_TOKENS`/`DECORS2_TOKENS` arrays (already positionally parallel per their shared "every decors1()/decors2() call... enum entries" doc comment -- zero new data duplication, zero new cross-module import); `resolveDecorPair` now translates through these maps on inversion instead of relabeling the raw string verbatim (a token with no mirror -- the two decors2-only HALF_ARROW tokens -- passes through unchanged, best-effort, no worse than pre-fix). 5 new pinning tests: 2 in `renderer.test.ts` (setSharedIds dedup end-to-end via `renderDescription`; tail-only-decor `-backto-` id via `twoNodeGeo`), 3 in `parser.test.ts` (LG-5b/5c/5d -- jar-verified against `berelu-46-namo819`/`golati-24-xika861`'s real corpus shapes, plus an inverted-vs-plain-equivalent-form consistency check). Re-measured (`--families`): `svg/g/g/path/@id` 10 fixtures/21 diffs -> 1 fixture/4 diffs (the 1 residual is mechanism C's downstream dedup-order symptom on `usecase/xacaxe-43-bupe002`, not a new gap); `svg/g/g/@id` UNCHANGED at 83/426 (expected -- mechanism C untouched, out of scope). Instrumented a full before/after per-fixture diff-count comparison (`git show HEAD:<file>` temp-swap, NOT `git stash`/`checkout`, to stay within the read-only-git boundary) to audit bucket movement: 12 fixtures moved to a WORSE bucket (mostly 11-30->31+), diagnosed as a compare-harness ARTIFACT, not a regression -- `compare.ts#compareNodes` returns immediately on any `[childCount]` mismatch WITHOUT recursing into that subtree's attributes (`tests/oracle/svg-conformance/compare.ts:317-325`), so fixtures where mechanism B's phantom-arrowhead `childCount` mismatch is now FIXED (structurally correct) newly expose pre-existing, unrelated, already-out-of-scope geometry diffs (path/@d, polygon/@points, viewBox/width/height sizing -- I7/I8/I9/I10+ territory) that were previously MASKED by the structural mismatch's early return. Verified precisely on `component/cegale-42-loxa672` (21->38 diffs: pre-fix `g[5][childCount]` actual=3/expected=2 masked its subtree; post-fix childCount correctly matches at 2/2 and the walker recurses, revealing a pre-existing ~28px path/polygon coordinate offset tied to the SAME viewBox/width sizing gap already visible elsewhere in the same fixture) and `component/duvoru-86-lubo341` (29->79 diffs: four edge groups' childCount mismatches (1v2/3v2/1v2/3v2) all now correctly 2v2, unmasking pre-existing ~26px path/polygon offsets from the SAME already-flagged viewBox/height gap). Zero conformant-fixture regressions: the exact same 14-fixture zero-diff set holds before and after (verified by direct set comparison, not just the count). Full census: conformant HELD at 14/355 (no NEW zero-diff fixture from A/B/D alone -- every affected fixture also carries mechanism C or an out-of-scope geometry gap keeping it non-zero), 1-3 bucket 20 (unchanged), 4-10 109->108, 11-30 64->60, 31+ 147->152 (the unmasking above), errors unchanged at 1. DOT gate re-verified frozen: component 262/262, usecase 90/90, class 708/708, object 78/80, state 267/267 (untouched files). Full suite: 308/308 files, 8237/8237 tests (5 new) before the ratchet backfill below, 8241/8241 after. Ratchet-parity backfill (per prompt instruction, do-not-widen-the-gap): 14 census-zero-diff fixtures vs 7 previously-ratcheted -- 7-fixture gap. Cross-checked each candidate's `dotEqual` eligibility against `tests/oracle/svg-conformance/parity.json` (2026-07-10, PRE-DATES this mission and is stale for 3 of the 7 -- `norebe-58-bixu182`/`sidame-35-cozu078`/`zoriso-46-vata931` show `dotEqual:false` there despite usecase's CURRENT, live `dot-sync-report.ts --equal-list` showing all 90/90 usecase fixtures EQUAL, confirmed by direct slug lookup in `test-results/dot-sync-equal/usecase.txt`). The ratchet's OWN eligibility test (`description.golden.ratchet.test.ts`, AC3) gates strictly on the parity.json field as written, not live re-derivation -- correctly FAILED when those 3 were force-added, so they were backed out rather than overridden; only the 4 fixtures with a clean, non-stale `dotEqual:true` entry were backfilled (`component/{cifaki-66-boxa005,jesibe-85-sozu187,vapalu-27-muxa300}`, `usecase/dipixi-71-nuga611}`). `parity.json` regeneration (full `svg-parity-survey.ts` corpus run, ~355 subprocess-isolated renders) is out of this iteration's write-set -- noted below, not silently run inline. Ratchet: 7 -> 11 fixtures (`ratchet.json` + 4 new `oracle/goldens/svg-description/<type>/<slug>/{in.puml,golden.svg}` dirs), `description.golden.ratchet.test.ts` green (14 tests) alongside `description-parity.ratchet.test.ts` (351 tests, untouched). Remaining 3-fixture gap (usecase norebe-58-bixu182/sidame-35-cozu078/zoriso-46-vata931) is NOT silently closed -- flagged for a future iteration to regenerate `parity.json` (stale since 2026-07-10, predates the DOT gate reaching 100% for usecase) and re-run the backfill. | diagnosis.md mechanism fix + re-measure + before/after masking-artifact audit | no |
| 2026-07-14 | I3b | MECHANISM (uid numbering ORDER, `g/@id` residual 83 fixtures/426 diffs, resumed at I3's own deferral point -- README + I3's `renderer-uid.ts` doc comment both named this exact gap): re-derived per protocol, instrumented via temporary `scripts/_tmp-i3b-*.ts` (find-family/dump-ast/render/dump-svg, all deleted before finishing) dumping jar-vs-ours id sequences for 6+ representative fixtures, jar Java read BEFORE any fix (full-tree grep, not scoped to `net/sourceforge/plantuml/`, per CLAUDE.md's own silito-78 caution). Counter semantics verified against `net.atmp.CucaDiagram.java` + `abel/Entity.java:171` + `abel/Link.java:135` + `descdiagram/command/CommandLinkElement.java` + `svek/{SvekResult,Bibliotekon,GraphvizImageBuilder}.java`, answering the prompt's (a)-(d): **(a)** `cpt1 = new AtomicInteger(0)`, `addAndGet(1)` -- starts at 0, first assignment returns 1 (`ent0001`/`lnk1`), confirmed against every sampled fixture's lowest id. **(b)** groups/clusters consume counter values identically to leaves -- `Entity`'s uid is assigned in ONE shared private constructor (`abel/Entity.java:162-176`) regardless of which public constructor (leaf vs. group `GroupType`) calls it; jar-verified on `component/bijoko-90-riro507` (`node node {...}` cluster = `ent0002`, sequenced exactly like a leaf). **(c)** note entities consume values (same shared `Entity` constructor, `LeafType.NOTE` no different) -- jar-verified via a synthetic single-note fixture and matched byte-for-byte once fixed; PHANTOM groups (`set separator`-derived, `eventuallyBuildPhantomGroups`) do NOT need modeling here -- traced the call chain (`reallyCreateLeaf:238` only fires when `LeafType.isLikeClass()`, and NONE of component/usecase's leaf types -- `DESCRIPTION/PORTIN/PORTOUT/USECASE/USECASE_BUSINESS/CIRCLE`, `CommandCreateElementFull.java:274-295` -- are in that `LIKE_CLASS` `EnumSet`; the ONLY other call site is `getTextBlock:465`, i.e. AFTER every parse-time id is already assigned) AND our port's own `layout.ts#buildGeoAndEdges` builds the rendered geo tree from `ast.nodes` (the UNGROUPED original), never `groupedNodes` -- phantom groups never reach the SVG as a drawn cluster in this port at all, a separate pre-existing gap, confirmed out of scope. **(d)** the jar does NOT skip uid consumption for entities/links that never render -- `remove`d entities keep the index assigned at declaration (upstream's `remove` is a lazy marker, `Entity.isRemoved()`, checked only at DOT-emission/render time, long after uid assignment); dropped `single`-dedup links AND the discarded pre-inversion `Link` from `Link#getInv()` (`abel/Link.java:145-147`, called by `CommandLinkElement.java:325-326` whenever direction is LEFT or UP -- constructs a WHOLE NEW `Link` object with a FRESH `getUniqueSequence("lnk")` call, discarding the pre-inversion one) both burn a counter value that never appears in the diagram, jar-verified byte-for-byte on `component/berelu-46-namo819` (gaps at lnk5/lnk10, exactly the 2 LEFT/UP links' discarded uids) and `component/golati-24-xika861`. THREE further, un-prompted mechanisms found by direct jar/ours comparison, each independently verified: **(E) draw/document ORDER (not uid VALUE) -- `SvekResult#drawU` (java:70-102) draws EVERY cluster (`Bibliotekon#allCluster()`) THEN every node (`#allNodes()`, a `LinkedHashMap` in `createNode()` call order) THEN every edge; `GraphvizImageBuilder.buildImage:226-227` populates that node order via `printGroups(getRootGroup())` (every group, recursively -- and, PER GROUP, `printGroup` java:425-436 draws that group's OWN leaf children BEFORE its nested subgroups) run to COMPLETION before `printEntities(getUnpackagedEntities())` (top-level UNGROUPED leaves) even starts** -- i.e. root-level ordering is groups-then-leaves, but WITHIN a group it's leaves-then-subgroups (asymmetric), entirely independent of uid VALUE assignment; jar-verified on `component/babafi-51-dixi026` (2 diffs, `ent0001<->ent0002` -- both VALUES already matched pre-fix, position was wrong) and `bijoko-90-riro507` (cluster+4 port members drawn before the earlier-declared top-level leaf `c`). **(F) an anonymous container (`node {`/`cloud {`, no CODE) burns ONE extra shared-counter value** for its auto-generated internal quark name (`descdiagram/command/CommandPackageWithUSymbol.java:178-180`: `codeArg.length()==0 ? diagram.getUniqueSequence("##") : codeArg`) BEFORE the group Entity's own uid -- jar-verified on `component/dugovi-24-kupu658` (`node { [c] portout p }`, ids start at `ent0002` not `ent0001`) and `pafoxe-99-xulo156`. **(G) an EXPLICITLY-braced but EMPTY container (`component X { }`) still participates in the group-sibling draw-order phase** (java:416-418: `printGroups`' own loop mutes an empty `GroupType.PACKAGE` to `LeafType.EMPTY_PACKAGE` and calls `printEntity` DIRECTLY, inline, in group-sibling order -- never falling through to `getUnpackagedEntities()`) even though it renders leaf-shaped -- jar-verified on `babafi-51-dixi026` (`component "b" as b {}` drawn before the earlier-declared `actor a`). Ruled out as a 5th mechanism: `usecase/dopova-50-digo290`'s residual (`ent0005<->ent0004`) traced to a SEPARATE, pre-existing PARSER gap -- `(Use) as "Use the application"` (a paren-shorthand usecase with a QUOTED alias display) matches NO command in `command-table.ts` at all (confirmed: `parseDescription('(Use) as "Use the application"')` returns zero nodes), so `Use` gets auto-created later at first link reference instead of at its true declaration position -- a missing-grammar-rule bug, not a uid-ordering bug; ledgered, not fixed here. `usecase/xacaxe-43-bupe002`'s residual (8 diffs, was 4 pre-fix) re-diagnosed post-fix: EVERY uid VALUE now matches jar exactly (verified byte-for-byte, both entities and all 9 links); the only remaining divergence is EDGE draw/document order for same-connection-pair duplicates (jar groups all `SDK<->Website`-connection edges adjacent in the SVG regardless of their declaration position, ours draws them in plain `geo.edges`/declaration order) -- a narrower, distinct DRAW-order-for-EDGES mechanism (parallel to (E) above but for edges, not nodes), not yet understood (Bibliotekon's `allLines()` read as plain append-order, so the real driver is presumably a graphviz-side edge reordering not yet traced) -- ledgered as a new, narrower residual, not the same mechanism I3 originally deferred it as. | diagnosis.md (instrument-before-hypothesize: id-dump/AST-dump/render-dump on 10+ representative fixtures per mechanism; full jar Java read across `net.atmp` + `abel` + `descdiagram.command` + `svek` before any fix, not scoped to `net/sourceforge/plantuml/`) | no |
| 2026-07-14 | I3b | Fix landed per the 6 mechanisms journaled above (numbering interleaving + LEFT/UP double-burn + endpoint auto-create raw order + anonymous-container burn + node draw-order asymmetry + empty-declared-group draw-order), TDD (10 new `parser.test.ts` cases + 3 new `renderer.test.ts` cases, all red-then-green against hand-derived expected values before any fix landed): **AST** (`ast.ts`) -- additive `creationIndex?: number` on both `DescriptiveNode` and `DescriptiveLink`; `DescriptionNodeGeo`/`DescriptionEdgeGeo` (`layout-helpers.ts`) gain the same field plus `DescriptionNodeGeo.declaredAsGroup?: true`, threaded straight through by `layout.ts#buildGeoNode` (both branches) + `degenerateSingleLeaf` + `layout-geo-post.ts#assembleEdgeGeo` (pure passthrough, no layout math reads any of them). **Counter** (`parse-state.ts`) -- `ParseState.uidCounter: number` (mirrors `cpt1`, reset to 0 in both `makeInitialState`/`parser.ts` and `startNewPage`, matching `NewpagedDiagram`'s brand-new-`CucaDiagram`-per-page semantics), `nextCreationIndex(state)` helper (`+= 1; return`), called unconditionally inside `emitNode` (covers every node: explicit declarations, `ensureEndpoint` auto-creates, note leaves -- one call site, matching `Entity`'s single shared constructor) and at the note-attachment link push in `attachNoteToEntity` (a real jar `new Link(...)`, `CommandFactoryNoteOnEntity.java:342-360`). **Link execute handler** (`command-table.ts`) -- `ParsedLink.inverted` (new field, `link-grammar.ts#parseLinkLine`, threading `resolveDirectionInfo`'s already-computed flag out) drives: (i) `ensureEndpoint` call order flips to raw-`to`-then-raw-`from` when inverted (mirrors `getDummy(ent1)` then `getDummy(ent2)` running BEFORE `link.getInv()`'s swap); (ii) an extra discarded `nextCreationIndex(state)` call when inverted, THEN the real one assigned to `parsed.link.creationIndex`, both BEFORE `addLink`'s `single`-dedup check (so a dropped-as-duplicate link still burns its value, matching upstream). Anonymous-container burn: both the container-inline (case 12) and container-open (case 13) handlers in `command-table.ts` call `nextCreationIndex(state)` once (discarded) when `id.length === 0`, before `emitNode`. **`renderer-uid.ts#buildUidPlan`** rewritten: when EVERY node (recursive) and edge in a `DescriptionGeometry` carries `creationIndex` (always true for real `parseDescription()`/`layoutDescription()` output), format `ent%04d`/`lnkN` DIRECTLY from that value -- no traversal, no ordering computed here anymore; falls back to the OLD pre-order-then-edges approximation only when `creationIndex` is absent (hand-built test geometries, e.g. `renderer.test.ts`'s `makeGeo`/`makeDNode`/`makeEdge` helpers, which never set it -- verified this fallback path is exercised by every PRE-EXISTING renderer test, all of which passed UNCHANGED). **`renderer.ts#collectByKind`** rewritten for the root-vs-nested draw-order asymmetry: a `isGroupLike(node) = children.length > 0 || declaredAsGroup === true` discriminator (the `declaredAsGroup` OR-branch is additive-only -- catches the empty-explicit-group case without breaking hand-built container geometries that never set the flag, which `children.length > 0` alone still classifies correctly, per the first attempt's regression: 9 `renderer.test.ts` failures from using `declaredAsGroup` as the SOLE discriminator, fixed by widening to the OR-form and re-verified 8253/8253 green); root level does group-siblings-then-true-leaves (two full passes over `nodes`), nested level (`visitGroup`) does own-true-leaves-then-group-siblings (opposite order, exactly mirroring `printGroup`'s `printEntities(g.leafs())` THEN `printGroups(g)`). Re-measured (`--families`): `svg/g/g/@id` 83 fixtures/426 diffs -> 2 fixtures/6 diffs (both are the 2 ledgered-above residuals, `dopova-50-digo290`'s missing-parser-rule and `xacaxe-43-bupe002`'s edge-draw-order gap -- NEITHER a uid-ordering bug); `svg/g/g/path/@id` unchanged at 1 fixture/4 diffs (still `xacaxe-43-bupe002`'s SAME residual, now re-diagnosed as edge-draw-order rather than I3's original "mechanism C downstream" characterization -- the underlying VALUES are now provably correct). Full census: conformant HELD at 14/355 (no regression; none of the 81 newly-id-correct fixtures reach zero-diff alone -- every one also carries an out-of-scope geometry/viewBox/path-d gap per the README's own iteration queue, exactly as the prompt predicted), buckets 1-3 20->22, 4-10 108->107, 31+ 152->151 (net: fixtures whose ONLY remaining issue was the id family graduated into smaller buckets), errors unchanged at 1. DOT gate re-verified frozen EXACT: component 262/262, usecase 90/90, class 708/708, object 78/80, state 267/267 (untouched files -- these 10 changed files are description-engine-only, DOT-emission-layer untouched). Full suite: 308/308 files, 8253/8253 tests (12 new: 10 in `parser.test.ts` pinning `creationIndex` semantics -- sequential order, true interleaving, LEFT/UP double-burn, non-inverted single-burn, raw endpoint order under inversion, anonymous-container burn, named-container non-burn, newpage reset, note-link burn -- plus 2 in `renderer.test.ts`'s draw-order describe block, plus 1 in its uid-assignment describe block pinning the exact creationIndex-direct-format path with gaps). Ratchet: no new zero-diff fixtures this iteration (conformant count unchanged), so no backfill; `description.golden.ratchet.test.ts` (14 tests) and `description-parity.ratchet.test.ts` (351 tests) both re-verified green, byte-identical to their pinned goldens -- confirming the 14 zero-diff/11 ratchet-pinned fixtures' ids either never depended on the fixed mechanisms or already matched the true upstream order (matches the prompt's own prediction: "a correct fix cannot move them"). Temp scripts (`scripts/_tmp-i3b-*.ts`) all deleted before finishing. | diagnosis.md mechanism fix + TDD + re-measure + DOT-gate re-verify | no |
| 2026-07-14 | I4 | MECHANISM(S) (`text/@textLength` family, re-derived before touching anything: 33 fixtures/55 diffs, down from the README's stale 94/239 -- I3/I3b's ordering + I2's font-constant fixes already shrank it). Instrumented via temporary `scripts/_tmp-i4-*.ts` (find/dump/dump2/classify/fulldiff/measure, all deleted before finishing) walking `compareSvg`'s own `NormalizedNode` trees by the diff's own XPath-like path (same tag-counter grammar `compare.ts#compareNodes` emits) to pair each actual/expected `<text>` element exactly, rather than matching by textLength VALUE (an early, discarded attempt that paired unrelated elements). Classified all 33 fixtures' textLength diffs into "isolated" (same text, same x/y, textLength alone differs) vs "non-isolated": **0 isolated, 55 non-isolated** -- refuting the prompt's working assumption that this family is dominated by a pure rounding/format bug; the evidence instead shows THREE independent, jar-verified mechanisms plus several already-known/out-of-scope contributors. **(1) FIXED -- `format()`'s HALF_UP rounding operates on the double's exact binary expansion (JS `toFixed(4)`), not its shortest round-trip decimal string (Java `%.4f`/`java.util.Formatter`'s `FloatingDecimal`-based conversion)**: for a value whose shortest decimal sits exactly on a 4th-decimal-place boundary but whose true IEEE754 double is a hair below it (e.g. `10.7 * 0.8125 = 8.69375` as a JS literal, but the actual double is `8.6937499999999996447...`), `toFixed(4)` rounds DOWN ("8.6937") while Java's `%.4f` -- which rounds the value's shortest-round-trip decimal string "8.69375" HALF_UP, not the raw binary expansion -- rounds UP ("8.6938"). Jar-verified byte-for-byte on `component/luniju-97-tuja870` (edge label "X", `ARROW_LABEL_FONT_SIZE=13`, `charWidth('X')=10.7` from the SANS_SERIF table, `factor=13/16=0.8125`, `10.7*0.8125` computed identically in both languages per IEEE754 determinism -- confirmed via `x.toPrecision(20)` -> `8.6937499999999996447` in JS -- yet `x.toString()` -> `"8.69375"`, the SAME shortest-decimal Java's Formatter would round from) and `usecase/xacaxe-43-bupe002` ("cancel" 37.6187->37.6188, "switch()" 44.7687->44.7688, both edge labels at the same fixed size). Origin: `src/core/klimt/drawing/svg/svg-graphics-core.ts#format:446-450` (pre-fix: `trimTrailingZeros(x.toFixed(4))`), the SAME function every numeric SVG attribute (x/y/cx/cy/width/height/rx/ry/stroke-width/font-size/textLength/points/d) is emitted through -- but the bug only SURFACES as a visible diff for `textLength` (no `NUMERIC_ATTRS`/tolerance entry in `compare.ts`, exact-string compared) since every tolerance-compared attribute absorbs a 0.0001 last-digit divergence well within its 0.01 tolerance band. Jar: `java.util.Formatter`'s well-documented shortest-decimal-then-HALF_UP semantics (no single upstream PlantUML file to cite -- this is a JDK `Formatter`/`FloatingDecimal` behavior, not PlantUML application code; the causal chain is JS-vs-Java number-formatting semantics, not a porting mistake in any one method). **(2) FOUND, NOT FIXED -- per-element-type `FontSize` skinparam/style overrides are entirely unwired for entity/cluster/stereotype label text**: `renderer-symbol.ts#textFont:116-128` computes `size: theme.fontSize + sizeDelta` -- a SINGLE global constant (or the fixed per-role delta wired in I2) -- with NO per-SNAME or per-style-selector font-size lookup at all. `resolveSkinparam` (`skinparam.ts:139-327`) maps only a bare, diagram-wide `fontsize`/`defaultfontsize` key (line ~212); the existing D4 per-element-type override mechanism (`ELEMENT_BUCKET_SNAMES`/`matchElementColorKey`/`ELEMENT_ROLE_SUFFIXES`, `skinparam.ts:104-136`) covers ONLY `backgroundcolor`/`bordercolor`/`fontcolor` suffixes for a 7-SNAME allowlist (`database, component, node, actor, usecase, artifact, rectangle`) -- no `fontsize` suffix, and no `<style> <sname> { FontSize N }` selector routing either. Jar-verified against 9 sampled fixtures, ALL matching the SAME "ratio == jar-fontSize/14" signature (proving font-SIZE, not textLength math, is the actual defect): `component/kuciku-99-tedu217`/`mavicu-17-mago821` (`skinparam StereotypeFontSize 20`/`<style>stereotype{FontSize 20}` -> ratio 50/35=1.4286=20/14), `component/loroto-06-fano471`/`toxine-81-xofo986` (same + a PER-STEREOTYPE `StereotypeFontSize<<bar>> 10`/`.bar{FontSize 10}` override -> the SAME fixture's "foo" ratio 1.4286 (20/14) and "bar" ratio 0.714 (10/14) diverge in OPPOSITE directions, ruling out a single constant-offset explanation), `component/cukafa-49-fona812` (`skinparam componentFontSize 18`/`interfaceFontSize 18` -> ratio 18/14=1.2857 on `component1`/`interface`/`component2`), `component/fasave-91-jaka816`/`zonobi-55-zuna105` (`<style>component{FontSize 19}` etc -> ratio 19/14=1.357), `component/xagino-11-vazo768` (`skinparam package{FontSize 40}` -> ratio 40/14=2.857), `component/revusu-28-pexi248` (`<style>actor{FontSize 15}` -> ratio 15/14=1.0714). Also newly surfaced by the same sampling: `collections`/`database`/`label`/`package`/`interface`/`actor` are NOT in `ELEMENT_BUCKET_SNAMES` either, so `fasave-91-jaka816`'s `collections`/`database` labels get NEITHER the FontSize NOR the FontColor override (fill stays the jar-default `#000000`, not even the raw unresolved `green`) -- a second, adjacent pre-existing gap in the SAME bucket-allowlist infrastructure. Disposition: NOT FIXED here -- reach (9+ of 33 in-family fixtures, ~27%, almost certainly wider corpus-wide since font-size skinparams are common) and shape (skinparam per-SNAME key routing + style-block per-selector routing + a SEPARATE stereotype-specific variant of both + the adjacent SNAME-allowlist gap) make this a multi-file WIRING/FEATURE mechanism, not a targeted bug -- same class of judgment call as I2's deferred "per-diagram-type ArrowFont* skinparam family" (ledger.md I2, "NOT FIXED, unbuilt feature"). Flagged needs-signoff for its own future iteration (extend the D4 `ElementColors`-style bucket pattern with a parallel numeric `fontSize` field + widen `ELEMENT_BUCKET_SNAMES`, route `<style> stereotype {}`/`skinparam StereotypeFontSize<<X>>` separately since stereotype text uses `STEREOTYPE_STYLES` not a per-SNAME bucket at all). **Ruled out as I4-scope mechanisms** (each independently jar-verified, each a DIFFERENT root cause than textLength derivation): **(a) `scale N` directive entirely unimplemented for description diagrams** -- `component/saveje-35-vumu271` (`scale 2`, no per-diagram-type FontSize) shows EVERY coordinate/dimension/font-size uniformly x2 (`svg/@width 81->184`, `text/@font-size 14->28`, down to `rect/@stroke-width 1->2`) and `component/berome-43-xini276` (`scale 10`, clamped to an effective x4 -- likely a jar max-image-size clamp not investigated further, out of scope) shows the same uniform-multiplier signature across polygon points/lines/text/font-size; grep-verified zero wiring anywhere (`grep -rn "\\bscale\\b" src/diagrams/description src/core/klimt src/core/SvgOption.ts` -- no hits outside sprite-atom-unrelated code) -- a whole missing top-level directive, not a text-measurement bug (affects every drawn primitive, not just `<text>`). **(b) creole/entity-naming text-CONTENT bugs producing a DIFFERENT string than the jar measures** (textLength is then CORRECTLY derived for the wrong string, not a derivation bug): `component/junoxu-15-gori632`/`lurupu-11-fubo915` (unresolved `<U+00B5>`/`<U+1F601>` HTML-unicode-escape placeholders left literal instead of resolving to `µ`/`😁` -- a creole/unicode-escape gap), `component/xenusu-76-sabi405`/`xusuxe-62-guba767` (retained literal quote characters `"..."` around a display name the jar strips), `usecase/mutere-78-geko363` (retained literal `==` creole-heading markers), `usecase/fotisa-06-xipe681`/`saduja-80-goba120` (retained literal `:actor:`-style colon wrapping), `usecase/nenedo-78-fiva569` (literal `\n` two-character escape not converted to a real newline), `component/gafico-37-cuma657`/`nujito-06-neca370` (multi-line note content collapsed to a single unrelated character `"c"` -- a much deeper note-rendering gap). None investigated further -- each is its own distinct parser/creole mechanism, none is "textLength value derivation" by any reasonable reading of I4's charter. **(c) already-known, already-ledgered residuals re-confirmed, not re-diagnosed**: `usecase/dopova-50-digo290` ("Use"<->"Start" mismatch) is I3b's ledgered paren-shorthand-usecase missing-parser-rule gap (decision-journal I3b "Ruled out" -- same slug, same root cause, re-derived independently here as a cross-check); `usecase/gogamo-72-pibo470`/`xacaxe-43-bupe002`'s off-by-one entity/edge-position swaps are I3/I3b's ledgered residual node/edge draw-order gaps (mechanism C / the edge-draw-order note) -- `xacaxe-43-bupe002`'s "cancel"/"switch()" pairs (mechanism 1 above) coexist in the SAME fixture as this SEPARATE, already-ledgered ordering residual on its OTHER labels ("notify(...)"<->"/login?...", "(Facebook)"<->"cancel"). | diagnosis.md (instrument-before-hypothesize: path-exact actual/expected `<text>` element pairing via `NormalizedNode` tree-walk on 33 fixtures, classified isolated-vs-non-isolated before forming any hypothesis; jar Java read for the rounding mechanism -- `java.util.Formatter`/`FloatingDecimal` semantics -- and jar-side skinparam/style-block source read for the FontSize mechanism) | no |
| 2026-07-14 | I4 | Fix landed per mechanism (1) above, TDD (red before fix): `src/core/klimt/drawing/svg/svg-graphics-core.ts` gains `javaFixed4(x)` (new, ~25 ln) -- takes the value's shortest round-trip decimal via `Math.abs(x).toString()` (JS's own `Number->String` conversion is the same "shortest, correctly-rounded round-trip" algorithm class `Double.toString` uses, so no bespoke binary-to-decimal implementation was written), then rounds THAT decimal string HALF_UP to 4 fractional places via `BigInt` digit-string arithmetic (avoids a second binary round-trip that would reintroduce the same divergence); `format()` (`svg-graphics-core.ts:446-450`) now calls `trimTrailingZeros(javaFixed4(x))` instead of `trimTrailingZeros(x.toFixed(4))`. One new pinning test in `tests/unit/core/klimt/svg-graphics.test.ts` ("rounds HALF_UP off the shortest round-trip decimal..."), red-verified against the pre-fix code (asserted `cx="8.6938"` for `svgEllipse(8.69375, ...)`, failed with `cx="8.6937"` before the fix) then green after. Full existing `svg-graphics.test.ts` suite (62 tests, incl. the pre-existing `rx="3.1416"` from-3.14159 case, which sits nowhere near a rounding boundary and is unaffected) re-verified green, unchanged. Re-measured (`--families`): `svg/g/g/text/@textLength` 33 fixtures/55 diffs -> 32/52 (both `component/luniju-97-tuja870`'s and `usecase/xacaxe-43-bupe002`'s rounding-only diffs closed; neither fixture reaches zero-diff overall since both carry SEPARATE, already-ledgered/out-of-scope residuals -- luniju a pre-existing node/edge draw-order divergence unrelated to this family, xacaxe the I3-ledgered edge-draw-order gap on its OTHER labels). Full census: conformant HELD at 14/355, buckets UNCHANGED (1-3:22, 4-10:107, 11-30:60, 31+:151, errors:1) -- the exact same 14-fixture zero-diff SET verified identical (not just count) before/after via direct list diff, confirming zero regression and (as expected, per the prompt's own prediction) zero new ratchet-eligible fixtures from this fix alone. DOT gate re-verified frozen EXACT: component 262/262, usecase 90/90, class 708/708, object 78/80, state 267/267 (file touched is description-engine-adjacent but shared SVG-emission infra -- re-verified rather than assumed, since `format()` is used by every klimt-backed diagram type, not just description; DOT emission is a wholly separate, untouched pipeline). Full suite: 308/308 files, 8254/8254 tests (8253 baseline +1 new). typecheck/lint/build all clean. Ratchet: no changes (no new zero-diff fixtures; the 11 ratchet-pinned + 3 census-only zero-diff fixtures all re-verified byte-identical to their pinned goldens via the full ratchet suite, 14+351 tests green). Temp scripts (`scripts/_tmp-i4-*.ts`) deleted before finishing. Mechanism (2) (FontSize skinparam/style wiring gap) and the ruled-out items (a)/(b)/(c) above are journaled but NOT fixed this iteration -- see the mechanism row and `ledger.md`'s I4 section for full disposition/needs-signoff detail. | diagnosis.md mechanism fix + TDD + re-measure + DOT-gate re-verify | no |
| 2026-07-14 | I4b | MECHANISM (per-element FontSize/StereotypeFontSize skinparam + <style> wiring, resuming I4's own deferred mechanism (2) -- I4's 9 jar-verified sample fixtures re-confirmed unchanged before touching anything: `component/kuciku-99-tedu217`, `mavicu-17-mago821`, `loroto-06-fano471`, `toxine-81-xofo986`, `cukafa-49-fona812`, `fasave-91-jaka816`, `zonobi-55-zuna105`, `xagino-11-vazo768`, `revusu-28-pexi248`). Read each fixture's raw `.puml` (`~/git/pdiff/dbhum/`) to pin the EXACT syntax shape before hypothesizing a fix shape: kuciku = `<style> node { stereotype { FontSize 20 } } </style>`; mavicu = `skinparam node { StereotypeFontSize 20 }` (BLOCK form, not bare -- I4's paraphrase "skinparam StereotypeFontSize 20" omitted the `node {}` wrapper); loroto = the SAME `<style>` shape PLUS a nested `.bar { FontSize 10 }` per-stereotype-NAME sub-override; toxine = the SAME skinparam-block shape PLUS a second block `skinparam node { StereotypeFontSize<<bar>> 10 }`; cukafa = flat `skinparam componentFontSize 18` / `interfaceFontSize 18` (plus co-occurring `interfaceBorderColor`/`componentFontColor` etc, already-broken siblings of the SAME allowlist gap); fasave/zonobi = `<style> component { FontSize 19 } collections/database/label { FontSize 19 } </style>`; xagino = `skinparam package { ... FontSize 40 }` (block form); revusu = `<style> actor { ... FontSize 15 ... } </style>`. Read upstream Java for the true precedence chain (full-tree grep per CLAUDE.md's silito-78 caution, not scoped to `net/sourceforge/plantuml/`): `FontParam.java#getFontConfiguration` -> `FontConfiguration.create(ISkinParam, FontParam, Stereotype)` -- but `EntityImageDescription.java:172-174` does NOT call this FontParam-direct path at all; it resolves `styleTitle`/`style`/`styleStereo` as THREE separate `StyleSignatureBasic` queries (`[root,element,<diagramType>,<sname>,title]` / `[root,element,<diagramType>,<sname>]` / `[root,element,<diagramType>,<sname>,stereotype]`) each calling `Style#getFontConfiguration`, which pulls `PName.FontSize` off the MERGED style sheet. The style sheet itself is populated from TWO sources merged into ONE cascade: (a) `<style>` blocks, parsed directly into `Style` objects keyed by their literal SName path; (b) EVERY skinparam key, converted into an EQUIVALENT `Style` rule at parse time via `FromSkinparamToStyle.java`'s static registration table -- `addMagic(sname)` (lines 270-286) registers `<sname>FontSize` -> `[[sname],FontSize]` AND `<sname>StereotypeFontSize` -> `[[stereotype,sname],FontSize]` as TWO SEPARATE, independently-resolved style rules (confirmed: no bare, sname-less `StereotypeFontSize` registration exists anywhere in the static block -- I4's paraphrase of kuciku/mavicu as "generic" was imprecise; every reachable stereotype-font-size skinparam requires an sname prefix, matching all 9 fixtures' actual syntax). `addMagic` is called for `agent, artifact, boundary, card, cloud, collections, component, control, database, entity, file, folder, frame, hexagon, interface_, node, package_, person, queue, rectangle, stack, storage, usecase` (plus the four early explicit calls `boundary, control, collections, actor, database, entity`) -- restricted to `descriptive-keywords.ts`'s USymbol union, this is 24 SNames, of which only 7 (`database, component, node, actor, usecase, artifact, rectangle`) were in this port's `ELEMENT_BUCKET_SNAMES` (`skinparam.ts:104-112`) -- CONFIRMS I4's "adjacent gap" finding: `interfaceFontSize`/`interfaceBorderColor` (cukafa), `collectionsFontSize`/`databaseFontSize` (fasave), `packageFontSize` (xagino) were ALL unreachable, not just the FontSize suffix. `label` has NO `addMagic` registration (verified absent) but zonobi's `<style>label{FontSize 19}` still must resolve, because `<style>` block routing bypasses `addMagic` entirely (addMagic is skinparam-to-style TRANSLATION only; a literal `<style>label{}` block builds its Style rule directly against `SName.label`) -- this port's OWN architecture conflates the two mechanisms into one `ELEMENT_BUCKET_SNAMES` allowlist shared by both `resolveSkinparam` (skinparam path) and `collectElementStyleBuckets` (style-block path), so `label` needs allowlist membership for the style-block half even without an `addMagic` counterpart for the skinparam half (a pre-existing, out-of-scope architectural note, not something restructured here). Precedence/cascade: `styleStereo`'s selector `[stereotype,sname]` is STRICTLY MORE SPECIFIC than `style`'s `[sname]` alone -- PlantUML's style-merge (`StyleSignatureBasic`/`Style#getMergedStyle`, general CSS-cascade shape: more-specific selectors override, less-specific ones remain as a fallback for any property the specific one doesn't set) means a plain `<sname>FontSize`/`<style><sname>{FontSize}}` rule with NO stereotype-specific counterpart SHOULD still apply to that sname's stereotype text -- not independently jar-verified against one fixture combining both (none of the 9 samples do), but it is the correct reading of the cascade architecture itself, not a guess from nothing. THREE further sub-mechanisms surfaced, deliberately NOT fixed this iteration (see Disposition): (i) per-stereotype-NAME sub-overrides (`StereotypeFontSize<<bar>> 10` / `.bar { FontSize 10 }`, loroto/toxine) -- upstream's `getFirstValueNonNullWithSuffix("fontsize" + stereotype.getLabel(...), param)` checks a STEREOTYPE-LABEL-qualified key BEFORE the plain sname key; this port's preprocessor `RE_SKINPARAM_BLOCK_ENTRY = /^\\s*(\\w+)\\s+(.+)$/` (`preprocessor.ts:54`) cannot match `StereotypeFontSize<<bar>> 10` at all (`\\w+` cannot cross the `<` characters, and there is no whitespace between `FontSize` and `<<bar>>`) -- the line is SILENTLY DROPPED before it ever reaches `resolveSkinparam`; separately, `resolveSkinparam`'s existing `rawKey.includes('<<')` guard (`skinparam.ts:180-183`) treats EVERY stereotype-qualified key as blanket-`unknown`, so even a syntactically-flat `stereotypefontsize<<bar>>` key would still be rejected today. The `<style>` equivalent (`.bar { FontSize 10 }` nested one level inside `stereotype {}`) technically already parses via `parseStyleBlock`'s existing dot-led-selector branch (`skinparam.ts:450`, unmodified) into a selector path `"<sname>.stereotype..bar"` (a literal double-dot, an existing quirk of that pre-existing code, not touched here) -- `collectElementStyleBuckets`'s new `.stereotype`-suffix check (added this iteration) does NOT match that path (it ends in `..bar`, not `.stereotype`), so it is correctly left unrouted rather than silently misrouted. (ii) 'note' font-size/color (`addConFont("note", SName.note)`, a real, non-`addMagic` upstream registration) -- NOT added to `ELEMENT_BUCKET_SNAMES` this iteration: `drawNoteFallback` (`renderer-entity.ts`) already uses a SEPARATE, non-`resolveElementPaint` background-color path (`theme.colors.noteBackground`/`theme.colors.border` literals, not the bucket system at all), so wiring `note`'s FONT size/color through the SAME allowlist as the 24 `addMagic` snames would mix two different, not-yet-unified mechanisms without a sampled fixture to verify against -- deferred, not evidenced as broken by any I4 sample. (iii) 'circle'/'action'/'process'/`actor-business`/`usecase-business` USymbols -- no confirmed upstream SName mapping traced (not investigated further; no I4 sample exercises them), left out of the widened allowlist rather than guessed. | diagnosis.md (instrument-before-hypothesize: read each of the 9 fixtures' raw .puml source before forming the fix shape; full-tree jar Java read across `klimt/font/FontParam.java`, `abel/Entity.java`, `svek/image/EntityImageDescription.java`, `style/FromSkinparamToStyle.java`, `skin/SkinParam.java`, `klimt/font/FontConfiguration.java` before any fix) | no |
| 2026-07-14 | I4b | Fix landed per the mechanism above, TDD (tests written and verified failing against the pre-fix code before implementing, per each fixture's exact syntax shape): `theme.ts` -- `ElementColors` gains additive `fontSize?: number` / `stereotypeFontSize?: number` fields, plus a new `resolveElementFontSize(theme, sname, role: 'title'|'stereotype')` helper (parallels `resolveElementPaint`'s cascade shape, returns `undefined` -- not a hard default -- so callers keep their own `sizeDelta` fallback). `skinparam.ts` -- `ELEMENT_BUCKET_SNAMES` widened from 7 to 25 entries (the 24-SName `addMagic` set restricted to `descriptive-keywords.ts`'s USymbol union, plus `label` for the style-block-only reason above); a new `matchElementFontSizeKey` (mirrors the existing `matchElementColorKey`, `fontsize`/`stereotypefontsize` suffixes) plus a `resolveSkinparam` default-branch fallthrough (`elem` bucket-color match first, THEN font-size match, THEN `unknown`) route the two new suffixes into the same per-sname bucket, numeric (non-finite values recorded as `unknown`, not silently dropped). `style-map-element.ts#collectElementStyleBuckets` -- gains a `fontsize` prop read on the existing bare-selector branch, PLUS a new `<sname>.stereotype` selector-suffix branch (merges into the SAME sname bucket as `stereotypeFontSize`, order-independent via `{...elements[sname], ...bucket}` spread-merge regardless of which selector the Map visits first). `renderer-symbol.ts#textFont` -- gains a `role: 'title'|'stereotype' = 'title'` parameter; `size` is now `resolveElementFontSize(theme, symbol, role) ?? theme.fontSize + sizeDelta` (was: unconditionally `theme.fontSize + sizeDelta`). `renderer-entity.ts`/`renderer-cluster.ts` -- both stereotype `textFont(...)` call sites pass `role: 'stereotype'` (title call sites are unchanged, defaulting to `'title'`); `renderer-edge.ts` untouched (its `ARROW_LABEL_FONT_SIZE` literal is the SEPARATE, already-ledgered "ArrowFont*" unbuilt-feature gap, I2/I4 -- out of this mechanism's scope, per-element FontSize never applied to edge labels upstream either). 19 new pinning tests: 5 in `skinparam.test.ts` (componentFontSize/interfaceFontSize dual-bucket, block-form packageFontSize, nodeStereotypeFontSize distinct-from-fontSize, database dual fontSize+stereotypeFontSize non-collision, non-numeric-value-recorded-as-unknown), 4 in `style-map-element.test.ts` (bare `<sname>{FontSize}`, `<sname>.stereotype{FontSize}`, bare+stereotype bucket MERGE, non-bucket-SName `.stereotype` suffix ignored), 5 in `theme.test.ts` (`resolveElementFontSize`: undefined-when-unset, plain-title-override, stereotype-cascades-to-plain-fontSize per cukafa, stereotype-specific-wins-over-plain per loroto's shape, stereotype-override-does-NOT-leak-into-title), 5 end-to-end in `description/renderer.test.ts` (leaf title override per cukafa, a DIFFERENT sname unaffected by another's override, leaf stereotype independent-of-title override per mavicu, stereotype-falls-back-to-plain-FontSize cascade, container title override per xagino). Re-measured (`--families`): `text/@font-size` 16 fixtures/26 diffs -> 8/11; `text/@textLength` 32/52 -> 24/37 (correct font-size changes several entities' jar-matching character-width measurement, closing coincidental textLength mismatches as a bonus, same mechanism class as I2's font-style bonus closure); `text/@font-style` untouched at 3/3 (unrelated, already-closed family, no regression). Full census: conformant HELD at 14/355, SAME exact 14-fixture zero-diff set verified via direct list comparison (`component/{buduni-98-bima526,cifaki-66-boxa005,jesibe-85-sozu187,pamake-46-sadu464,sipuma-82-fene185,vacuxi-18-baxu582,vapalu-27-muxa300,vumija-03-xise495}`, `usecase/{dipixi-71-nuga611,kevipe-39-gaji640,majuma-84-loma401,norebe-58-bixu182,sidame-35-cozu078,zoriso-46-vata931}`) -- no NEW zero-diff fixture from this fix alone (every affected fixture also carries an out-of-scope geometry/color/creole gap keeping it non-zero, same pattern I4's own mechanism (1) predicted for itself), so no ratchet backfill this iteration. Buckets: 1-3 held at 22, 4-10 107->110 (+3, net movement INTO this bucket from smaller-improvement fixtures elsewhere in the corpus beyond the 9 named samples), 11-30 60->58 (-2), 31+ 151->150 (-1), errors held at 1 -- net corpus-wide improvement, zero regressions (no fixture moved to a WORSE bucket, confirmed by the 1-3/0-diff counts holding exactly and the family-diff counts only shrinking). DOT gate re-verified frozen EXACT: component 262/262, usecase 90/90, class 708/708, object 78/80, state 267/267 (all six touched files are description-engine + shared theme/skinparam infra, zero DOT-emission-layer files touched). Full suite: 308/308 files, 8273/8273 tests (8254 baseline +19 new). typecheck/lint/build all clean. Ratchet: unchanged (14 census zero-diff / 11 ratchet-pinned / 3-fixture gap, same as I3's ledgered backfill state -- not re-attempted here, out of this iteration's mechanism). Deferred sub-mechanisms (i)/(ii)/(iii) above are journaled but NOT fixed -- see `ledger.md`'s I4b section for full disposition. No temp instrumentation scripts were created this iteration (fixture `.puml` sources were read directly from `~/git/pdiff/dbhum/`, and all new coverage lives in permanent unit tests, not throwaway scripts). | diagnosis.md mechanism fix + TDD + re-measure + DOT-gate re-verify | no |
| 2026-07-14 | I4c | MECHANISM(S) + fix landed (creole text-CONTENT bugs, resuming I4's ruled-out list, 6 named suspects re-verified individually per diagnosis.md before any fix; a temporary `scripts/_tmp-i4c-*.ts` pair (text-content-set diff across the full 355-fixture corpus, then a per-slug full-diff dump) enumerated the real reach and confirmed each fix's before/after content-diff disposition, both deleted before finishing). FOUR tractable, independent mechanisms diagnosed and FIXED (full detail + jar Java citations in ledger.md I4c): (1) `<U+XXXX>`/`&#NNN;` unicode/entity escapes left literal (AtomText.manageSpecialChars, klimt/creole/legacy/AtomText.java:89-163) -- new `resolveTextEscapes` (parse-helpers.ts), wired into `extractNodeStereotype` and `parseNameSection` via a new `finalizeDisplay`. (2) link-label quote retention when the WHOLE post-colon label is one quoted string (Labels.java#init's unconditional fallback, java:102, outside the 3-branch null-guard) -- `applyEmbeddedQualifiers` (link-grammar.ts) restructured to match Java's exact control flow. (3) colon/paren-wrapped DISPLAY before "as CODE" never stripped (CommandCreateElementFull.executeArg:311's unconditional strip, only applied conditionally per-branch in this port) -- `finalizeDisplay`'s unconditional `stripFullWrap`. (4) literal two-char `\n`/`\r`/`\l` escape not converted to a real newline (Display.getWithNewlines, klimt/creole/Display.java:259-343) -- new `resolveNewlineEscapes` (parse-helpers.ts), wired into `finalizeDisplay` after the quote-strip. All four land in `src/diagrams/description/{parse-helpers,link-grammar}.ts` only -- no renderer string-patching, fix at the parse/creole layer per the prompt's own instruction. TWO mechanisms ruled out as blocked-on-E2-remainder (ledger.md I4c, full disposition): (5) `==` creole-heading marker retention -- requires a per-LINE font-styling cascade (CreoleStripeSimpleParser + fontConfigurationForHeading) this port has no seam for (one uniform per-entity font today, not per split line); a content-only marker-strip would leave font-size/weight wrong on that line, a worse-not-better partial fix. (6) multi-line note / nested creole markup (`<b>`, `<font>`, `<color:>`, `<size:>`, `<u:>`, TIM `$var` interpolation with embedded `<U+000A>`) -- confirmed via the full-corpus content-set scan to be ONE broad mechanism (no full creole atom/tag model exists in this port at all) reaching far beyond the 2 originally-named suspects (~45 additional fixtures show the identical signature, enumerated in the ledger, not individually diagnosed -- word-wrapped long single-line text is the SAME root cause via a distinct symptom). TDD: 8 new pinning tests (6 in `parser.test.ts` -- 2 colon/paren-wrapped-display-before-as, 3 unicode/entity-escape resolution, 1 newline-escape; 2 in `parser.test.ts`'s embedded-qualifier-labels block -- whole-quoted-label strip, fallback-applies-even-with-explicit-qualifiers), all red-verified against the pre-fix code via the pre-existing test run (which showed 2 UNRELATED pre-existing tests failing as a mechanism-4 side effect -- see below) before implementing. Two PRE-EXISTING tests pinning the OLD (unconverted-newline) behavior were corrected, not just extended, per diagnosis.md precedent (I3's identical judgment call): `parse-helpers.test.ts`'s vivido-49-nisu863 case (`id` keeps its literal `\n`, upstream `quark.getName()` never runs through `Display.getWithNewlines`; `display` now diverges from `id` and gets a real newline, matching upstream exactly) and `parser.test.ts`'s `b\n====\ncan be used by a` container-open case (now 3 real lines; the `====` middle line stays literal text, per-line heading/separator styling being mechanism-5's ledgered gap). Re-measured (per-slug full-diff dump on all 6+ affected fixtures): zero remaining text-CONTENT diffs on any of them -- the surviving diffs are all unrelated, pre-existing, out-of-scope geometry/color/style gaps (component/xenusu-76-sabi405's unresolved `#PRIMARY_COLOR`/`#BORDER_COLOR` named-skinparam-variable syntax; component/xusuxe-62-guba767's DOGU TIM-macro stereotype formatting; general rect/ellipse/path sizing deltas already tracked by I7/I9/I10+). Full census: conformant HELD at 14/355 (SAME exact set, verified via direct list comparison against I4b's), 1-3 held at 22, 4-10 110->104 (-6), 11-30 58->57 (-1), 31+ 150->157 (+7), errors held at 1 -- net movement is a masking-artifact (I3's own documented pattern): fixing content-level diffs lets `compareSvg`'s tree-walk recurse PAST the point where a childCount/text mismatch previously short-circuited it, unmasking pre-existing geometry diffs on fixtures whose multi-line display (mechanism 4) or label shape (mechanism 2/3) changed structurally -- confirmed via the per-slug diff dumps above showing zero content diffs remain on every one of these fixtures, only unrelated/pre-existing geometry. DOT gate re-verified frozen EXACT: component 262/262, usecase 90/90, class 708/708, object 78/80, state 267/267 (both touched files are description-engine parse-layer only, zero DOT-emission-layer files touched). Full suite: 308/308 files, 8281/8281 tests (8273 baseline +8 new). typecheck/lint/build all clean. Ratchet: unchanged (14 census zero-diff / 11 ratchet-pinned / 3-fixture gap, re-verified byte-identical via the full ratchet suite, 351+14 tests green) -- no new zero-diff fixtures (every I4c-affected fixture also carries a separate out-of-scope gap, matching the pattern every I4/I4b iteration predicted for itself). Mechanisms 5/6 are journaled in ledger.md I4c as needs-signoff/blocked-on-E2-remainder respectively, not guessed at or partially hacked. | diagnosis.md (instrument-before-hypothesize: full-corpus text-content-set diff before touching anything, jar Java read for all 4 mechanisms -- AtomText.java, Labels.java, CommandCreateElementFull.java, Display.java -- before any fix; TDD red-before-fix; per-slug diff-dump audit post-fix to rule out the masking-artifact-vs-regression question, matching I3's own precedent) | no |
| 2026-07-14 | ruling | MAINTAINER RULING: conformance bars are now globally "100% minus known divergences" — the ≥90% slack is retired across Phase A/D/G and this mission's exit bar. Practical G1 consequence: the drill runs until every non-zero-diff fixture has a named mechanism disposition (fixed / DIVERGENCES entry / ledger entry), not until a percentage. | maintainer, chat 2026-07-14 | no |
| 2026-07-14 | I-scale | MECHANISM (`scale N` directive, entirely unimplemented, resuming I4's own ruled-out item (a) -- its 2 named evidence fixtures re-confirmed unchanged before touching anything: `component/saveje-35-vumu271` (`scale 2`), `component/berome-43-xini276` (`scale 10`)). Jar Java read (full-tree, not scoped): `command/CommandScale.java` (`scale N` / `scale N/M` -> `new ScaleSimple(scale)`) + 5 siblings all registered by `command/CommonCommands.java#addCommonScaleCommands` (`CommandScaleWidthAndHeight` `scale WxH`/`W*H`, `CommandScaleWidthOrHeight` `scale N width|height`, `CommandScaleMaxWidth`/`MaxHeight` `scale max N width|height`, `CommandScaleMaxWidthAndHeight` `scale max WxH`), all reachable for component/usecase via `DescriptionDiagramFactory.java:90`'s `CommonCommands.addCommonCommands1` -> `addCommonScaleCommands`. Every `Scale*.java` implementation (`ScaleSimple`/`ScaleWidth`/`ScaleHeight`/`ScaleWidthAndHeight`/`ScaleMaxWidth`/`ScaleMaxHeight`/`ScaleMaxWidthAndHeight`) extends `ScaleProtected`, whose `getScale(width,height)` (`ScaleProtected.java:42-51`) clamps the strategy-specific raw factor: `<=0 -> 1`, `>4 -> 4` -- jar-verified as the EXACT explanation for `berome-43-xini276`'s "`scale 10`" rendering at an effective x4 (`text/@font-size="56"` = base-14 * 4, not * 10). Application chain traced end-to-end: `UgDiagram#getExporter` (java:139) -> `TextBlockExporter.Builder#styled(diagram)` sets `this.scale = diagram.getScale()` (the strategy object, unresolved) -> `TextBlockExporter#computeScaleFactor(dim)` (java:205-209) resolves it to a scalar FACTOR via `scale.getScale(dim.width, dim.height) * dpi/96.0` against `dim = calculateFinalDimension()` (the diagram's own UNSCALED document size, i.e. `SvekResult#calculateDimension` + the `CucaDiagram` outer margin -- this port's `computeDocumentDims`; dpi is always 96 here, no `skinparam dpi` wiring exists anywhere, so that term is always 1) -> `createUGraphicSVG` sets `SvgOption.scale = scaleFactor` -> `SvgGraphics.java#format(xx)` (java:881-884, the ONE low-level number formatter every emitted coordinate/font-size/stroke-width/textLength/points/path-d value passes through) multiplies `xx * option.getScale()` BEFORE formatting to a string, and `#finalizeRootAttributes` (java:750-773) separately computes `maxXscaled = (int)(maxX*option.getScale())` for the root `viewBox`/`width`/`height`/style. Jar-byte-verified against `saveje-35-vumu271`'s cached `in.svg` (`scale 2`): EVERY primitive (`rect/@x`, `text/@font-size` 14->28, `path` `stroke-width` 1->2, `ellipse/@rx`) is exactly 2x its unscaled value, and root dims are the scaled extent -- coordinates are baked at format() time, NOT a root `<g transform="scale(...)">` wrapper. `ensureVisible` (java:126-131, called with RAW unscaled coordinates from every draw call, including `ensureVisible(minDim.getWidth(), minDim.getHeight())` at construction) accumulates `maxX`/`maxY` entirely in the UNSCALED coordinate space -- DOT/svek layout reads NONE of `option.scale`, confirming scale is a pure SVG-emission-time concern, never a layout concern. GREENFIELD FINDING (not a defect -- diagnosis.md's "building new behavior" carve-out applies): `svg-graphics-core.ts` ALREADY carries a byte-faithful, pre-existing port of this ENTIRE emission-layer mechanism (`SvgOption.scale: number` field, default `1.0`; `format(xx){ const x = xx*this.option.scale; ...}`; `finalizeRootAttributes(){ const maxXscaled = Math.trunc(this.maxX*this.option.scale); ...}`) -- verified nothing in `svg-graphics-core.ts`/`svg-graphics-elements.ts`/`svg-graphics-shadow.ts` needed ANY change. The entire unimplemented surface was: (1) nothing parses `scale ...` into the AST, (2) nothing computes a factor from a `ScaleSpec`, (3) nothing threads a non-default value into `SvgOption.scale` at render time. Ruled out: chrome-interaction (upstream applies scale to title/legend chrome too, since `addChrome` runs before `TextBlockExporter`, but this port's `applyChrome`/`assembleKlimtShell` are STRING-level post-processing on an ALREADY-rendered, already-scaled klimt SVG body -- a pre-existing, out-of-scope architectural gap per I1's own precedent; confirmed moot for this iteration since none of the 5 corpus scale fixtures carry title/caption/legend/header/footer annotations). | diagnosis.md (instrument-before-hypothesize: read `CommandScale*.java` x6 + `Scale*.java` x7 + `ScaleProtected.java` + `TextBlockExporter.java` + `SvgGraphics.java`'s `format`/`finalizeRootAttributes`/`ensureVisible` before any fix; jar-byte-verified `saveje-35-vumu271`/`berome-43-xini276`'s cached `in.svg`; grep-confirmed `svg-graphics-core.ts` already carries the emission-layer mechanism faithfully) | no |
| 2026-07-14 | I-scale | Fix landed per the mechanism above, TDD (red-then-green, all new code): `src/core/scale-command.ts` (new, diagram-type-agnostic, no AST/geometry imports) -- `ScaleSpec` discriminated union (7 variants, one per `Scale*.java`), `matchScaleCommand(line)` (table-driven dispatch over 6 anchored regexes mirroring `CommandScale*.java`'s registration order, CCN-flat per the complexity hook), `resolveScaleFactor(spec, width, height)` (one arm per variant, `clampScale` shared helper mirroring `ScaleProtected#getScale`'s `<=0->1`/`>4->4` clamp). Wiring (description engine only, per the prompt's explicit allowance -- parse LAYER is the shared/reusable piece): `ast.ts` gains `DescriptionDiagramAST.scale?: ScaleSpec` (mirrors the `kermor`/`linetype` pattern); `command-table.ts` gains one COMMANDS rule (`/^scale\s/i` trigger + `matchScaleCommand(match.input)`, positioned with its sibling directives before rule 3); `layout-helpers.ts`'s `DescriptionGeometry` gains `scale?: ScaleSpec` (mirrors the `seed`/`sprites` passthrough pattern, doc-commented as type-carrying-only); `layout.ts` threads `ast.scale` through all 3 `DescriptionGeometry` producers (empty-AST, degenerate-single-leaf, normal); `renderer.ts#renderDescription` computes `scale = resolveScaleFactor(geo.scale, width, height)` against the SAME unscaled `{width,height}` already computed for `SvgOption.minDim` (before `basicSvgOption(...)` is called) and passes it as the new `scale` field -- `minDim` itself stays unscaled (mirrors `ensureVisible(minDim.getWidth(), minDim.getHeight())`'s own unscaled semantics). Diagnostic finding worth recording: `resolveScaleFactor`'s `width`/`height` params are the PRE-`ensureVisible` `computeDocumentDims` result (this fixture's baseline measured 20x20, not the root's own post-`ensureVisible` `Math.trunc(x)+1` padded 21x21) -- verified as the FAITHFUL match to upstream (`Scale#getScale(dim.width,dim.height)` is called on `calculateFinalDimension()`'s result, BEFORE `SvgGraphics`'s constructor -- and therefore `ensureVisible` -- ever runs), not a bug; a test that initially assumed the padded 21x21 baseline failed and was corrected once the true upstream call order was re-traced (see `renderer.test.ts`'s `scale width N` test comment). 47 new tests: `tests/unit/scale-command.test.ts` (34, 100% line/branch/function coverage on the new module, all 6 command forms + the clamp + zero/malformed rejection), `parser.test.ts` (+6, `ast.scale` pinning for 3 forms + default-undefined + malformed-no-crash), `layout.test.ts` (+6, `geo.scale` passthrough across all 3 producers + a "does not perturb node/edge geometry" invariant + one real-source end-to-end case), `renderer.test.ts` (+7, exact scaled root-dim bytes for `scale 2`/clamped `scale 10`/fractional `scale 1.5`/`scale width N`/`scale max` never-enlarges, plus a font-size-doubles-on-a-real-leaf interior check). Re-measured (temporary `scripts/_tmp-iscale-diffcount.ts`, deleted before finishing) against all 5 corpus scale fixtures: `berome-43-xini276` (`scale 10`) diffCount 0 (NEW zero-diff, direct proof the ScaleSimple+clamp mechanism is byte-exact); `givape-84-xano421` (`scale 200 height`) root width/height/viewBox now EXACT (proves `ScaleHeight` resolves correctly), 24 small (~1.5px pre-scale) interior residuals remain, ruled out as scale-related and NOT diagnosed further (ledgered); `saveje-35-vumu271` (`scale 2`) 18 residual diffs, ALL attributable to two already-separate, already-ledgered-or-newly-ledgered gaps (named-CSS-color `grey`->`#808080`, I2/I4's ruled-out item; and an interface-lollipop-decor childCount gap, newly observed, not diagnosed further this iteration -- see ledger); `cumofi-94-lixe862` (`scale 3`) 5 residual diffs, all attributable to `skinparam handwritten true` being an entirely unimplemented drawing backend (newly ledgered); `vimulo-11-buni641` (`scale 1000 width`) 27 residual diffs, entirely downstream of I4c's already-ledgered `<latex>` gap (cross-referenced, not re-ledgered as a new entry). Full census: conformant 14->15 (+1: `component/berome-43-xini276`), buckets 1-3 held at 22, 4-10 held at 104, 11-30 57->59 (+2), 31+ 157->154 (-3), errors held at 1 -- net corpus-wide improvement (scale multiplies EVERY primitive, so many non-scale fixtures with a `scale` directive show reduced diff counts even without reaching zero), zero regressions (no fixture moved to a worse bucket; 355 total held exactly). DOT gate re-verified frozen EXACT: component 262/262, usecase 90/90, class 708/708, object 78/80, state 267/267 (every touched file is description-engine-only or a new, description-only-consumed shared module; zero DOT-emission-layer files touched, and `ast.scale`/`geo.scale` are read by nothing in the DOT/layout pipeline -- confirmed by construction, no code in `layout.ts`'s DOT-input-building functions references either field). Full suite: 308->309 files, 8281->8335 tests (47 new unit tests + 1 new ratchet AC1 pinning test). typecheck/lint/build all clean. Ratchet: `berome-43-xini276` backfilled (`oracle/goldens/svg-description/ratchet.json` 11->12 entries, `oracle/goldens/svg-description/component/berome-43-xini276/{in.puml,golden.svg}` added from `test-results/dot-cache`, eligibility confirmed via `parity.json`'s `dotEqual:true` entry for that slug per the established AC3 gate) -- `description.golden.ratchet.test.ts` 14->15 tests, green. The pre-existing 15(census)-vs-12(ratchet) gap for the OTHER 3 census-zero fixtures (`pamake-46-sadu464`/`sipuma-82-fene185`/etc. -- wait, already ratcheted from I2/I3) is fully closed for THIS iteration's own addition; any residual census-vs-ratchet gap predates this iteration and is unrelated to scale. Temp script `scripts/_tmp-iscale-diffcount.ts` deleted before finishing. Ledger: 3 new entries added (`skinparam handwritten` unimplemented, `<latex>`-downstream cross-reference, unresolved givape-84 interior residual). | diagnosis.md mechanism fix + TDD + re-measure + DOT-gate re-verify + ratchet backfill | no |
| 2026-07-14 | I5 | SUB-CLASSIFICATION (childCount family, re-derived before touching anything: `svg/g/g[childCount]` 99 fixtures/249 diffs, `svg/g[childCount]` 64/64, plus the already-ledgered `svg[childCount]` residue -- matches the README's 129+64 estimate closely enough to proceed without re-triage). Built a temporary classifier (`scripts/_tmp-i5-classify.ts`, deleted before finishing) that resolves every `[childCount]` diff's XPath back into both normalized trees (actual/expected) and diffs their children's tag multisets -- printed a reach-sorted table + a per-group sample dump (slug/path/parent attrs). Classified into 9 sub-families (full table + jar-verified evidence per row in ledger.md I5): (A) chrome sibling-<g> nesting, already ledgered (I1 residual) -- excluded, re-verified unchanged at 18-19. (B) port fallback (`EntityImagePort.drawU`) never drew its label text, ~20 fixtures -- DRILLED (below). (C) entity/link multi-stereotype only renders the FIRST `<<tag>>` (12 fixtures, 2+ consecutive stereotype tags corpus-wide; `mamase-39-buto560` is an upstream-style dedicated stress fixture, `component 3..9 <<1>>..<<9>>`) -- `extractNodeStereotype`'s OWN doc comment (parse-helpers.ts:319-335) already named this exact gap as deferred ("a text-metric detail"); `EntityImageDescriptionSupport.ts#buildStereo` already stacks N stereotype lines correctly once fed an array -- only `DescriptiveNode.stereotype: string` (single, not `string[]`) blocks it -- queued I5b. (D) bracket-body `[Line1\nLine2]` shorthand literal `\n` unsplit (saxosu-09-nodi002, seguci-13-zure968 -- `finalizeDisplay`'s `resolveNewlineEscapes` (I4c) doesn't reach the `[...]`-bracket parse path) -- queued I5c. (E) transparent/near-zero-alpha color (`FontColor transparent`, `BackgroundColor transparent`, `#00000000`) draws the element instead of eliding it entirely (jar-verified on component/cobadu-43-gabi397's `FontColor transparent`/`#00000000` card/node labels -- jar omits the `<text>` outright; component/catari-10-xiza828's `skinparam BackgroundColor transparent` -- jar's root `style` carries no `background:` property and no content-level `<rect>` at all) -- ~14 fixtures grep-confirmed, 37 diff instances (25 text + 12 rect) -- queued I5d. (F) link-endpoint auto-create stereotype (`Name<<tag>>` on an arrow's target) wrongly drawn as the LINK's own visible `<<tag>>` label (jar-verified on component/minulo-12-bare186: jar's edge groups carry only the plain label text, no stereotype run at all -- the tag exists ONLY to configure `skinparam Arrow Color<<tag>>`/`<style>arrow{.tag{}}` selectors and the auto-created entity's own stereotype, never a link-visible label) -- ~6 fixtures -- queued I5e. (G) lollipop/interface decor missing ellipse+path pairs on link groups, already ledgered (I-scale ledger) -- excluded, re-verified unchanged at 9 (5+4). (H) sprite/icon multi-path glyphs (`<$bi-globe>` etc, usecase/bootstrap-0) collapsed to fewer `<path>` elements than jar (9 diff instances, ~6 fixtures directly observed; 23 fixtures corpus-wide use `<$name>` sprite syntax, an upper bound if the mechanism proves systemic) -- queued I5f. (I) content-level `<g>` wrapper count mismatch, several distinct +N<g>/-N<g> signatures, not diagnosed (~20 fixtures combined, candidate suspects noted but NOT verified: group-anchor artifacts, interface/lollipop shield wrapping, `-[hidden]-` link handling) -- queued I5g. (J) `<linearGradient>` def count mismatch (4 fixtures, `svg/defs[childCount]`) -- queued I5h. (K) nested-creole text-run splitting (3 fixtures) and (L) already-ledgered creole/word-wrap corpus reach (~8 fixtures) both re-confirmed as I4c mechanism 6 -- excluded, no new mechanism. (M) one-off `#red|green;line.dashed;line:blue` multi-modifier-color-spec-then-bracket-body parser gap (component/balomu-94-kegi822, 1 fixture) -- noted, not queued as its own iteration (single-fixture reach). | diagnosis.md (instrument-before-hypothesize: XPath-resolved tag-multiset classifier across all 355 fixtures before forming any hypothesis; each sub-family's reach/signature jar-verified against 2-6 representative fixtures, raw .puml + cached in.svg read directly, before being placed in the table) | no |
| 2026-07-14 | I5 | MECHANISM + fix landed (sub-family B, port fallback -- `EntityImagePort.drawU`, svek/image/EntityImagePort.java:99-137, jar-verified against component/bijoko-90-riro507's 3 ports before any fix): pre-fix `renderer-entity.ts#drawPortFallback` drew only a filled square via the shared `drawFallbackBox` helper (no text at all) with `theme.colors.border` used for BOTH fill and border (visibly wrong -- jar's ports are `#F1F1F1`-filled/`#181818`-bordered, matching the same defaults every other entity type resolves via `resolveElementPaint`). Upstream draws the port's own display text (`getDesc()`, CENTER-aligned) FIRST, positioned above or below the fixed 12x12 box per `upPosition()` (java:76-80: `node.getMinY() < parent.getRectangleArea().getPointCenter().getY()`), THEN the box at a FIXED 1.5 stroke thickness (`getUStroke()`, java:139-141, independent of the regular-entity 0.5 default and any per-entity line-style override -- upstream's port `drawU` never reads `getEntity().getColors()`'s stroke override at all). Ruled out: computing exact sub-pixel text position beyond the above/below side (upstream's own x-centering formula, `x = -(dimDesc.width - node.width)/2`, IS ported exactly -- but the resulting jar-vs-ours x/y VALUES still differ since this port's graphviz-ts layout assigns different absolute coordinates than the jar's own dot/svek pass; that value-level residue is I6/I7 territory, out of a childCount iteration's scope by construction). | diagnosis.md mechanism fix | no |
| 2026-07-14 | I5 | Fix landed, TDD (red-then-green): `DescriptionNodeGeo.portLabelAbove?: boolean` (layout-helpers.ts, additive) computed once by new `layout.ts#applyPortLabelPositions`, called from `buildGeoNode`'s container branch immediately after `computeContainerBbox(children)` -- the one point in the recursive geo-tree build where a port child's own resolved `y` and its parent's just-computed bbox are both in scope together; mirrors `upPosition()`'s exact center-comparison. `renderer-entity.ts#drawPortFallback` rewritten to build the label via the ALREADY-EXISTING `EntityImageDescriptionSupport.ts#buildTextBlock` (no new text-construction primitive -- reuses the same multi-line/atom-aware TextBlock every other entity type already draws through), center it horizontally, position above/below per `portLabelAbove` (`undefined` -- a port with no resolved parent, unreachable from real `parseDescription()` output -- defaults to the jar's own `false`/below branch), draw it, THEN the box with fill/border through `resolveElementPaint(theme,'port','background'/'border')` (no per-sname 'port' override exists in this port's theme tables, so both fall back to the shared `nodeBackground`/`border` defaults -- jar-verified `#F1F1F1`/`#181818`) plus a new `PORT_STROKE_WIDTH=1.5` constant. One pre-existing test that pinned the OLD wrong fill (`renderer.test.ts` "port renders a small box filled with theme.colors.border") corrected, not just extended, per diagnosis.md precedent (I3/I3b/I4c's own judgment call). 4 new tests: 2 in `renderer.test.ts` (label drawn before box in child order; label position flips per `portLabelAbove`), 2 in `layout.test.ts` (`portLabelAbove` matches the `upPosition()` formula exactly against REAL computed geometry from a 3-port fixture, not a hand-asserted constant; a non-port sibling never gets the field set). Re-measured (`--families`): `svg/g/g[childCount]` 99 fixtures/249 diffs -> 89/214. Full census: conformant HELD at 15/355 (same exact 15-fixture set, verified by direct list comparison -- expected, no port fixture was blocked ONLY by this mechanism), buckets 1-3 held at 22, 4-10 held at 104, 11-30 59->56, 31+ 154->157, errors held at 1 -- the 11-30/31+ shift is a masking-artifact (I3's own documented pattern: a now-correct childCount lets `compareNodes` recurse past the point where it previously short-circuited, unmasking pre-existing out-of-scope x/y-coordinate diffs), NOT a regression, confirmed by the zero-diff SET staying byte-identical. DOT gate re-verified frozen EXACT: component 262/262, usecase 90/90, class 708/708, object 78/80, state 267/267 (every touched file is description-engine-only). Full suite: 309/309 files, 8339/8339 tests (8335 baseline + 4 new). typecheck/lint/build all clean. Ratchet: unchanged (no new zero-diff fixture from this mechanism alone -- every port-bearing fixture also carries an unrelated, out-of-scope geometry gap). Temp classifier (`scripts/_tmp-i5-classify.ts`) and its companion single-fixture dump script deleted before finishing. 7 sub-families (C-J minus the one-off M) queued as I5b-I5h in the README's iteration table with their reach numbers, for the orchestrator to launch next. | diagnosis.md mechanism fix + TDD + re-measure + DOT-gate re-verify | no |
