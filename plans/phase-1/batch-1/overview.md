# Batch 1 — Project Bootstrap

Single task. Must complete before any other batch starts. Creates all config
files, installs dependencies, and verifies the empty project passes all
quality gates.

| ID | Description | Agent | Writes | Depends On | Done |
|----|-------------|-------|--------|-----------|------|
| T1 | Project bootstrap | build-engineer | package.json, tsconfig.json, vite.config.ts, vitest.config.ts, eslint.config.ts, playwright.config.ts, .gitignore, src/index.ts (stub) | — | [ ] |
