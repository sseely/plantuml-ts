# G3 decision journal

| When | Iter | Decision | Why | Flag |
|---|---|---|---|---|
| 2026-07-19 | O0 | Reused `render-fixture-class.ts#renderFixtureClass` verbatim for the object census/ratchet instead of writing a dedicated object render helper | Confirmed (grep + `tests/unit/object/renderer.test.ts`'s own doc comment) that object diagrams have no separate engine upstream — same class engine, same pipeline, zero divergence to accommodate | |
| 2026-07-19 | O0 | Landed the `headerRows()` centering/baseline/textLength fix (single mechanism) rather than also fixing the sibling data-row (object field / map cell / json entry) bug in the same pass | Same missing-width/wrong-baseline PATTERN, but a functionally separate mechanism (3 different call sites, different padding constants, variable per-row heights for map/json) — O0's mandate is "measurement + harness, optionally ONE quick fix"; expanding scope risked turning O0 into a multi-function rewrite with a correspondingly larger regression-verification burden. Named explicitly as the top O1 candidate with the formula pre-derived. | |
| 2026-07-19 | O0 | Did NOT attempt the multi-stacked-stereotype render split (`<<Bar>> <<Foo>>` -> 2 lines) or the `<<tag>> { FontSize }` cascade for object/map/json, even though both were surfaced during triage | Both are genuinely separate, unbuilt features (not extensions of the O0 mechanism) — named in the ledger family table for O1+ rather than folded in | |
