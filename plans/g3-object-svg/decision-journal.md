# G3 decision journal

| When | Iter | Decision | Why | Flag |
|---|---|---|---|---|
| 2026-07-19 | O0 | Reused `render-fixture-class.ts#renderFixtureClass` verbatim for the object census/ratchet instead of writing a dedicated object render helper | Confirmed (grep + `tests/unit/object/renderer.test.ts`'s own doc comment) that object diagrams have no separate engine upstream — same class engine, same pipeline, zero divergence to accommodate | |
| 2026-07-19 | O0 | Landed the `headerRows()` centering/baseline/textLength fix (single mechanism) rather than also fixing the sibling data-row (object field / map cell / json entry) bug in the same pass | Same missing-width/wrong-baseline PATTERN, but a functionally separate mechanism (3 different call sites, different padding constants, variable per-row heights for map/json) — O0's mandate is "measurement + harness, optionally ONE quick fix"; expanding scope risked turning O0 into a multi-function rewrite with a correspondingly larger regression-verification burden. Named explicitly as the top O1 candidate with the formula pre-derived. | |
| 2026-07-19 | O0 | Did NOT attempt the multi-stacked-stereotype render split (`<<Bar>> <<Foo>>` -> 2 lines) or the `<<tag>> { FontSize }` cascade for object/map/json, even though both were surfaced during triage | Both are genuinely separate, unbuilt features (not extensions of the O0 mechanism) — named in the ledger family table for O1+ rather than folded in | |
| 2026-07-19 | O1 | Split `class-object-map-sizing.ts` into that file + a new `class-map-sizing.ts` (map-only sizing) | The pre-O1 file (376 lines) plus O1's data-row fix + map-centering doc comments would have exceeded the 500-line cap in one file; mirrors the existing `class-lollipop.ts`/`class-magma.ts` synthesising-helper-split precedent | |
| 2026-07-19 | O1 | Scoped the `skinparam objectBackgroundColor` fix to the PLAIN (non-`<<tag>>`) form only, leaving the tag-scoped `objectBackgroundColor<<azerty>>` form as a named, deferred mechanism | Traced the tag-scoped form to `SkinParam#getHtmlColor`/`#getColors(ColorParam, Stereotype)` — a genuinely separate, broader upstream mechanism (tries every stereotype tag for EVERY ColorParam-based color engine-wide, not object-specific, not shaped like this port's existing narrow `classBorderThicknessByStereo` precedent) — implementing it would mean threading a new stereotype-qualified-color subsystem not yet traced to its populating call site, disproportionate to a near-zero-harvest priority-3 item with 1 fixture of reach | |
| 2026-07-19 | O1 | Did NOT root-cause `linuxu-41-cogo780`'s leading-`~`-stripped object display name, or triage the remaining 3 `viewBox`/`childCount` near-zero fixtures beyond `fafozi-27-reja300`'s stacked-stereotype attribution | Time-boxed against the mission's priority order (data-row mechanism first, family-table items second, near-zero harvest third) — grepped `VisibilityModifier`/`Display#manageGuillemet`/`CommandCreateEntityObject.java` for the `~`-stripping mechanism without finding it; named in the ledger for O2+ rather than guessed at | |
| 2026-07-19 | O1 | Logged (did not fix) `renderer-classifier-box.ts`'s pre-existing 707-line file-size-cap overage (before O1's own +47 lines) | Predates this iteration entirely (confirmed via `git show HEAD:<path> | wc -l`); a full split is a disproportionate scope expansion for a narrow near-zero-harvest color-cascade fix — logged to `.agent-notes/` per pr-workflow.md's "violations in other files" rule for a dedicated cleanup task | |
