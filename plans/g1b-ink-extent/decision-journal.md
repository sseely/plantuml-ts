# G1b decision journal

| When | Iter | Decision | Why | Flag |
|---|---|---|---|---|
| 2026-07-15 | J1 | X-axis "open sub-question" closed WITHOUT a separate shape-specific formula: implemented the GENERAL `LimitFinder` ink walk (mirroring `SvekResult#calculateDimension`/`TextBlockUtils.getMinMax`) over RAW (pre-shift) assembled geometry, and let it find the true per-axis minimum automatically rather than hand-deriving a closed form per shape in isolation. Verified against `component/zanibo-14-sami874`: `APP` (plain `URectangle`-shaped component) is the fixture's real X-axis binding entity (`LimitFinder#drawRectangle`'s `-1` min-corner inset on BOTH axes, `x-1,y-1`, means a rect leaf's ink-min-x = `box.x-1`; jar's golden `rect x="7"` implies ink-min-x=`7-1=6`, exactly the forced constant) -- NOT the actor (`EMP`'s own ink-left, worked analytically from `ActorStickMan`'s local geometry, `startX=max(armsLenght,legsX)-headDiam/2+thickness=5.5`, body-path leftmost local x=`-armsLenght=-13` from `centerX=13.5` => absolute ink-left `0.5` -- a much wider box-relative offset than a rect's, so it was never binding in this chain topology). The prior iteration's "does not cleanly reduce" finding was checking the WRONG entity's isolated offset instead of the actual global minimum across the assembled tree -- resolved by implementing the walk, not by further hand derivation. | Mission instruction: "close the X-axis open sub-question with jar evidence FIRST... THEN implement" -- satisfied by proving the general mechanism (not a per-shape table) against a jar-cited numeric case before writing the shift-computing code, per the mission's explicit "do NOT hand-code per-shape offset tables" constraint. | no |
| 2026-07-15 | J1 | `computeGlobalShift` (`layout-geo-post.ts`, pure geometry) replaced entirely by `computeInkShift` (`layout-ink-shift.ts`, new module) rather than extended in place -- the ink walk needs the theme-aware draw primitives (`drawClusters`/`drawEntities`/`drawEdges`, `buildUidPlan`, the `LimitFinder`/`StringBounder` machinery), which `layout-geo-post.ts` deliberately has no dependency on. Extracted `collectByKind`/`drawClusters`/`drawEntities`/`drawEdges` out of `renderer.ts` into a NEW shared module `renderer-draw-sequence.ts` (verbatim move, `drawEdges`'s signature narrowed from `geo: DescriptionGeometry` to `edges: readonly DescriptionEdgeGeo[]` -- the only field it read) so BOTH `renderer.ts` and the new `layout-ink-shift.ts` can share the exact same draw sequence without `layout.ts` importing `renderer.ts` (checked: `renderer.ts`'s only dependency on `layout.ts` is a TYPE-only import, erased under `verbatimModuleSyntax` -- but importing VALUES from `renderer.ts` into `layout.ts`-adjacent code would still create a confusing "layout depends on renderer" direction even if not a runtime cycle, so the shared pieces were extracted to a THIRD, renderer-and-layout-agnostic module instead, typed only against `layout-helpers.ts`). Similarly extracted `runInkWalk` (the raw `LimitFinder`-walk primitive) and moved `driverBounderFor` out of `computeDocumentDims`/`renderer.ts` into `renderer-ink-extent.ts`, both now exported and reused by `computeInkShift` -- pure extraction, `computeDocumentDims`'s own behavior is unchanged (still calls `runInkWalk` then `.getDimension().delta(15,15)` exactly as before). | Keeps `layout-geo-post.ts` (pure geometry) and `layout.ts` free of draw/theme dependencies while still sharing the EXACT draw sequence the real render pass uses (upstream's own `SvekResult#drawU` is the single shared call site for both the ink-walk pass and the real pass -- this port now mirrors that with 2 call sites into the SAME shared functions instead of 2 divergent implementations). | no |
| 2026-07-15 | J1 | `buildGeoAndEdges` (`layout.ts`) now builds edges TWICE: once with a zero `(dx=0,dy=0)` mapping (the RAW, fully spline-clipped/labeled edge geo `computeInkShift`'s ink walk needs -- edges are part of the assembled draw tree, same as jar's `SvekResult#drawU` third loop), then again with the real `dx`/`dy` `computeInkShift` returns. This is NOT a double-clip: `clipEdgePoints` (`layout-geo-post.ts`) clips against RAW (unshifted) container bboxes either way, and a uniform translate commutes with that clip -- translating an already-clipped spline by `(dx,dy)` gives byte-identical results to clipping a pre-shifted spline by a pre-shifted bbox. Verified empirically: no `path/@d`/`polygon/@points`-family diff attributable to double-clipping appeared anywhere in the full-corpus before/after scan (see ledger.md J1). | Avoids writing a NEW "translate an already-built `DescriptionEdgeGeo`" helper (would duplicate `buildEdgeGeos`'s own point/label translate logic) -- reusing the existing pure function twice is simpler and provably equivalent, at the cost of one extra (cheap, non-hot-path) pass. | no |
| 2026-07-15 | J1 | 5 fixtures in the full-corpus before/after magnitude scan regressed (diff count grew): `component/duvoru-86-lubo341` (79->98), `fopako-15-labi027` (101->102), `gabogi-09-zoda184` (12->15), `mekimu-46-luzu886` (141->146), `xirika-05-beju263` (56->89). All 5 diagnosed: every one uses `portin`/lollipop-interface (`()`) entities with directional port-style arrows (`-up-(`, `<-l-`, etc.) -- squarely mechanism B's (`FrontierCalculator`/port-only-container) territory, explicitly J2's target, NOT J1's. None of the 5 was in the protected zero-diff/ratchet set (all were already far from conformant, 12-141 diffs, dominated by mechanism B's broken port/interface positions before this iteration too). Mechanism C's now-accurate ink walk reads the (still mechanism-B-broken) port entity positions and computes a numerically DIFFERENT margin than the old flat one did -- not a new defect, a downstream interaction with an already-known, not-yet-fixed mechanism. Not reverted, not further investigated (out of J1 scope; J2 will re-measure these 5 once mechanism B lands). | Mission instruction: "diagnose every regression... childCount-bail unmasking has precedent" -- satisfied; none crossed the ratchet/zero-diff tripwire, so no stop condition triggered. | no |
| 2026-07-15 | J2 | Ported `Cluster.java#manageEntryExitPoint`/`svek/FrontierCalculator.java` into a new pure module `frontier-calculator.ts` (RectangleArea/Point + `manageEntryExitPoint`/`ensureMinWidth`, y-DOWN throughout — verified frame-agnostic: the same jar numbers reduce to 177x99 in both graphviz-native y-up and jar's final SVG y-down frame). The Java constructor's `initial` (`Cluster.getRectangleArea()` BEFORE `manageEntryExitPoint` overwrites it — the raw graphviz-assigned cluster subgraph rect) has NO public equivalent in graphviz-ts's `LayoutSnapshot`/`getLayout()` API (nodes/edges/overall-bounds only, no per-cluster geometry) — reaching into graphviz-ts's private `Graph`/`GraphInfo` model was ruled out (`graphviz-ts OUT OF SCOPE, pinned .tgz` — its own `exports` map only surfaces `.`/`./api`/`./render`, deliberately not the internal model, ADR-1 in its own source). Found a fully public path instead: `render(graph, 'xdot', opts)` (`graphviz-ts/render`) re-serializes the laid-out graph as xdot text INCLUDING every subgraph's own `bb=` attribute — verified byte-identical to real `dot -Txdot` on the jar's own cached `svek-1.dot` for `component/gafegu-06-nito976` (cluster bb `(8,8)-(177,121)`, ports at native y=107/x=22,69,116,163 — both graphviz-ts and real `dot` agree to the last digit). New module `frontier-shadow-layout.ts` builds a small ISOLATED shadow graph (mirroring `svek-dot-emit.ts#portClusterBlock`'s exact structure: ports ranked at the cluster's own level with an ordering chain + bare chain-to-anchor edge, anchor nested one level down in `${id}ee`) and text-parses `bb=`/`pos=` back out of the xdot render. Deliberately NOT wired into the real `layoutGraph()`/`addClusters` seam (every diagram type funnels through that) — would have meant widening the REAL port-cluster's anchor sizing/nesting/edges for class/state/usecase/dot/json too, a far larger blast radius than this mission's mechanism-B scope; the real pipeline's port x-positions are ALREADY faithful (same nodesep/width formula, verified identical relative spacing to the shadow calc and to jar), so a single reference-port (dx,dy) translation (`frontier-cluster-bbox.ts#computeAlignedInitial`) aligns the shadow calc's `initial` into the real pipeline's own coordinate frame without needing the two graphs' layouts to share an origin. | The "thread the graphviz cluster rectangle" instruction assumed graph-layout.ts already had or could easily expose this; it doesn't, and widening the shared cross-diagram-type seam was assessed as disproportionate risk for a single-mechanism iteration — the isolated-shadow-graph design achieves the same numeric result (jar-verified exact: 177x99) with zero blast radius outside `src/diagrams/description/`. | no |
| 2026-07-15 | J2 | `computePortClusterBbox` (`frontier-cluster-bbox.ts`) is scoped to `insides.length === 0` (a PURE port-only container) — falls back to the prior `computeContainerBbox` padded-union formula whenever the cluster ALSO has normal ("insides") children. Root cause: Java's `core == null` branch (`insides` empty) is the ONE case this iteration numerically closed; when `insides` is non-empty, `core` seeds from the REAL merged `insides` rects directly (precise, no approximation needed) and `initial` matters only for its "untouched-axis snap-back" role — but THIS module's `initial` for that case would need a representative "insides" placeholder fed through the shadow calc, since the shadow calc has no visibility into the real normal-children layout. Tried exactly that (a single combined-bbox placeholder node in the shadow graph's `${id}ee`) and it REGRESSED two real fixtures in the full-corpus scan: `component/cuxelu-66-zopu195` (26->27 diffs) and `component/dugovi-24-kupu658` (31->36 diffs), both `component X { [normal] portout p }` shapes — the placeholder is not a faithful stand-in for graphviz's real per-node rank-separation contribution. Reverted to the padded-union fallback for `insides.length > 0` rather than ship an imprecise mechanism-B result; full-corpus rescan after reverting confirmed 0 regressions (was 2). | "Fix at origin, never post-hoc coordinate surgery" — an approximated placeholder is exactly the coordinate-surgery pattern the mission's standing rules forbid; better to leave the mixed-children case at its prior (already-tested) fidelity and ledger it as a named remainder than ship a regression. | no |
| 2026-07-15 | J2 | `frontier-shadow-layout.ts` maps every caller-supplied port/anchor id to a synthetic, always-bare-DOT-identifier internal id (`n0`, `n1`, ...) for BOTH graph construction and its own xdot text-parsing, translating back to the caller's id only in the returned map. Root cause of the bug this fixes: `component/bujige-52-gase998` (`set separator .` -> qualified ids like `srv1.br0`) threw `no node statement for srv1 br0` — the module's FIRST implementation used the caller's raw id directly in both the graphviz-ts builder call AND the regex that re-parses that id's `pos=` out of the xdot text; graphviz's DOT serializer quotes an id containing `.` (`"srv1.br0" [...]`) since it isn't a valid bare identifier, so the bare-text regex silently failed to match. Caught by the full census run (errors: 1->2, not by any of the direct/targeted fixtures) — diagnosed via a debug harness that logs the slug on every census catch (census's own `catch {}` swallows the error object). | Textbook argument for never keying parse-back-out logic to caller-controlled strings when the module fully controls the graph it built — sidesteps DOT's whole id-quoting grammar rather than trying to replicate it in a regex. | no |
| 2026-07-15 | J3 | `TITLE_LABEL_HEIGHT=16`/`TITLE_LABEL_H_PADDING=20` (`layout-helpers.ts`) were both wrong per jar's real formula — found by reading the literal `label=<TABLE WIDTH=".." HEIGHT="..">` jar emits in its OWN cached `svek-1.dot` (an available oracle no prior iteration consulted for this constant), not by re-deriving `ClusterHeader.getTitleAndAttributeHeight()` in isolation. Real formula (verified against 4 independent fixtures' literal dot caches): `titleAndAttributeWidth/Height = measure(display).width/height + USymbol#suppWidth/HeightBecauseOfShape(symbol)` (only `node`: +60w/+5h, `database`: +15h, else 0/0 — `USymbolNode.java:192-198`/`USymbolDatabase.java:173-175`, base `USymbol` returns 0), table height reduced by `SvekEdge.appendTable`'s constant `-5`. `component` "comp" -> 34x9 (component/gafegu-06-nito976, gocexi-61-biso565, rapaji-98-xato067); `node` "srv1"/"srv2" -> 86x14 (component/bujige-52-gase998, both clusters) — all 4 exact. Wired the corrected formula (`measureTitleLabel`, `title-label-sizing.ts`) into `buildAnchorNode` (real DOT-emission anchor — `scripts/dot-sync-report.ts --slug` now shows byte-identical `WIDTH="34" HEIGHT="9"` against jar's own cached dot, was `WIDTH="54.2125" HEIGHT="16"`) and `ensureMinWidth`'s `titleWidth`/`titleHeight` floor (`frontier-cluster-bbox.ts`, direct 1:1 `Cluster.java:427-428` application, never regresses since the floor never engaged in the 3 fixtures checked — core width 177 >> titleWidth+10 either way). | Mission instruction: "find the jar's real derivation... fix at origin if it's a genuine constant/formula gap." The literal dot-cache oracle is authoritative and had simply never been read for this specific constant before — J2's own "17 ~= getTitleAndAttributeHeight()-5" citation (ledger.md J2) does not actually reduce to 17 from the real formula (14-5=9, not 17); J2 empirically curve-fit 17 to make `computePortClusterBbox` hit jar's exact 99 without reconciling it against jar's own literal DOT text. | no |
| 2026-07-15 | J3 | Did NOT wire the jar-exact `measureTitleLabel` height into `frontier-shadow-layout.ts`'s isolated shadow-graph anchor node (`PortClusterInfo.anchorWidth`/`anchorHeight`) — instrumented directly (`computePortClusterInitialRect`, temp probe script, deleted) and found feeding it the jar-exact anchor (34x9) REGRESSES `computePortClusterBbox`'s result: `initial.maxY` drops from 120 to 113 (a clean 7px = 16-9 pass-through, confirmed varying ONLY anchorHeight, anchorWidth has zero effect on `initial`'s height), while the REAL `dot -Txdot` cross-check jar's own full dot text produces (`decision-journal.md`'s J2 3rd row, `(8,8)-(177,121)`) needs maxY=121 — an 8px gap between our isolated shadow graph's reconstruction and jar's real full-graph layout that exists REGARDLESS of anchor size (verified: old anchor 16 gives maxY=120, 1px short of 121; anchor 17 gives maxY=121, exact — J2's "17" empirically absorbed this real 8px structural gap, it was never jar's real anchor size). Kept `anchorWidth`/`anchorHeight` on the OLD (16/54.2125) legacy values (`measureShadowAnchorDims`, `title-label-sizing.ts`, explicitly documented as NOT jar-faithful) so `component/gafegu-06-nito976`/`gocexi-61-biso565`/`rapaji-98-xato067` stay at their J2 baseline (98, 1px off jar's 99) rather than regressing to 91 (8px off). The shadow graph's own structural gap (missing `protection0`/`protection1`/`thereALinkFromOrToGroup` wrapping subgraphs vs jar's full `ClusterDotString` output, most likely) is a NEW, real, unresolved mechanism — named in ledger.md J3's accounting table (`G1b J3 NEW: frontier-shadow-layout 8px structural gap`), not fixed this iteration. | Diagnosis.md: "no fix before a stated mechanism" — I have the SYMPTOM (8px gap, isolated to the shadow graph specifically) but not yet the CAUSE (which structural element jar's real dot has that the isolated shadow graph omits); shipping the jar-exact anchor anyway would have been the exact "fix at origin" violation the mission forbids (post-hoc coordinate surgery via a value that's provably wrong for ITS purpose, chosen only because it happens to cancel a DIFFERENT unresolved bug). | no |
| 2026-07-15 | J3 | Regenerated `tests/oracle/svg-conformance/parity.json` via its normal path (`npm run svg:survey`) — stale since 2026-07-10, three prior iterations (I3, I9, J1) each found the SAME 6 fixtures' `dotEqual` stuck at `false` despite `test-results/dot-sync-equal/{component,usecase}.txt` (the live DOT-sync source of truth) already listing them, and each declined regeneration as "out-of-write-set for this iteration." J3 is the mission's explicit closing/accounting iteration and the mission brief explicitly authorized attempting the regen here ("if... a one-line refresh... do it"). Ran clean (355 fixtures, ~3 min, no crashes); all 6 target fixtures (`component/mamase-39-buto560`, `usecase/{norebe-58-bixu182,sidame-35-cozu078,zoriso-46-vata931,komivo-22-toki497,rabida-94-kula497}`) now show `dotEqual:true`, added to the ratchet (35->41). One side effect: the regeneration ALSO flipped `component/bujige-52-gase998` (this session's own J3 anchor-sizing fix made ITS DOT emission byte-exact too, confirmed via `dot-sync-equal/component.txt`) from `dotEqual:false` to `true`, breaking `description.golden.ratchet.test.ts`'s negative eligibility test (which hardcoded that slug as a "known non-dotEqual" example) — swapped to `component/gutute-00-gaki684` (confirmed still `dotEqual:false`, a genuine DOT divergence, not `!pragma layout elk`-blind). | Mission instruction explicitly framed this as a "try it, ledger the blocker if not" decision for J3 specifically (unlike prior iterations where it was declined outright); the negative-test fixture swap is the minimal, directly-caused fix for a test that broke as a PROVEN-CORRECT consequence of shipping the anchor fix, not a pre-existing violation being opportunistically touched. | no |
