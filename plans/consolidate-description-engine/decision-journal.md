# Decision Journal

Appended during execution. One row per non-trivial judgment call.

| Date | Batch/Task | Decision | Rationale |
|------|-----------|----------|-----------|
| 2026-06-26 | infra | Switch package manager pnpm → npm (per user). Regenerated lockfile, updated brief gate commands. | User directive mid-Batch-1; gates re-verified green under npm. |
| 2026-06-26 | B1/T1 | Include upstream `portin`/`portout` in `ALL_TYPES`, both mapping to `USymbol 'port'`. | D2 wants the *complete* ALL_TYPES; the locked USymbol contract stops at `port`. Folding the two keywords onto `port` keeps classification faithful without widening the contract type. |
| 2026-06-26 | B1/T1 | Handled T1 directly instead of dispatching typescript-pro. | Single small fully-specified foundational module; correctness of the upstream ALL_TYPES mapping is load-bearing for all downstream batches, so verified it first-hand. |
| 2026-06-26 | B2/T2 | Test drives the singleton registry via side-effect import of `src/index.js` + `extractBlocks`, not a hand-built registry. | Faithful to the "real registry" requirement and immune to registration-order drift. cocice resolves to `component` post-guard (correct pre-merge). All 4 gates green; Phase 1 complete. |
| 2026-06-26 | B4/T4 | Allowed an extra file `src/diagrams/description/parse-helpers.ts` beyond the declared write-set (parser.ts + test). | typescript-pro split stateless helpers out to keep parser.ts under the complexity linter. New module is in the engine's own dir, in no other task's write-set → no conflict. Batch-8 cutover must account for it. All 4 gates green; spot-check of every acceptance criterion passed (33/33 ALL_TYPES keywords, shorthands, business variants, container nesting, include link). |
| 2026-06-26 | B5/T5 | Extra file `src/diagrams/description/layout-helpers.ts` (same rationale as parse-helpers). Batch-8 cutover must account for both helper files. | Grid/sizing sub-functions extracted to satisfy the complexity linter; engine's own dir, no conflict. |
| 2026-06-26 | B5/T5 | **Edge routing: center-to-center 2-point lines for all edges**, dropping component's graphviz spline routing. Maintainer-approved STOP/flag (not a silent choice). | usecase already used center-to-center; the two-level layout makes seam splines hard to compose. No gate or acceptance criterion pins routing (component test only asserts ≥2 points; DOT-gate checks counts). Recorded as a PROVISIONAL divergence in DIVERGENCES.md, to be re-evaluated at the visual-QA pass; spline restoration is a possible follow-up task. All 4 acceptance criteria + 4 gates green. |
| 2026-06-26 | B6/T6 | Extra file `renderer-helpers.ts` (node shape renderers in a `Map<USymbol,NodeRenderer>`). Batch-8 cutover must account for parse-helpers, layout-helpers, AND renderer-helpers. | Complexity linter; engine's own dir, no conflict. Edges render as cubic beziers from spline control-point triples (dot/renderer.ts precedent), not polylines — faithful to upstream DotPath. All 4 gates green; acceptance verified (ellipse/cylinder/hexagon-fallback/include/bezier). |
| 2026-06-26 | B6/T6 | Pre-existing gap surfaced, NOT fixed in this mission: parser does not auto-create link-only endpoints (`(A) ..> (B)` yields 0 nodes). | OLD usecase+component parsers behave identically — faithful carry, not a regression. Logged to `.agent-notes/description-autocreate-link-endpoints.md` as a future enhancement (out of consolidation scope). Diagrams with explicit declarations render fully incl. dashed «include». |
| 2026-06-26 | B5/T5 | **Confirmed the rebuild mirrors upstream Java** (user asked). | `Cluster.java:333` emits `cluster_*` subgraphs; `DotStringFactory` builds one DOT (single pass); `SvekEdge.solveLine`→`DotPath` reads splines; container-endpoint edges use `ltail`/`lhead` + `simulateCompound` to clip the path to the cluster rectangle, with graphviz `compound=true` deliberately disabled (`DotStringFactory:157`). Our anchor-leaf + clipSplineStart/End reproduces simulateCompound. Verified empirically: P→Q container edge clips 124→172 (P right border → Q left border), routed edges >2 spline pts. Provisional DIVERGENCES entry resolved/removed. All 4 gates green, 3281 tests. |
| 2026-06-26 | B5/T5 | **SUPERSEDED the center-to-center decision: rebuild T5 as a single-pass, cluster-aware layout that uses real graphviz splines.** Maintainer confirmed upstream Java routes descriptive edges as dot bezier splines (`SvekEdge.solveLine`→`DotPath`), not center-to-center; chose "Restore spline routing" and authorized free rebuild. | Validated empirically: graphviz-ts implements cluster subgraph layout — members stay contained and cross-cluster edges get routed splines (probe: `a->c` returned a 7-point spline across two clusters). Architecture: flatten all nodes + declare containers as `cluster_*` subgraphs, ONE `layoutGraph` call, use returned node coords + edge spline points directly (no two-level composition). Requires teaching the seam (`graph-layout.ts`) to forward `clusters` to graphviz-ts — verified additive/zero-blast-radius: NO current `layoutGraph` caller passes `clusters` in its input (dot/layout computes cluster boxes itself post-layout; the field was emitter-only). Seam edit is authorized scope expansion. |
