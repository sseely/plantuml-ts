# Decision Journal — object-dot-sync

| Date | Decision / event | Detail | Why | Status |
|------|------------------|--------|-----|--------|
| 2026-07-11 | T1 done (ee6c640) | OBJECT leaf = existing `Classifier` with `kind:'object'`; members reuse `Member` as `{visibility:'+', name, type: rawValue}`. Command lives in new `class-object-commands.ts` (500-line cap on class-commands.ts), spliced as rule 7a right after classifier declarations — mirrors CommandCreateClass→CommandCreateEntityObject order. Duplicate `object foo` = silent no-op (parser has no error channel; existing classifier untouched, unlike class redeclaration which re-applies fields). REL_DISPATCH_RE (rule 6) precedes it, so `Object <|-- Foo` stays a relationship. Gates green (5654 tests). | T1 interface contract must be journaled | done |
| 2026-07-11 | CommandAddData is unregistered upstream | `grep -rn 'new CommandAddData'` over upstream: zero hits — the file exists but NO factory registers it. T2's spec attributes `X : field = value` to CommandAddData; the oracle's actual path in CLASS-typed diagrams is `CommandAddMethod` (registered early in initCommandsList), which CREATES missing targets as CLASS leaves on the fly (CommandAddData errors instead) and requires spaces around `:` (spaceOneOrMore both sides vs AddData's spaceZeroOrMore). T2 ports CommandAddMethod semantics; commit message keeps the spec's title with a clarifying body. | Java is the spec; oracle jar behavior is what parity measures | push-forward (logged) |
| 2026-07-11 | Object members get their own field parser | `parseMemberLine` (class-member-parser.ts) cannot parse `name = value` (attr regex has no `=` form) — pre-existing, also drops Java-style `Type name` (see .agent-notes/iter10). Fixing it GLOBALLY would change member counts → node heights pinned by the 687 class goldens (stop-condition-4 territory). T2 routes object-leaf member lines through an object-specific parser (plugin parseField semantics) and leaves parseMemberLine untouched. | Class-ratchet protection | push-forward (logged) |
| 2026-07-11 | T2 done (b5e0d3b) | Multiline `object X {` reuses `pendingBodyId` body machinery; per-line parse branches on `kind==='object'` → new `parseObjectField` (`name = value` / bare `name`), replicated from the plugin, parseMemberLine untouched. Rule 6-pre routes object targets the same way; missing target still creates CLASS leaf (CommandAddMethod semantics). Verified: `dot-sync-report class` still 680/680 EQUAL. Gates green (5661 tests). | — | done |
| 2026-07-11 | KNOWN GAP: unparseable body lines dropped | Upstream Bodier keeps ANY raw body line as display text (`Member.field` never rejects); our `Member` shape has no raw-display slot, so lines matching neither `name = value` nor bare `name` are dropped (also true for separator rows `--`). Affects node HEIGHT (sizes are asserted). If Phase L hits height mismatches on fixtures with such lines, the fix is a raw-display Member variant — flagged for Phase L, not fixed here. | No AST invention mid-port; corpus will adjudicate | open — Phase L |
| 2026-07-11 | T3 done (ced2d12) | MAP AST: `MapRow {key, value, linkedCode?}`, `Classifier.rows?`, kind 'map'. Commands in new `class-map-commands.ts`; body via pendingBodyId machinery. Row link `k *-> dest` = `type:'association', sourceDecor:'none', targetDecor:'open', fromPort:k, length:arrowLen-2` — LinkType(ARROW,NONE) params are TARGET-then-SOURCE decor (CommandLinkClass#getLinkType swaps: `new LinkType(decors2, decors1)`). Duplicate map: pendingBodyId='' sentinel consumes body silently. Divergences (no error channel): missing link dest → row kept/link skipped; non-`=>`/`*->` body line → dropped. NEW READ-ONLY helper `resolveExistingId` — engine's `resolveReference` MUTATES ast.namespaces even on miss (phantom namespace → fake leaf via collapseEmptyNamespacesFinal); flagged for future existence checks. Gates green (5670 tests, class 680/680). | — | done |
| 2026-07-11 | Batch 1 closed | Write-set audit clean (all under src/diagrams/class/** + tests/unit/class/**); one commit per task (ee6c640, b5e0d3b, ced2d12); four gates green at batch boundary. | Protocol | done |
| 2026-07-11 | T4 done (ee31a5a) | New `class-object-map-sizing.ts` ports EntityImageObject/EntityImageMap/TextBlockMap; hooks in at `measureClassifier` (single site class-dot-graph reads dims from). 0px delta vs oracle on 4 fixtures (beleso, figeze, majake, bepafe). KIND_SHAPE map→plaintext; isPort explicitly excluded for map (row-link fromPort would have flipped it to portTable). Removed plugin-era 'O' circled badge (upstream object header has none) — divergence-fix, not ledgered (aligns TO upstream). Object EQUAL 34→41. Class 680/680 unchanged. Gates green (5678 tests). Notable: object/map fields use MethodsOrFieldsArea (6,4) margin, not class ICON_WIDTH=18. Stereo-width formula unverified by corpus (no fixture where it dominates) — noted in module doc. | — | done |
| 2026-07-11 | T0 baseline captured | `dot-sync-report object` (no --type-tag, EXPECTED_TAG.object='CLASS'): 80 comparable, 34 EQUAL (43%), 26 no-candidate, 1 oracle-blind (pragma layout). Buckets: degree 18, minlen 18, edgeCount 17 (all under), shape 9, label 7, nodeCount 4 (under). Matches brief baseline exactly. Gates: 5647 tests pass, typecheck, lint, build green. | Brief T0 step 3 | done |
| 2026-07-11 | T5 done | Deleted `src/diagrams/object/{index,parser}.ts` + `objectPlugin` registration (decisions.md#d2: upstream has no object engine, ClassDiagramFactory owns the commands — T4 already ported full parity). Dispatch: pure-object block now resolves to classPlugin (type 'class'); dispatcher.ts's AMBIGUOUS_TYPES typed-lookup required no edit — `find(p => p.type === 'object')` now returns undefined (no plugin claims that type) and falls through to accepts() scanning unchanged. Migrated tests/unit/object/{parser,layout,renderer}.test.ts to parseClass/layoutClass/renderClass, kept in tests/unit/object/ (directory name is semantic — still object-diagram BEHAVIOR coverage) plus a new registry-dispatch test proving the routing change. Two plugin-era assertions replaced with documented-divergence tests (verified against NameAndCodeParser.java and CommandCreateEntityObjectMultilines.java): inline single-line `object Foo { x=1 }` body and unquoted `object Foo as bar` alias were never valid upstream syntax and were not carried into the T4 port; both now assert zero classifiers created (silent no-op, no error channel). Baselines unchanged: component 235 EQUAL/1 no-candidate/4 oracle-blind, usecase 65/14/3, class 680/680. Object EQUAL 41→63/80 (T4's 41 was measured via the old plugin path; T5 exposes the same class-engine logic through real routing — no code-level object rendering changed in this task). Gates green (5676 tests, coverage 97.77/93.91/98.05). | decisions.md#d2 | done |
| 2026-07-11 | Batch 2 closed | Write-set audit clean (class/**, object/** deletion, src/index.ts registry line, tests). One commit per task (ee31a5a, 13fdbd2). Four gates green at boundary (5676 tests). Object EQUAL entering Phase L: 63/80. | Protocol | done |
| 2026-07-11 | Phase L iter 1 — mechanism (BEFORE fix) | Bucket: edges-under family (beleso-08, fikojo-87, sarepa-89 — `bamboo <-- <unicode-id>`). MECHANISM: `CLASS_ID` (class-relationship-parser.ts:39) uses ASCII `\w` for identifier atoms; upstream `CommandLinkClass.getClassIdentifier()` is `[%pLN_$]+` = Unicode `[\p{L}\p{N}_$]` (Pattern2.java:56). Non-ASCII endpoints fail REL_DISPATCH_RE/REL_RE → the relationship line is dropped → edge/degree/minlen under. RULED OUT: sizing (maxSizeDelta 0), declaration parsing (both classifiers present with correct unicode ids). Repo precedent exists: description link-grammar.ts:70 and class-declaration-parser.ts:281 already use `\p{L}\p{N}` with u-flag. Fix at origin: CLASS_ID charset + u-flag on derived regexes. Full non-EQUAL census (17): unicode-endpoints 3, map-port `=>` links 2 (lafemo, sivapa), allow_mixing keyword-names 3 (gapisu, ruturo, togixe), json-adjacent 3 (baloca, bepafe, maxosa), style-block 1 (lisepi), map oddities 2 (gatefi `map map0` no-brace + empty, satuco empty-key rows), preprocessor 2 (zicope, zuvila), fafozi 1 (double stereotype, TBD). | Loop protocol step 3 | in progress |
| 2026-07-11 | Phase L iter 1 — fixed + ratchet seeded | Fix: `ID_ATOM = [\p{L}\p{N}_$]+` in CLASS_ID (u flag on REL_RE/REL_DISPATCH_RE/LOLLIPOP_RE). Object EQUAL 63→66 (beleso, fikojo, sarepa). RATCHET CREATED: tests/oracle/object-dot-parity.test.ts + 66 goldens in oracle/goldens/object/. D4 size assertion exposed a SIZE BACKLOG: 25/66 EQUAL fixtures have maxSizeDeltaIn 0.06–4.95in — pinned per-fixture in oracle/goldens/object/size-backlog.json (delta may only shrink; absent slug = 0 required; entries deleted as size mechanisms are fixed). Repeated 0.6625 delta across 6 fixtures suggests one shared mechanism. Gates green (5748 tests). | Loop protocol steps 4-7; D4 | done |
