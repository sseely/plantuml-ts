# Decision Journal

Appended during execution. One row per non-trivial judgment call; one row per
loop iteration with the measured EQUAL delta.

| Date | Phase/Iter | Decision / Mechanism | Rationale / Delta |
|------|-----------|----------------------|-------------------|
| 2026-07-04 | B0/T0 | Gates on main verified by tree-hash equality (main^{tree} == branch^{tree}, 05ee26d8) instead of a re-run. | The merged tree is byte-identical to the tree that passed all four gates minutes earlier; a re-run adds runtime, not signal. |
| 2026-07-04 | B0/T0 | Pinned the maintainer-built graphviz-ts-0.1.0.tgz (2026-06-25) rather than repacking live source. | Live source is mid-refactor per maintainer; a maintainer-built tarball is the deliberate snapshot D2 wants. All gates green against it (bundle 905→889kB, expected dist delta). |
| 2026-07-04 | B0/T0 | SKIPPED step 6 (settings.autonomous.json widening: java/git-merge allow rules) — denied by the permission classifier as self-modification. | Surfaced to maintainer instead of working around. Impact: oracle-jar dumps (`java`) will permission-prompt in later iterations until the maintainer adds the rule; `git merge` not needed again until mission end. |
| 2026-07-04 | B0/T0 | Typecheck fix in scripts/dot-sync-report.ts (fail-counter indexing) folded into its first commit. | Script predated gating; baseline must be green before the ratchet means anything. |
| 2026-07-04 | B1 | Corrected batch plan: T3 depends on T1 (pins under the NEW bar); ran T1∥T2 then T3. Dependency column fixed in overview. | Pinning under the old bar would have baked in false-EQUAL goldens. |
| 2026-07-04 | B1/T1 | Maintainer added Bash(java) to settings.autonomous.json (the classifier had blocked me self-adding it in T0). | Oracle-jar runs no longer prompt. |
| 2026-07-04 | B1/T3 | **Baseline under the tightened bar: 0 EQUAL of 312 cached fixtures** (was 18+1 under the old bar). Ratchet ships with an empty pin set; zero-goldens path verified. | Every fixture fails ≥1 of rankdir/nodesep/ranksep — confirms graph attrs as Phase 2 category #1. |
| 2026-07-04 | B1/T2 | Probe: json and dot produce NO svek-*.dot (0/5 each). Phase 5 is a maintainer-decision phase as scoped. | Evidence in phase-5-json-dot/probe.md. |
| 2026-07-04 | P2/i1 | **Mechanism (graph attrs).** (1) rankdir: `description/layout.ts:429` hardcodes `LR`; parser DROPS `left/top…direction` (parser.ts:135 ignore rule). Upstream emits `rankdir=LR` iff skinparam Rankdir==LEFT_TO_RIGHT (`DotStringFactory.java:170-173`), set only by CommandRankDir; TB default emits nothing. (2) nodesep/ranksep: ours hardcodes 60/80px. Upstream `createDotString`: nodesep=max(maxHorizDzeta/10, 35), ranksep=max(maxVertDzeta/10, 60), each replaced outright by skinparam nodesep/ranksep when set. Dzeta (`SvekEdge.java:1159-1204`): self-loop→decor margins; length==1 edges sum label/tail/head WIDTHS+decor margins (horizontal); length>1 sum HEIGHTS+margins (vertical). Decor margins (`LinkDecor.java`): NONE=2, ARROW=10, ARROW_TRIANGLE=10. Ruled out: seam+emitter pass attrs through faithfully (babafi drill-down verified). Blockers found: AST link lacks `length` (needed for isHorizontal; also unblocks i2 minlen=length−1); edge labels never measured in layout; Theme lacks nodesep/ranksep (skinparam override deferred, journaled). | Fix: add length+rankdir to AST/parser, compute attrs per Java in layout with measured labels; floors dominate un-labeled fixtures. |
| 2026-07-04 | P2/i1 done | **Graph attrs fixed** (5439db3): rankdir TB-default + `left to right direction` parsed; nodesep/ranksep = dzeta/10 with 35/60 floors; link `length` added to AST (unblocks i2 minlen). Delta: attr fails 229-233→0-3; **EQUAL 0→18 component, 0→1 usecase** under the strict bar. Residuals root-caused and ledgered (kermor pragma; zilisi alias-syntax dispatcher misroute; rich-text label measurement). Ratchet pinned 19 slugs (3120 tests green). 2 pre-existing tests recomputed for TB. | Next (i2): minlen = length−1 (163+38 fixtures) — cheapest now that length is in the AST. |
| 2026-07-04 | P2/i2 | **Mechanism (minlen).** Upstream: `SvekEdge.java:417-427` with `SkinParam.useRankSame()` hardwired false (SkinParam.java:1146) → EVERY svek edge emits `minlen = link.getLength()-1` (`->`→0 same-rank, `-->`→1). Ours: description `buildDotEdges` never set `attributes.minLen`; emitter defaults `minlen=1` (svek-dot-emit.ts:103) → every length-1 link compared 1 vs 0. Ruled out: emitter/comparator (both faithful); useRankSame branch (dead upstream). Fix at origin: layout.ts buildDotEdges sets `minLen: link.length-1` (uses i1's AST length). Also moved endpoint-resolution helpers to layout-helpers.ts (file-size hook). | Delta: pure-minlen failures cleared (163→160 comp, 38→34 uc); the residual minlenOk failures coincide 1:1 with edgeCountOk — shadow-failures of missing edges, not minlen logic. EQUAL steady at 18+1. Category closed; next: edges-under (categories 3+4, 160 fixtures). |
| 2026-07-04 | P2/i3 | **Mechanism (container-open keyword set).** `component "display" as b {` mis-parsed: CONTAINER_SYMBOLS had only 8 keywords, so the container-open rule never fired for component/artifact/card/queue/stack/hexagon/file/action/process; the generic keyword rule swallowed the whole line (id included the `{`), and edges to the alias resolved to nothing → dropped. Upstream spec: the 17-keyword SYMBOL alternation in `descdiagram/command/CommandPackageWithUSymbol.java`. Ruled out: link parsing (babafi's link parsed correctly — runtime AST dump), parseNameSection alias forms (correct once the rule fires). Fix: CONTAINER_SYMBOLS mirrors the 17. | Delta: babafi-class fixtures now parse structurally right (babafi remains non-EQUAL only on labelOk — edge-label category). Aggregate: edgeCount 160→159, nodeOver 34→20. EQUAL steady 18+1. Remaining edges-under (158) = arrow grammar (direction hints, inline styles, reversed/long arrows) + auto-create endpoints → next iteration is the CommandLinkElement port. |
| 2026-07-04 | P2/i4 | **Mechanism (arrow grammar / edges-under, 158 fixtures).** Our link rule accepts 7 fixed arrow tokens; upstream `CommandLinkElement.java` accepts `HEAD1 [-=.~]+ [style] [direction] [inside] [style] [-=.~]* HEAD2` with quoted qualifier labels and 7 endpoint notations. Key semantics verified: queue=BODY1+BODY2; `.`→dashed/`~`→dotted/`=`→bold; explicit direction token (or `<`-head) wins, else length-1 queue→RIGHT else DOWN; **LEFT/RIGHT force queue="-" (length 1 → minlen 0)**; **LEFT/UP invert the link** (getInv); auto-create endpoints per notation (getDummy: ()→interface, (x)→usecase, :x:→actor, [x]→component, bare→unknown leaf; trailing / = business); FIRST/SECOND quoted labels → tail/head qualifiers; hidden links still emit (style=invis). Ruled out: layout/emitter (drop happens at parse: unmatched lines fall to other rules). | Fix dispatched: port the grammar into description parser/AST + layout mapping. |
| 2026-07-04 | P2/i4 done | **CommandLinkElement grammar ported** (ca67673): full arrow regex + decors + direction/inversion + auto-create endpoints + tail/head qualifier labels + hidden→invis. Delta: **component EQUAL 18→44 (17%)**, edgeCount fails 159→77, no-candidate 12→2; usecase edges 34→25 (EQUAL still 1 — blocked on shape/cluster). Ratchet 19→45 pins. Agent root-caused two more mechanisms for later iterations: (a) empty containers: upstream emits cluster+synthetic point node, we flatten to leaf (balipa); (b) main edge label/labelWidth/Height never wired to the seam (babafi/bagoze labelOk). Also surfaced+fixed missing `!exit` preprocessor halt. Bare auto-created endpoints map to 'rectangle' (STILL_UNKNOWN analog). | Next (i5): shapeOk (104+50 fixtures) — USymbol→svek shape parity, the largest independent category. |
| 2026-07-04 | P2/i5 | **Mechanism (clusters own the shape/node/degree mass).** Probes show oracle non-rect shapes are cluster machinery, not per-symbol mapping: `shape=point,width=.01` synthetic nodes come from ClusterDotString.java:149-183 (special anchor points for edges targeting groups + placeholders in empty clusters), Cluster.java:600 (`clusterNempty`), DotStringFactory.java:224; `shape=plaintext` HTML-table nodes are shielded entities (EntityImageDescription.getShield:239, SvekNode.appendHtml). Our divergence: edges to containers anchor to the first descendant LEAF (no extra node) vs upstream's dedicated point node inside the cluster (+1 node, degree shift); empty containers flatten to leaves vs (sometimes) clusters with point placeholders — babafi's empty component was a LEAF in oracle while berufi has 3 points, so the leaf-vs-cluster condition must be diagnosed precisely, not guessed. Re-prioritized: i5 = cluster DOT emission port (upstream of nodeCount 71 / shape 104 / degree 111 / cluster 91); shield-plaintext deferred to its own iteration. | Agent dispatched with diagnosis-first mandate. |
| 2026-07-04 | P2/i5 done | **Cluster point-anchor emission ported** (a616cc0, pins 7663d28→45→92): Bibliotekon.getNodeUid falls back to Cluster.getSpecialPointId ("za"+uid) when an edge endpoint IS a group — one shared point node per referenced group, member of its own cluster. Empty description groups are ALWAYS demoted to leaves (GroupType.PACKAGE hardcoded in the block commands; GraphvizImageBuilder.java:416-418) — our behavior already matched. Side-fixes: cluster ids c0→cluster0 (ours were invisible to clusterOk since forever); `remove A` was a no-op (minimal CommandRemoveRestore port). Delta: **component EQUAL 44→91 (35%)**, clusterOk 91→31. Four residuals ledgered (together{}, edge-to-member anchor redirect, invis ordering edges, port placeholders). | Next (i6): shield/plaintext shapes (95+50) — hideText/asSmall entities (actors, lollipops) emit shape=plaintext HTML per EntityImageDescription.getShield:239-260 + SvekNode.appendHtml; includes the ShapeType→shape map (appendShapeInternal). Usecase corpus is blocked on exactly this. |
| 2026-07-04 | B1 close | Executor follow-ups to T2's commit: added rankdir/nodesep/ranksep to the aggregate's per-check breakdown (T2 had deliberately deferred to honor byte-compatibility during parallel work); split drill-down into scripts/dot-sync-drilldown.ts (file-size hook, 501>500); replaced `1 << 28` and svek regex literals that broke lizard's TS parser (bogus CCN-67 swallow-to-EOF); gitignored /svek.dot,/svek.svg (oracle-jar trace droppings, DotStringFactory.java:312). | Fresh component breakdown under new bar: 0/263 EQUAL; rankdirOk fails 229, nodesepOk/ranksepOk 233. All four gates green (3083 tests). |
| 2026-07-04 | P2/i5 done | **Group-anchor point mechanism ported.** `Bibliotekon.getNodeUid` falls back to `Cluster.getSpecialPointId` (`"za"+uid`) for any edge endpoint that IS a group entity — one shared point per referenced group (never per edge), emitted as a direct cluster member (`ClusterDotString.java:149`). Confirmed empty-group leaf-demotion (`GraphvizImageBuilder.java:416-418`) is unconditional for description diagrams (every block-group command hard-codes `GroupType.PACKAGE`) — no change needed there, matches our existing behavior. Two bugs found and fixed as necessary side effects: (1) our cluster ids were `c0`/`c1`/… but the comparator requires literal `/^cluster\d+$/` — every one of our clusters was invisible to clusterOk regardless of the anchor mechanism (renamed to `clusterN`); (2) that rename unmasked a pinned-ratchet regression (zijase-36-cice967, `remove A` emptying a frame) — `remove <id>` was unimplemented entirely; added the minimal simple-identifier form (CommandRemoveRestore.java) — `<<stereotype>>`/`@unlinked` matching stays out of scope, ledgered. Two NEW distinct mechanisms discovered and deliberately NOT fixed this iteration (ledgered, need their own drill-down): `together{}` blocks aren't modelled as sub-clusters, and berufi-69-dara369 shows edges to a cluster **member** (not the cluster itself) redirected to the anchor — a different, unexplained mechanism, not `ClusterDotString`/`Bibliotekon`. | Delta: **component EQUAL 44→91 (17%→35%)**, clusterOk fails 91→31, degreeOk 111→104, shapeOk 104→95; usecase clusterOk 15→10, degreeOk 33→31 (EQUAL steady at 1 — blocked on the two newly-discovered mechanisms above). All four gates green (3187 tests); ratchet 45/45 held. |
