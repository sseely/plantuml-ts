# G5 decision journal

## C0

| # | Decision | Reasoning | Alternatives considered |
| --- | --- | --- | --- |
| 1 | Build the calibration harness to compare against the font-size the ORACLE SVG reports for each sample, not a size guessed from the sample's diagram role. | The only way to isolate "is the measurer calibrated" from "does a call site feed the wrong size" is to hold size fixed at the jar's own ground truth per sample. Conflating the two was exactly what produced the "calibration gap" framing in G4 S13. | Guessing a size per text role from diagram-type conventions — rejected: would silently re-introduce the same conflation the harness exists to resolve. |
| 2 | Exclude `!pragma layout elk` and `@startjson` fixtures from the calibration buckets, after discovering they were the dominant driver of the FIRST report run's non-trivial error buckets. | Both route through code paths (`CucaDiagramFileMakerElk.java`; the JSON engine's own placeholder-text renderer) outside the svek/graphviz pipeline this port implements — their divergence is not a text-measurement signal. Confirmed narrowly (8 fixtures / 5 fixtures) before excluding, not assumed. | Leaving them in and reporting a noisier, harder-to-read top-N list — rejected: would have buried the real (zero) measurer-defect signal under two unrelated, already-out-of-scope code paths. |
| 3 | Do NOT modify any file under `src/diagrams/**`, even after identifying the exact five call sites causing the S13 gap, because the write-set restricts changes to `src/core/** measurer/font-metrics modules only`. | Hard boundary in the mission brief; matches `~/.claude/rules/autonomous-execution.md`'s explicit STOP condition ("a task requires modifying files outside its declared write-set AND those files aren't in any other task's write-set either"). The mission brief's own "Calibrate" task presupposed the fix would land in a measurer module — that premise didn't survive the diagnosis, and I don't have authorization to silently widen my own write-set. | Fixing it anyway since the pattern is proven safe elsewhere (`CARDINALITY_FONT_SIZE`, `ARROW_LABEL_FONT_SIZE`) — rejected: the mission brief flags DOT-gate movement from a size-driven change as "an orchestrator decision," which this iteration cannot make unilaterally regardless of how safe the pattern looks. |
| 4 | Report "0 measurer-level bugs found, fix is out of write-set" as a complete, valid C0 outcome rather than searching further inside `src/core/**` for SOME fixable issue to justify landing something. | Task quality bar is accuracy, not activity — inventing an in-scope change to have "landed something" would be worse than reporting the true, negative-but-conclusive result (measurer is exact; real bug is elsewhere). The `code-principles.md` "build to defined scope" rule cuts the other way too: fabricating scope to fill the write-set is the same failure mode as trimming a real requirement. | Widening the diagnosis to force a measurer-side workaround (e.g., a measurer variant defaulting to 13pt for short all-caps strings) — rejected as a heuristic hack that would silently mask the real bug and diverge from upstream's actual FontParam model. |
| 5 | Investigated but did NOT root-cause the secondary `gutute-00-gaki684` (component port-label) finding beyond ruling out scale/transform and the `svek_trace` pragma. | Low corpus impact (1/265 component fixtures), unrelated to the S13 founding mechanism this mission exists to unblock. Diminishing returns vs. the mission's actual deliverable. Logged as a named follow-up (README item 3) rather than silently dropped, per this project's divergence-tracking discipline. | Continuing the investigation to full root cause — deferred, not abandoned; flagged explicitly for a future iteration rather than left as an anonymous miss. |

## C1

| # | Decision | Reasoning | Alternatives considered |
| --- | --- | --- | --- |
| 1 | Verified each of the five call sites against upstream (`FontParam.ARROW(13, normal)`, `klimt/font/FontParam.java:54`) individually before editing, including checking whether a `skinparam ArrowFontSize` override path exists in this port's `core/skinparam.ts` (it does not — `ELEMENT_BUCKET_SNAMES` omits `'arrow'`) and whether any corpus fixture would regress if it did (`kekeli-82-vagi037`, `figika-36-sola271` both use `ArrowFontSize`/`ComponentArrowFontSize`, neither in the frozen gate corpus). | Per diagnosis.md discipline: no fix before a stated mechanism, and the mission brief explicitly asked to confirm 13 is right "for THAT site" per call site, not assume the pattern transfers uniformly. | Trusting C0's Part B table verbatim without re-deriving per site — rejected: C0's own table entry for `description/layout.ts:735` turned out to be materially incomplete (see decision 4 below), so per-site verification caught a real gap C0's static audit missed. |
| 2 | Landed sites 1 (`state-dot-graph.ts`), 2/3 (`state-composite-pass.ts`), 4 (`class-dot-graph.ts`) as separate, individually-gated edits, each with a jar-evidence-anchored TDD unit test (real oracle `textLength` values from cached `in.svg`, not synthetic strings) before the source change. | Matches the mission's own "ONE AT A TIME" protocol and testing.md's TDD requirement; jar-anchored assertions (not just "13 ≠ 14") give the test genuine diagnostic value if the measurer itself ever regresses. | Batching all four sites into one commit-sized change before any gate check — rejected: the mission brief explicitly requires isolating which site (if any) moves a protected set, and batching would have destroyed that diagnostic power (as decision 5 below proves it mattered). |
| 3 | Split `description/layout.ts` (793 lines, over the 500-line hook cap) into `layout.ts` + a new `layout-dot-tree.ts` as a PURE MOVE (zero behavior change) before attempting site 5's actual font-size fix, verified via a dedicated pre/post regression pass (DOT gate + full description/class/object test suites) between the split and the fix. | The complexity hook blocks ANY edit to an over-cap file regardless of edit size; `complexity-hook-workarounds` memory's own precedent (`svg.ts`→`svg-markers.ts`) sanctions exactly this mechanical split. Verifying the split alone (before layering the real fix on top) isolates "did the split break anything" from "did the fix break anything" — two independent questions that would have been undiagnosable if bundled. | Cramming the fix into the existing over-cap file via inline `#lizard forgives` escalation only — not viable: the 500-LINE cap (unlike the per-function NLOC/CCN caps) has no forgives escape hatch; the split was mandatory, not optional. |
| 4 | Discovered mid-implementation that `description/layout.ts:735`'s single `fontSpec` is NOT edge-label-specific (contra C0's Part B table entry) — it is shared with node/title/leaf measurement (`buildDotNodes`, `buildPortClusterInfoByAstId`, `degenerateSingleLeaf`) throughout `runLayout`/`layoutDescription`. Fixed by threading a SEPARATE `edgeFontSpec` (13) into `runLayout`, applied ONLY to the two edge-label-consuming calls (`buildDotEdges`, `computeGraphSpacing`), leaving the shared `fontSpec` (14) untouched for every node/title call. | Naively swapping the shared `fontSpec` to 13 would have narrowed EVERY entity/title measurement in every component/usecase diagram — a correctness regression far exceeding the mission's edge-label scope, caught by reading `runLayout`'s full call graph before editing rather than trusting the C0 table's site description. | Swapping `fontSpec` wholesale and letting the DOT gate/censuses catch the fallout — rejected: node/title mis-sizing is EXACTLY the class of regression the "ANY movement = STOP" protocol exists to prevent, and finding it via a failing gate (rather than via reading the code first) would have cost a full gate cycle for a mistake diagnosable by inspection alone. |
| 5 | Reverted sites 2 and 3 (`state-composite-pass.ts`'s `addLevelEdges`/`sweepOrphanEdges`) after landing them and running the FULL battery (not just the DOT gate) revealed a `tests/oracle/state-dot-parity.test.ts` (size-backlog ratchet) regression: 16-17 of 103 backlog fixtures' `maxSizeDeltaIn` grew past their pinned tolerance. Bisected via `git show HEAD:<path>` A/B per file (site 1 alone: 0 regressions; site 2 alone: 16; site 3 alone: 1 additional; site 4 alone: 0) per diagnosis.md before deciding to revert, rather than guessing which site(s) to keep. Kept sites 1 and 4 (proven zero-impact); reverted sites 2 and 3 in full, including deactivating (not deleting — `describe.skip` with a full explanatory header) their TDD tests, which remain jar-verified-correct and ready for C2. | The `size-backlog.json` protected-set rule is explicit ("tighten-only... entries may only shrink/pass") and the mission brief's DOT-gate STOP protocol's spirit clearly extends to it: a change that regresses a protected/frozen artifact must be isolated and reported as a finding, not silently shipped because the DOT structural gate (a DIFFERENT, narrower check) stayed clean. Root-caused (not just observed) before reverting: EVERY regressed fixture already carried a nonzero `size-backlog.json` entry BEFORE this change (a pre-existing, ALREADY-NAMED gap in `state-composite-autonom.ts#buildPlainAutonomSpec`'s `childImg = Math.max(geometry.width, result.width)` floor, explicitly flagged "NOT FULLY CLOSED" and "Queued for S5" in that function's own doc comment from an earlier mission) — the font-size fix doesn't introduce a new defect, it removes the ACCIDENTAL oversizing (wrong font inflating `result.width`) that was PARTIALLY masking this pre-existing floor-formula gap. See ledger §C1 for the full mechanism and the connection to G4 S11-S13's parked edge-label-ink mechanism. | (a) Landing sites 2/3 anyway since the DOT gate stayed clean — rejected: violates the explicit tighten-only protected-set rule and the project's own "never fix apparent upstream bugs case-by-case" discipline extends to "never ship a proven size-backlog regression case-by-case" either. (b) Attempting to fix `state-composite-autonom.ts`'s floor formula in the same iteration to unblock sites 2/3 — rejected: out of the five-site write-set, and per diagnosis.md a formula fix needs its own jar-verified derivation and TDD cycle, not a bolt-on inside an already-large iteration; flagged as the clear C2 priority instead. |

## C2

| # | Decision | Reasoning | Alternatives considered |
| --- | --- | --- | --- |
| 1 | Kept `shiftToOrigin`'s `minX`/`minY` derivation nodes/edges-only, and only added clusters as a RECEIVER of that shift, never a contributor to deriving it. | Verified via direct probe: graphviz's own default cluster margin (8pt) means a cluster box legitimately extends beyond its topmost/leftmost member node in the raw pre-shift frame. Letting clusters participate in deriving the shift would move EVERY existing cluster-bearing caller's node/edge positions (state composites, class namespaces) by that margin — a real regression risk for a field (`DotLayoutResult.clusters`) that has zero consumers this iteration. | Including clusters in the min-shift derivation for "more faithful" canvas framing — rejected: the mission brief's own boundary is explicit ("Additive: existing consumers unaffected"), and this would violate it for no benefit until a consumer actually needs it. |
| 2 | Fixed the test suite's own incorrect assumption (`c.x`/`c.y` >= 0) once direct probing showed a real, legitimate negative cluster coordinate (`-8,-8`), rather than changing the implementation to force non-negative output. | The negative coordinate is CORRECT given decision 1 above (same frame, pure translation, cluster genuinely extends past the shift-origin-defining node) — the test's assumption was the bug, not the code. Pinned the exact deterministic values instead of a weaker bound, per testing.md's "assert on specific values, not just non-null" discipline. | Padding the shift to also cover clusters (making the test's original assumption true) — rejected, same reasoning as decision 1. |
| 3 | Invested significant time (~half the iteration) in a from-scratch jar-verified diagnosis of mechanism 16's render half (reading `ClusterHeader.java`/`ClusterDotString.java`/`SvekEdge.appendTable` from the Java reference, running the pinned jar directly on 4 synthetic minimal repros, and byte-sampling 19 real corpus fixtures) BEFORE attempting any `src/diagrams/state/*.ts` change. | Per diagnosis.md: "no fix before a stated mechanism." The task's own hint (S1/S3/S6's "16px vs 24px side margin") only explained PART of the divergence (size); reading the render dispatch (`renderer-composite-box.ts#renderComposite`) revealed a SECOND, independent gap (shape — the fallback vs jar's real half-rounded-header shape) that the task prompt did not name explicitly. Skipping straight to a size-only fix would have shipped a wrong-shape, right-size composite that still fails every fixture's diff. | Landing a size-only improvement immediately (real bbox, keep the dashed-rect fallback shape) since it doesn't touch the DOT gate or size-backlog ratchet — considered, but rejected: confirmed (by reading `renderer-composite-box.ts`'s dispatch) that the SHAPE mismatch alone would keep every affected fixture's diffCount non-zero regardless of size accuracy, so a size-only change couldn't move any ratchet — pure unverified motion with no jar-confirmed payoff, exactly the kind of "fix that doesn't fix" this project's rules warn against shipping as if it were progress. |
| 4 | Empirically verified (via a throwaway, NOT-landed probe script) that graphviz-ts's undocumented `HTML_STRING_MARK` (`String.fromCharCode(1)`) prefix DOES work end-to-end through the exact programmatic builder path `graph-layout-build.ts` uses, rather than stopping at "issue 05 says the engine supports it" and assuming that transfers to the programmatic API. | Issue 05's own RESOLVED note only demonstrated the capability via DOT-TEXT parsing (where `label=<...>` is natively recognized) — a MEANINGFULLY different code path from the programmatic `createGraph()`/`addSubgraph()`/`setAttr()` builder `layoutGraph()` exclusively uses. Assuming the DOT-text finding transferred without checking would have been exactly the "guess to make progress" diagnosis.md forbids. | Trusting issue 05's RESOLVED note as sufficient proof and either landing the marker technique directly or declaring the gap fully closed — rejected both: the first skips verification of the ACTUAL code path this port uses; the second would have been wrong (the programmatic path genuinely has no PUBLIC way to do this, confirmed by reading `api/index.d.ts`). |
| 5 | Did NOT land the verified-working `HTML_STRING_MARK` marker technique in `src/`, despite confirming empirically that it reproduces jar's real 19px header-height constant exactly (`HEIGHT=3` -> `gap=19`). Filed it as a new graphviz-ts issue (07) instead. | The technique depends on an internal, non-exported constant, not a public API contract — a materially different class of dependency than this iteration's two PRE-SANCTIONED channels (SVG-text regex for port labels; the new `clusters` snapshot field). The mission brief scopes sanctioned channels explicitly; silently adding a third, unlisted channel (reaching into an unexported control character) crosses into "ask first" territory per the boundary discipline, not something a single iteration should decide unilaterally — especially for a change this large (a new render function, new DOT emission strategy for every state composite cluster). | Landing it anyway, reasoning that "it works and is verified" — rejected: verified-to-work is necessary but not sufficient; an unexported internal implementation detail can change without notice in a graphviz-ts patch release with no changelog entry, unlike a public API. This is exactly the class of decision `architecture.md`'s reversibility-premium guidance flags for explicit escalation, not unilateral adoption. |
| 6 | Re-verified C1's own sites-2/3 hypothesis directly (read `bemena-23-zebu249`'s `.puml`, confirmed `Configuring`'s child pass has zero nested clusters) rather than assuming chunk 1's cluster-geometry landing would unblock it as the task prompt's framing suggested. | The task prompt's own hypothesis ("this may make the buildPlainAutonomSpec floor removable") was explicitly speculative language ("may"), and per diagnosis.md the only way to confirm or refute it is direct inspection of the actual fixture and code path, not trusting the framing. Finding it does NOT hold is itself a valuable, precise, jar-verified negative result — better than silently re-landing sites 2/3 without re-checking the prerequisite and discovering the size-backlog regression a second time. | Attempting to re-land sites 2/3 anyway to see if the DOT gate/size-backlog stayed clean this time (empirical retest without a stated mechanism) — rejected: this is precisely the "guess to make progress" pattern diagnosis.md forbids, and C1 already proved (via bisection) that this exact regression traces to the edge-label-ink gap, unrelated to what changed in C2. |

## C3

| # | Decision | Reasoning | Alternatives considered |
| --- | --- | --- | --- |
| 1 | Wired `setHtmlAttr` opt-in via TWO NEW `DotInputCluster` fields (`titleTableWidth`/`titleTableHeight`), distinct from the pre-existing `labelWidth`/`labelHeight` (the Svek-DOT TEXT emitter's own convention) — `addClusters` only calls `setHtmlAttr` when BOTH are present. | The mission's own caution was explicit: unconditional wiring would change emitted DOT for every cluster-bearing graph across all types. A new, separate field pair keeps every existing caller (class namespaces, description packages) byte-identical (verified: DOT gate + all four censuses re-checked before touching any `src/diagrams/**` file) while giving state a precise, single opt-in point. | Reusing `labelWidth`/`labelHeight` for both purposes — rejected: their existing value (`height = font.size`, 14) is NOT the jar-calibrated reservation height (3) `setHtmlAttr` needs; conflating the two would either break the Svek-DOT text emitter's own (already-passing) oracle comparison or force the layout seam to accept a wrong value. |
| 2 | Did NOT try to reproduce `ClusterHeader.java`'s own `getTitleAndAttributeHeight() - 5` Java-internal formula bit-for-bit. Instead resolved the "title-height-convention discrepancy" (C2's own sub-item 3) by DECOUPLING the graphviz layout-space reservation from this port's text-height convention entirely — a jar-CALIBRATED constant (`CLUSTER_TITLE_TABLE_HEIGHT = 3`), verified end-to-end through THIS port's own `setHtmlAttr` seam to reproduce jar's real 19px gap, not derived from reading `Display.create()`'s internal height math. | The FIXEDSIZE table's only role is telling graphviz how much space to reserve — the visible title text is drawn separately by this port's own renderer using its own measured line width (already jar-exact, G5 C0). Matching Java internals bit-for-bit would require reverse-engineering `TextBlockUtils`/`Display.create()`'s own height convention (explicitly flagged in C2 as "a nontrivial side investigation of its own") for zero behavioral benefit, since the reservation only needs to produce the SAME graphviz-side gap, verifiable directly. | Reading `TextBlockUtils`/`Display.create()` in full to derive the Java-internal `titleAndAttributeHeight` value precisely — rejected as unnecessary scope: the calibrated-constant approach is provably correct (132/134 real corpus samples, C3's own corpus-wide probe) without it. |
| 3 | Ran a disposable, corpus-wide probe (`scripts/_tmp-c3-cluster-probe.ts`, deleted before finishing) across EVERY cached `test-results/dot-cache/state/*/in.svg`'s `<g class="cluster">` element, rather than hand-checking only the mission's three named fixtures. | The mission's own bar is "≥15 hand-samples"; a corpus-wide programmatic sweep (134 real cluster samples) is strictly stronger evidence than a hand-picked subset, and it SURFACED a real bimodal split (14.8889px vs 15.8889px baseline offset) that hand-checking 3 fixtures would likely have missed entirely (none of the three named fixtures exercise the `portRanksLabelOnEe` branch). | Hand-verifying only the 3 named fixtures (`decede-10-buvu414`, `gojuja-90-pune699`, `bajelo-54-dixe684`) and calling the constant "verified" — rejected: this would have missed the entrypoint/exitpoint-family baseline-offset difference AND the multi-line-title exception, both of which the eligibility gate now correctly excludes because the sweep found them. |
| 4 | Gated title-table eligibility on `ctx.insideAutonomPass !== true` (a NEW `DiagramCtx` field, set `true` inside `buildPlainAutonomSpec`/`buildConcurrentBranchAcc`'s own child-pass construction) AFTER discovering a real `state-dot-parity.test.ts` size-backlog regression on `fotuje-06-fifa085`/`rovese-43-tadu368`. | Per diagnosis.md: instrumented before hypothesizing — traced the regression to a nested cluster's real HTML title reservation changing THAT SPECIFIC pass's own `layoutGraph()`-computed `result.width`, which feeds the ALREADY-PARKED `buildPlainAutonomSpec#Math.max(geometry.width, result.width)` floor (C1's own "Queued for S5" finding). The write-set forbids touching that floor directly (PARKED, needs sign-off per the 3-strike rule), so the only compliant fix is to prevent MY change from perturbing its input — confirmed via `git show`-free A/B (toggling the eligibility gate) that this exact scoping restores 268/268 with zero loss to the mission's own three named fixtures (`bajelo-54-dixe684`'s `Run`, ALSO nested-in-autonom, happens not to flip the floor's outcome on its specific geometry, confirmed by direct measurement, not assumed). | Raising the two affected fixtures' `size-backlog.json` entries to absorb the regression — rejected: EXPLICITLY forbidden by the write-set's "final-state tighten-only" rule. Attempting to fix `buildPlainAutonomSpec`'s floor formula in the same iteration to unblock nested clusters too — rejected: that mechanism is PARKED (3-strike rule, requires orchestrator/maintainer sign-off), out of scope regardless of how tempting the unlock looks. |
| 5 | Discovered (via direct SVG diff inspection on `gojuja-90-pune699`, NOT assumed) that the initial eligibility gate — `!ctx.classify.needsAnchor.has(s.id)` — was WRONG: too broad, excluding composites like `state A { [*] --> Configuring }` (Configuring declared OUTSIDE `A`) that need a boundary anchor point but have NO direct border-point (`<<entrypoint>>`/`<<exitpoint>>`) children at all. Replaced with the PRECISE test `applyBorderPointRanks` itself uses (`directMembers.some(isInputPosition \|\| isOutputPosition)`). | `needsAnchor` fires for ANY composite requiring an anchor for an EXTERNAL edge to connect through its boundary — a materially broader condition than "has border-point children" (`portRanksLabelOnEe` only fires on the latter, narrower case). Diagnosed by comparing our own render output against the real oracle SVG byte-for-byte (`A` renders `class="cluster"` in the real jar, not the dashed-rect fallback my first gate produced) rather than trusting the initial hypothesis. | Shipping the broader `needsAnchor` exclusion as "safe/conservative" — rejected once direct evidence showed it silently regressed a LARGE, common case (any composite with an internal `[*] --> external` transition) back to the pre-C3 fallback shape for no jar-verified reason. |
| 6 | Restructured the renderer's child-nesting to FLATTEN a cluster-shaped composite's children as document-level SIBLINGS (`renderClusterSiblingMarkup`, new function, `renderer.ts`) instead of nesting them inside the cluster's own `<g>` wrap — discovered via direct SVG structural diff (jar's real `decede-10-buvu414`: `<g class="cluster">`(E's shape only)`</g><g class="entity">`(F, a SIBLING)`</g>`, NOT F nested inside E). | The DOT-native cluster/subgraph model has no coordinate-space containment the way `InnerStateAutonom`'s own wrapper does — every OTHER composite kind (leaf, autonom) DOES nest children inside its own `<g>`, so this is a genuinely different, cluster-specific document-structure rule, not a bug in the general recursion. Verified the fix doesn't regress autonom/leaf nesting (full test suite, unchanged). | Leaving children nested (matching the general `renderNodeWrapped` convention) and accepting the resulting `childCount` mismatch as a remaining gap — considered, but the fix was cheap, well-isolated (dispatches only on `clusterHeaderHeight !== undefined`), and closes a real, verified structural divergence rather than leaving it as unexplained residual noise. |
| 7 | Tightened `oracle/goldens/state/size-backlog.json` (103 → 92 entries; 10 fixtures removed at exactly 0, 13 shrunk) via a disposable probe measuring EVERY currently-listed fixture's actual `maxSizeDeltaIn` against its pinned value, rather than leaving the backlog untouched since no NEW fixture reached full byte-exact zero-diff this iteration. | The write-set explicitly includes `size-backlog.json` (final-state tighten-only) as an iteration deliverable, independent of the golden-ratchet byte-exact metric — DOT-structural-equality-plus-size accuracy is a real, separate, jar-verified improvement (real cluster bboxes now feed composite ink-extent for every ELIGIBLE top-level cluster) even where full SVG parity remains blocked by OTHER, separately-diagnosed gaps (document order, conditional body-fill — see ledger §C3's "not closed" list). | Leaving the backlog untouched since "no new fixture reached zero" — rejected: conflates two INDEPENDENT protected metrics (the mission brief's own "treat the two protected sets as INDEPENDENT checks" rule, C1's precedent) — size accuracy improved measurably and verifiably, and the tighten-only rule exists precisely to capture exactly this kind of partial, real progress. |
| 8 | Split `state-composite-pass.ts` (549 lines after this iteration's additions, over the 500-line hook cap) into `state-composite-pass.ts` (362 lines) + a new `state-composite-pass-types.ts` (217 lines, `DiagramCtx`/`GeoSpec`/`ExtractAutonomSpec`) as a PURE MOVE, mirroring this file's OWN established split precedent (four prior modules already split out of it) and `state-geo-types.ts`'s identical "types-only sibling" shape. | The complexity hook blocks ANY edit to an over-cap file regardless of edit size, per this mission's own hard boundaries ("split when you first touch one"); I touched this file (new `DiagramCtx`/`GeoSpec` fields for mechanism 16) and pushed it further over the pre-existing-but-untouched cap, so the split was mandatory, not optional. Verified zero behavior change (re-export shape preserves every existing external import path) via the full test suite before/after. | Cramming the new fields into the existing over-cap file — not viable, same as C1 decision 3's identical precedent (no forgives escape hatch for the 500-line cap). |
| 9 | Did NOT pursue the entrypoint/exitpoint family (mission item 3) this iteration, despite deriving its own baseline-margin constant (5, matching the autonom shape) from the SAME corpus-wide probe. | Landing it requires ALSO resolving the two NEWLY-discovered, more fundamental open items this iteration surfaced (document order for cluster-vs-autonom-vs-leaf siblings; whether/when the body-fill path is drawn for a content-less cluster like `gojuja-90-pune699`'s `A`) — both apply to EVERY cluster shape, entrypoint family included, and neither was jar-verified precisely enough to land safely within this iteration's remaining time. Landing a half-verified entrypoint shape risked exactly the case-by-case-approximation failure mode `CLAUDE.md`'s porting-discipline section warns against. | Landing the entrypoint baseline-margin constant alone, unverified against the two open document-structure items — rejected: per the mission's own "if the composite rewrite proves deeper than one iteration, land the seam adoption + whatever is jar-verified... do not force" instruction. |

## C4

| # | Decision | Reasoning | Alternatives considered |
| --- | --- | --- | --- |
| 1 | Gated the new `measuredLabelBox` mechanism behind a SEPARATE, purpose-built opt-in flag (`DotInputEdge.attributes.measuredLabelBox`), not mere presence of the pre-existing `labelWidth`/`labelHeight` fields, even though the task's own wording suggested "opt-in via the existing labelWidth/labelHeight attrs." | Direct grep confirmed `labelWidth`/`labelHeight` are ALREADY set by class/description/dot/state's-own-flat-pipeline, for a DOT-parity-comparator-only, size-tolerant echo-back — gating on mere presence would have engaged the real-reservation mechanism for every diagram type simultaneously on the first `addEdges` call, directly triggering the task's own named cross-type blast-radius risk (item 2) without any per-type verification. A dedicated flag keeps every existing caller provably byte-identical (confirmed: DOT gate + all four censuses re-checked mid-iteration while the mechanism was landed, before any revert). | Gating on `labelWidth !== undefined && labelHeight !== undefined` literally as worded — rejected: would have required auditing and re-verifying class/description/dot's own DOT-gate and census behavior under a completely different label-sizing regime within the SAME iteration already spending its full budget on the state-specific mechanism, for zero benefit to this iteration's own named deliverable. |
| 2 | Scoped `measuredLabelBox` to the note-free case only (`t.linkNote === undefined`) in `state-composite-edge-label.ts#edgeLabelAttrs`, leaving `note on link`'s merged multi-line box on the pre-existing plain-text path. | `measureLinkNote`'s merged box is a DIFFERENT approximation (multi-line, fold decoration) never independently jar-verified against this specific mechanism; the task's own named control set and re-land targets (sites 2/3, `EvNewValueSaved`/`go to yes`) are all single-line plain labels. Extending to the note-merged case would have been an UNVERIFIED expansion of scope beyond the task's own named re-land target. | Applying `measuredLabelBox` unconditionally to every edge label including note-merged ones — rejected: would have widened the mechanism's blast radius (and this iteration's own diagnostic surface) without any control-set fixture exercising that path to verify against. |
| 3 | Ran a disposable synthetic probe (`scripts/_tmp-c4-probe1.ts`) to verify the injection mechanism drives graphviz's own layout decision BEFORE wiring it into `src/diagrams/state/*.ts`, mirroring S12/S13's own probe-first discipline. | Per diagnosis.md: instrument before hypothesizing. Confirming the mechanism works in isolation (different canvas height/labelX for different fed dimensions) before touching production state code isolates "does the core mechanism work at all" from "does it produce jar-correct results for this specific fixture family" as two independently-falsifiable questions — exactly the same discipline that let C1 cleanly bisect its own five-site regression by site. | Wiring directly into `src/diagrams/state/*.ts` and debugging via the full test suite if something broke — rejected: would have conflated "is `setHtmlAttr` on an edge handle even the right API" with "is the state-specific formula correct," costing more total iteration time if the API itself had a defect (it did not, per the probe, but that was unknown beforehand). |
| 4 | Measured the FULL 15-fixture control set (S13's own 4 + S12's own 12, deduplicated) via a disposable delta-computation probe (`scripts/_tmp-c4-deltas.ts`) against EVERY currently-listed `size-backlog.json` fixture (93 total), not just the named control set, before deciding pass/fail. | The task's own bar names an explicit control set, but computing every backlog fixture's delta in one pass (a) confirmed no ADDITIONAL non-control-set fixture crossed a zero-gap boundary (ruling out a wholly new defect class) and (b) surfaced the 8 additional non-control-set regressions worth naming in the ledger for whoever attempts the floor fix next, at negligible extra cost (the same probe run already computes every fixture). | Only checking the 15 named control-set fixtures via the full `state-dot-parity.test.ts` vitest run and stopping there — considered, but the standalone probe was faster to iterate on and gave the SAME numbers (cross-checked: the vitest run's 14 failures exactly match the probe's 14 FAIL rows), so running both cost little and gave stronger evidence for the "no new defect class" ruling-out step. |
| 5 | REVERTED the entire attempt (7 files, both temp scripts) rather than landing the 9 net-improved fixtures' worth of infrastructure and leaving `state-dot-parity.test.ts` red, or widening `size-backlog.json` to absorb the 6 (14) regressions. | The write-set's own hard boundary is explicit ("size-backlog.json — final-state tighten-only"); widening any entry to accommodate a regression is not a judgment call, it is a stated hard constraint. Leaving the test suite red violates the quality bar ("`npm test -- --run`" must pass). The task's own protocol is explicit and matches S12/S13's own established precedent exactly: "if this attempt fails the control set, journal it and stop." | (a) Landing the mechanism with `size-backlog.json` entries widened for the 6 (14) regressed fixtures — rejected: explicitly forbidden by the write-set's tighten-only rule, not a judgment call. (b) Landing only the parts that don't touch the 6 regressed fixtures (e.g. keep the injection mechanism but revert JUST the render-position readback, or vice versa) — rejected: per the task's own boundary, splitting the single principled derivation into sub-variants to see which one "caused" the regression IS exactly the forbidden formula-search pattern this iteration's authorization explicitly disallows ("do not try formula variants past your first principled derivation"). (c) Reporting the mechanism as "landed with 6 known regressions, fix deferred" without reverting — rejected: leaves a red protected-set test on `main`-bound work, which no prior iteration in this mission has ever done regardless of how well-diagnosed the residual gap was. |
| 6 | Did NOT attempt to also fix `state-composite-autonom.ts#buildPlainAutonomSpec`'s `Math.max` floor in the SAME iteration, despite identifying precisely what a next-derivation candidate would look like (fold `TransitionGeo.label`'s real box into `addNodeInk` directly). | That floor is ALREADY 3-strike-parked from S4 + S13's three formula-variant attempts (four prior attempts total) — a fifth attempt is a SEPARATE mechanism from this iteration's own sign-off ("Attempt the ink mechanism," which the maintainer authorized specifically for the injection/readback derivation, per the task's own framing of "the mission's 3-strike rule" applying to THIS mechanism, not implicitly extending to the floor formula too). Touching it without its own explicit sign-off would repeat exactly the unauthorized-mechanism-expansion pattern `architecture.md`'s reversibility-premium guidance and this mission's own escalation discipline both warn against. | Attempting the floor fix anyway since a specific, well-scoped next derivation is now visible (fold the real label box into the ink walk) — rejected: however well-scoped, it is still a NEW formula attempt at an already-4-times-failed mechanism, squarely inside "do not try formula variants past your first principled derivation" and squarely requiring its own sign-off per the 3-strike rule this mission has consistently honored (S13's own closing paragraphs explicitly named this requirement for exactly this floor). |

## C5

| # | Decision | Reasoning | Alternatives considered |
| --- | --- | --- | --- |
| 1 | Derived document order from `GraphvizImageBuilder.java`/`Bibliotekon.java` source directly (reading `printGroups`/`printEntities`/`drawU`'s three loops) rather than trusting C1/C3's own "real states first, pseudo last" heuristic comment in `state-composite-pass.ts` at face value. | That comment's own S5/S7 history already shows it was revised once (S7's `nelupe-49-xova546` counter-example); a THIRD revision (cluster-hoisting) needed the SAME rigor -- diagnosis.md's "no fix before a stated mechanism" applies to modifying an EXISTING, previously-verified mechanism just as much as to a new one. | Trusting the corpus pattern alone (cluster always first, 35+ samples) without reading source -- rejected: strong correlational evidence but not a stated MECHANISM; the source read also revealed the edge-ordering sub-finding (decision 2) that pure corpus-pattern-matching on STATES alone would have missed entirely. |
| 2 | Discovered and fixed the edge/transition document-order bug (`buildLevelTransitionGeos` now sorts by `creationIndex`) as part of THIS iteration, even though the task's own item 1 named only "cluster/autonom/leaf nodes." | Reading `GraphvizImageBuilder.java` past the `printGroups`/`printEntities` split (this iteration's own primary target) to the very next lines (`for (Link link : dotData.getLinks())`) revealed the SAME registration-order mechanism applies to edges, and `gojuja-90-pune699`'s own two named-fixture transitions were VISIBLY swapped (`svg/g[1]/g[5]/@id`/`g[1]/g[6]/@id`) in a direct `compareSvg` probe -- fixing item 1 (states) alone would have left the mission's own named target fixture with a NEW, jar-verified, easily-fixable divergence unaddressed, for no principled reason (the SAME source read, the SAME mechanism, the SAME `sortSpecsByCreationIndex` primitive already exported). | Reporting the edge-order bug as a NEW C6+ queue item without fixing it, staying strictly inside the task's literal "nodes" wording -- rejected: the fix was a two-line, already-verified-safe addition (reuses an existing, unmodified function) directly downstream of the SAME investigation, not a scope expansion into a different mechanism; per code-principles.md's own guidance, a fix this cheap and this directly evidenced belongs in the SAME commit as its discovery, not deferred for someone else to re-derive from scratch. |
| 3 | Scoped BOTH `sortSpecsByDocumentOrder` (states) and the `buildLevelTransitionGeos` sort (edges) narrowly: states to the top-level pass ONLY (matching C3's own `insideAutonomPass` precedent); edges UNIFORMLY at all 6 existing call sites (not top-level-scoped). | The state-hoisting rule is genuinely UNVERIFIED for nested clusters (no corpus fixture exercises cluster-inside-cluster or cluster-inside-autonom-pass sibling ordering) -- applying it recursively would be an unverified guess. The edge-sort rule has NO such scoping concern: `TransitionGeo.creationIndex` is ALREADY correctly threaded at every one of the 6 call sites (a pre-existing invariant, not something this iteration built), and the underlying jar mechanism (`SvekResult#drawU`'s edge loop) is identical at every pass level by construction -- there was no basis to scope it narrower than "every call site," and doing so would have left OTHER passes' own edges silently un-sorted for no jar-verified reason. | Scoping the edge-sort to the top-level pass only, symmetrically with the state-hoisting scoping -- rejected: the two mechanisms have DIFFERENT verification profiles (one is genuinely untested outside top-level, the other has zero scoping basis in the jar source or the corpus), so applying the SAME scoping discipline to both would have been over-cautious for the edge case specifically, not principled. |
| 4 | Derived the conditional body-fill rule from `Cluster.java#drawUState` -> `RoundedContainer#drawU` -> `RoundedSouth#drawU`'s own `if (backColor.isTransparent()) return;` check, INSTEAD of my own first hypothesis ("draws body-fill iff the cluster has real declared content vs. anchor-only pseudo children"). | The first hypothesis fit `gojuja-90-pune699`'s own `A` (anchor-only, 1-path) and `decede-10-buvu414`'s own `E` (real content `F`, 2-path) -- but `bajelo-54-dixe684`'s `Run` (a DIFFERENT cluster, real declared children `Chg_Sector`/`Do_Sector`, yet 1-path in the real oracle) directly CONTRADICTS it. Per diagnosis.md, a hypothesis that fits 2 samples but is refuted by a 3rd is not yet a mechanism -- reading the actual Java resolved the contradiction (Run has no BACKGROUND override, matching the transparency rule; its real content is irrelevant to south-fill presence). | Shipping the "real content vs anchor-only" rule anyway since it matched the mission's own two NAMED fixtures — rejected: `bajelo-54-dixe684`'s counter-example was found BEFORE landing anything (checked proactively, not discovered as a regression after the fact), and shipping a rule known to be wrong for a THIRD sample already in the corpus (even if that sample isn't currently eligible for the real cluster shape) would have been exactly the "guess to make progress" pattern diagnosis.md forbids. |
| 5 | Approximated jar's per-zone (`name`/`description`/`body`) style-selector resolution with the SINGLE existing `resolveStateFillBucketed(node, theme, STATE_DEFAULT_BACKGROUND) !== STATE_DEFAULT_BACKGROUND` check, rather than building a genuine 3-zone style-bucket system to match `EntityImageStateCommon.STYLE.addSName(name/description/body)` bit-for-bit. | The 3-zone system is a materially larger feature (a NEW per-zone style resolution path this port has never had, for a color-cascade dimension `resolveStateFillBucketed` was never designed to carry) -- building it to close a body-fill CONDITION (not a color VALUE) would be significant unrequested scope for a single boolean gate. The approximation is PROVABLY correct for every override tier this port ALREADY implements (per-node `#color`, stereotype-qualified `stateBackgroundColor<<X>>`, the `state`-element `<style>`/`skinparam` bucket) -- confirmed on 10/13 real 2-path corpus samples (the other 3 use the diagram-level `stateDiagram{}` cascade tier, a SEPARATE, already-named, pre-existing gap this port doesn't implement AT ALL yet, so no approximation of THIS specific gate could close it without ALSO building that unrelated feature). | Building the diagram-level `<style>stateDiagram{}</style>` cascade support in the SAME iteration to close the remaining 3/13 samples too — rejected: explicitly out of scope (C3's own "known, unrelated divergences" note already named it as a separate, pre-existing gap outside mechanism 16's scope; building it now would be exactly the unrequested-scope-expansion pattern `code-principles.md`'s "build to defined scope" rule warns against). |
| 6 | Ran a disposable width/margin probe on 3 ADDITIONAL cluster fixtures (`bujuta-44-rovo666`, `cakaxu-97-nexe753`, `fevida-60-kope208`) beyond the mission's own two named fixtures, AFTER landing items 1a/1b/2 but BEFORE declaring the iteration done, once `gojuja`/`decede`'s own residual diffs revealed a consistent width-delta pattern. | Per diagnosis.md: a symptom pattern seen on 2 samples (both mission-named, possibly cherry-picked by the mission author for a DIFFERENT reason) is not yet evidence of a corpus-wide gap. 3 more, independently-selected samples (2 confirmed the SAME symmetric width-margin delta; 1 confirmed a SEPARATE, already-understood exclusion reason) turned a "these two fixtures still have an unexplained residual" observation into a "this is THE remaining blocker for the whole cluster family" finding precise enough to report and hand off. | Declaring items 1a/1b/2 done and reporting "0 pins, cause unclear" without the extra samples — rejected: would have left the NEXT iteration to re-derive the SAME width/margin characterization from scratch, exactly the "diminishing the value of a finding by under-investigating it" failure mode this project's own diagnosis discipline exists to prevent. |
| 7 | Did NOT attempt to fix the newly-confirmed width/margin gap in this same iteration, despite having a plausible mechanism sketch (jar's real DOT emission likely sets an explicit cluster `margin=` attribute `graph-layout-build.ts#addClusters` doesn't). | A plausible sketch is not a jar-verified derivation -- per diagnosis.md, "no fix before a stated mechanism," and this iteration's own time budget was already fully consumed deriving and landing items 1a/1b/2 plus characterizing the width gap's SCOPE (decision 6). Attempting a fourth, under-derived fix in the same iteration risked repeating C4's own lesson (an under-verified formula attempt costs a full revert-and-reverify cycle) for a mechanism with even less upfront derivation than C4's own edge-label-ink attempt had. | Attempting a quick fix anyway since the mechanism sketch looked simple (add an explicit cluster margin) -- rejected: "looks simple" was exactly S4/S12/S13's own trap on the edge-label-ink floor (C1/C4's own repeated finding that a plausible-looking formula fix can move the WRONG direction on a specific fixture family without full jar verification); named precisely for C6 instead. |

## C6

| # | Decision | Reasoning | Alternatives considered |
| --- | --- | --- | --- |
| 1 | Implemented `TransitionGeo.labelInk` as a NEW field, deliberately separate from the pre-existing `label` (render-position) field, rather than repurposing `label.x/y` or adding width/height to it. | The render-position formula (`attachTransitionLabel`'s perpendicular-offset-from-spline-midpoint approximation) and the real graphviz-reservation position (`DotLayoutResult.edges[].labelX/labelY`) are two DIFFERENT numbers, per G4 S13's own direct measurement (`x=51.83` vs `labelX=317.94` for `bemena`'s widest edge, a ~266px gap) — conflating them would either break rendering (if `label` started using the reservation position) or silently mis-measure ink (if the ink walk kept using the render-position approximation). A separate field lets each formula use the coordinate frame it actually needs. | Reusing `label` for both purposes, accepting the render-position formula also drives the ink walk — rejected: this is exactly G4 S13's own Variant 1 (already tried, already reverted, already proven to regress 10 fixtures via the render-position formula's own systematic overshoot for simple 2-node composites). |
| 2 | Ran the geometry-fold change ALONE first (before re-landing C1's font-fix or C4's injection), per the authorization's own step ordering ("jar-verified on the control set FIRST"), even though I suspected (and confirmed) it would be internally inconsistent — `labelX`/`labelY` reflecting graphviz's own Times-font guess (uncalibrated) while `labelWidth`/`labelHeight` echo back our own (also-uncalibrated, pre-C1) measured value. | The authorization explicitly named this as step 1, separate from step 2's re-landing. Testing it in isolation surfaced a genuinely useful data point (14/15 control-set PASS, only `pesita` regressed) even though I already expected the full, calibrated stack (step 2) to look different — isolating variables one at a time is the diagnosis.md discipline, not an optional nicety. | Skipping straight to the combined stack (geometry-fold + font-fix + injection) to save iteration time — rejected: would have made it impossible to separately attribute `pesita`'s regression to "the fold alone" vs. "the fold interacting with calibrated data," a distinction directly useful for the C7 queue. |
| 3 | After landing the full stack (fold + font-fix + injection), tested the floor's own necessity by literally removing `Math.max(geometry.width, result.width)` and comparing FULL-CORPUS output (267/268 fixtures) with vs. without it, rather than trusting the authorization's framing ("once 1+2 make it dead") as sufficient justification to remove it outright. | Per diagnosis.md, "no fix before a stated mechanism" — the authorization's own hypothesis needed direct verification, not assumption, especially since C4's own attempt showed the floor's interaction with the injection mechanism was subtle and fixture-dependent (masking/unmasking effects). Testing corpus-wide (not just the 15/23 control set) gives strictly stronger evidence than the control set alone could — and this is exactly what surfaced the finding that the 6 residual failures are NOT the floor's fault at all. | Trusting the authorization's framing and removing the floor unconditionally without measurement — rejected: would have missed the key finding (floor provably dead, but 6 fixtures still fail for an UNRELATED reason) and potentially misattributed the residual failures to "the floor formula still needs work" rather than correctly attributing them to C4's own injection mechanism. |
| 4 | Reverted the ENTIRE stack (geometry-fold + font-fix + injection + floor removal) rather than landing the geometry-fold/floor-removal alone (which, by itself, changes nothing for any fixture without the font-fix+injection also present) or landing everything except the 3 byte-identical-to-C4 fixtures' own contributing sub-mechanism. | The geometry-fold and floor-removal are BOTH provably no-ops without the font-fix+injection landed too (uncalibrated `labelX`/`labelWidth` mismatch, confirmed in decision 2's isolated test) — so there is no smaller, safely-landable subset of this iteration's changes that both (a) does real jar-faithful work and (b) doesn't require the font-fix+injection package that causes the 6 regressions. Landing "just the dead code removal" with no calibrated data behind it would be landing an untested, purely theoretical formula change with zero verification value. | Landing the geometry-fold + floor-removal WITHOUT the font-fix/injection re-land, accepting the fold would be a no-op in that configuration (mathematically inert, since it's fed uncalibrated data) — rejected: shipping code that is provably inert without a companion change that itself cannot land is dead weight with no test/audit value, and violates the mission's own "no fix before a stated mechanism" bar (there's no fixture this configuration would jar-verify against, since the isolated test in decision 2 already showed it's WORSE than baseline on `pesita` for no compensating benefit). |

## C7

| # | Decision | Reasoning | Alternatives considered |
| --- | --- | --- | --- |
| 1 | Ran the JAR'S OWN cached `svek-2.dot` (not a synthetic construction) through real `dot -Tsvg` FIRST, before writing any source, to derive the exact wrapper structure and margin arithmetic. | Per diagnosis.md ("instrument before hypothesizing"), the task's own "~16px" framing needed direct verification, not trust — a real-`dot`-run against jar's OWN emitted DOT is the strongest possible evidence for "what does graphviz actually compute for this exact structure," strictly stronger than reading `ClusterDotString.java` alone (which shows the STRUCTURE but not the resulting NUMBERS without running it). | Deriving the margin purely from reading `ClusterDotString.java` + `position.c`'s `CL_OFFSET` constant and reasoning about the arithmetic without running real `dot` — rejected: the exact nesting depth (2 wrapper levels vs 3, and which are inside vs outside the drawn cluster) was genuinely ambiguous from source alone until the real run showed `cluster6`'s own reported polygon already includes the compounded margin. |
| 2 | Discovered the touched/untouched (16px vs 24px) split by comparing `decede`/`fevida`'s cached DOT (no "i" wrapper) against `gojuja`/`cakaxu`'s (has "i") DURING the derivation, rather than assuming a single flat wrapper depth for every cluster. | The task's own framing ("2 levels = 16px") named only ONE case; the FIRST corpus grep (`i=yes` vs `i=none`, 56/28 split out of 84) immediately showed the depth genuinely varies — trusting the task's single-case framing and hardcoding 2 levels unconditionally would have OVER-margined every untouched cluster (a real, verifiable regression on `decede`/`fevida`, not a theoretical risk). | Implementing only the touched (2-level) case first, since it matched the task's own literal wording, and deferring the untouched case to a follow-up — rejected: the untouched case (28/84, roughly a third of the corpus) is common enough that shipping only half the mechanism would have been a known-incomplete fix, and the SAME investigation (one more DOT-cache grep) that found the split also found the exact discriminating condition (`isGroupTouched`), so there was no cost to closing both cases in the same iteration. |
| 3 | Reused the PRE-EXISTING `ctx.classify.needsZaentPoint` flag (already computed, already consumed for the anchor-node decision) for `innerMarginLevels`'s 1-vs-2 split, rather than writing a new `isGroupTouched(s.id, ctx.classify.allTransitions)` call at the `resolveClusterComposite` call site. | Proved analytically (reading `needsZaentPoint`'s own OR-condition against `titleTableEligible`'s own exclusions) AND empirically (84/84 corpus zero-mismatch between "i"-wrapper presence and `zaent`-point presence) that the two conditions are IDENTICAL for every `titleTableEligible` cluster specifically — writing a second, independent call to the SAME underlying logic would have been needless duplication with a real (if small) risk of the two conditions silently drifting apart under a future edit to one but not the other. | Calling `isGroupTouched` directly, ignoring `needsZaentPoint`'s existing computation — rejected: `isGroupTouched` alone is EXPORTED and simple to call, but `needsZaentPoint` is the flag ALREADY driving the exact SAME real-world consequence (anchor-node creation) this iteration's margin decision must track — reusing it is both less code and a stronger correctness argument (two independent bugs would have to align for a silent mismatch, vs one). |
| 4 | Named the graph-layout wrapper subgraphs `${outerName}i`/`${outerName}p1` (mirroring jar's own `"i"`/`"p1"` suffixes) rather than an arbitrary internal name. | `tests/oracle/svek-dot.ts#parseClusters`'s own pre-existing doc comment explicitly names the EXACT pattern it skips ("Svek's protection wrappers clusterNp0/p1/a/i are skipped") — matching that naming convention was the most direct way to confirm (not just hope) the DOT-parity structural gate would treat the new nesting as invisible, and it additionally makes any future `--slug` drill-down diff immediately legible against jar's own real DOT (same suffix vocabulary). | An arbitrary internal name (e.g. `${outerName}_wrap1`/`${outerName}_wrap2`) — rejected: would have worked equally well for the comparator (any non-`^cluster\d+$` name is skipped), but would have thrown away the free legibility win and made a future reader re-derive the jar correspondence from scratch. |
| 5 | Verified the width-exact claim via DIRECT numeric comparison against real oracle `<rect>` geometry (not just `compareSvg`'s pass/fail boolean) for all 5 of C5's own named samples, before writing the ledger entry. | `compareSvg`'s diff list conflates MANY unrelated dimensions (color, class attribute, rounded-corner radius, multiple path vertices) into one flat array — a `pass=false` result alone would not have told me WHETHER the specific side-margin mechanism this iteration targeted was actually fixed, only that SOMETHING was still wrong. Extracting and comparing the exact `width`/`x` numbers in isolation is what let me confidently report "width EXACT, height still off" instead of a vaguer "still failing, unclear why." | Reporting only the aggregate `compareSvg` diff counts (e.g. "gojuja: 57 diffs, down from N before") — rejected: diff COUNT is not close to as informative as diff CONTENT for confirming a specific, narrowly-scoped mechanism; the project's own "assertion quality" discipline (testing.md) applies equally to diagnosis evidence, not just test assertions. |
| 6 | Discovered and named the NEW vertical/height residual as a SEPARATE, not-yet-derived mechanism, rather than attempting a quick formula guess (e.g. "add one more CL_OFFSET to the vertical case too") to try to close it in the same iteration. | The four measured samples' own height gaps (6, 6, 4.5, ~1) do NOT correlate with `innerMarginLevels`'s own level count (gojuja=2 levels/6px gap; fevida=1 level/6px gap; decede=1 level/~1px gap) — a flat "+1 more CL_OFFSET" guess would have been WRONG for at least 2 of the 4 samples by construction (proven by the data itself, not assumed), repeating exactly the "looks simple" trap C1/C4/C6's own ledger entries already named as this mission's own recurring failure mode. | Attempting a quick vertical-margin patch anyway, since the WIDTH mechanism's own derivation felt validating and the vertical gap "looked adjacent" — rejected: per diagnosis.md, "no fix before a stated mechanism," and this iteration's own time budget was already consumed deriving, verifying, and corpus-sweeping the SIDE-margin mechanism; a fourth, under-derived vertical-margin guess in the SAME iteration risked exactly the C4/C6 "formula-search" pattern the mission's own protocol explicitly forbids without a stated mechanism first. |
| 7 | Measured `bajelo-54-dixe684`/`rovese-43-tadu368`'s size-backlog deltas with THIS iteration's fix landed (per the task's own explicit instruction), using the SAME disposable delta-measurement script pattern C1/C4/C6 established, rather than skipping the measurement since the margin fix's own scope ("side margin") seemed unlikely a priori to touch the edge-label-ink mechanism these two fixtures are associated with. | The task explicitly required this measurement ("record explicitly... that measurement decides attempt 6's scope") — an a priori guess that the mechanisms are unrelated is not a substitute for the actual measurement C6's own queue explicitly asked for, and the measurement itself (byte-identical deltas) is a STRONGER, more useful finding than a mere confirmation of my own prior expectation: it definitively rules out C6's own "shared root" speculation for a seventh attempt's scoping. | Skipping the measurement since neither fixture's own composite is `titleTableEligible` (checked before running) and reasoning by inspection alone that the fix therefore cannot affect them — rejected: "checked by inspection, not run" is exactly the kind of unverified inference diagnosis.md's own discipline exists to prevent, and the task's own wording treats this as a required, not optional, measurement. |
