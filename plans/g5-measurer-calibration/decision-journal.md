# G5 decision journal

## C0

| # | Decision | Reasoning | Alternatives considered |
| --- | --- | --- | --- |
| 1 | Build the calibration harness to compare against the font-size the ORACLE SVG reports for each sample, not a size guessed from the sample's diagram role. | The only way to isolate "is the measurer calibrated" from "does a call site feed the wrong size" is to hold size fixed at the jar's own ground truth per sample. Conflating the two was exactly what produced the "calibration gap" framing in G4 S13. | Guessing a size per text role from diagram-type conventions — rejected: would silently re-introduce the same conflation the harness exists to resolve. |
| 2 | Exclude `!pragma layout elk` and `@startjson` fixtures from the calibration buckets, after discovering they were the dominant driver of the FIRST report run's non-trivial error buckets. | Both route through code paths (`CucaDiagramFileMakerElk.java`; the JSON engine's own placeholder-text renderer) outside the svek/graphviz pipeline this port implements — their divergence is not a text-measurement signal. Confirmed narrowly (8 fixtures / 5 fixtures) before excluding, not assumed. | Leaving them in and reporting a noisier, harder-to-read top-N list — rejected: would have buried the real (zero) measurer-defect signal under two unrelated, already-out-of-scope code paths. |
| 3 | Do NOT modify any file under `src/diagrams/**`, even after identifying the exact five call sites causing the S13 gap, because the write-set restricts changes to `src/core/** measurer/font-metrics modules only`. | Hard boundary in the mission brief; matches `~/.claude/rules/autonomous-execution.md`'s explicit STOP condition ("a task requires modifying files outside its declared write-set AND those files aren't in any other task's write-set either"). The mission brief's own "Calibrate" task presupposed the fix would land in a measurer module — that premise didn't survive the diagnosis, and I don't have authorization to silently widen my own write-set. | Fixing it anyway since the pattern is proven safe elsewhere (`CARDINALITY_FONT_SIZE`, `ARROW_LABEL_FONT_SIZE`) — rejected: the mission brief flags DOT-gate movement from a size-driven change as "an orchestrator decision," which this iteration cannot make unilaterally regardless of how safe the pattern looks. |
| 4 | Report "0 measurer-level bugs found, fix is out of write-set" as a complete, valid C0 outcome rather than searching further inside `src/core/**` for SOME fixable issue to justify landing something. | Task quality bar is accuracy, not activity — inventing an in-scope change to have "landed something" would be worse than reporting the true, negative-but-conclusive result (measurer is exact; real bug is elsewhere). The `code-principles.md` "build to defined scope" rule cuts the other way too: fabricating scope to fill the write-set is the same failure mode as trimming a real requirement. | Widening the diagnosis to force a measurer-side workaround (e.g., a measurer variant defaulting to 13pt for short all-caps strings) — rejected as a heuristic hack that would silently mask the real bug and diverge from upstream's actual FontParam model. |
| 5 | Investigated but did NOT root-cause the secondary `gutute-00-gaki684` (component port-label) finding beyond ruling out scale/transform and the `svek_trace` pragma. | Low corpus impact (1/265 component fixtures), unrelated to the S13 founding mechanism this mission exists to unblock. Diminishing returns vs. the mission's actual deliverable. Logged as a named follow-up (README item 3) rather than silently dropped, per this project's divergence-tracking discipline. | Continuing the investigation to full root cause — deferred, not abandoned; flagged explicitly for a future iteration rather than left as an anonymous miss. |
