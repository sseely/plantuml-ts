# G0b decision journal

| When | Task | Decision | Why | Flag for review? |
|---|---|---|---|---|
| 2026-07-13 | T4 | DONE (33 new tests, 100% coverage on chrome.ts/blocks.ts). Chrome blocks drawn via core/svg.ts primitives, NOT klimt — klimt has no fragment seam (its UGraphicSvg emits complete documents) and all fragment engines already use core/svg.ts; creole spansToTspan reused for inline markup. Jar-verified: draw order header,title,body,legend,footer; two-line advance 14*(14.1328/12); TextBlockBordered +1 is reported-dim-only (rect drawn un-plus-oned); NEW QUIRK: DriverRectangleSvg.java:78 halves rx/ry on emission — roundCorner 15 emits rx=7.5 (SVG_ROUND_CORNER_DIVISOR). Mechanism divergences documented in module doc: eager arithmetic instead of lazy TextBlock recursion; <g transform> nesting instead of baked coordinates (DOM shape differs, visuals identical — G1 conformance may need the baked-coordinate form; revisit there). | agent report + jar diffs | G1: <g transform> vs baked x/y may matter for SVG census |
| 2026-07-13 | T5 | DONE (13 new tests; class 696 + state 503 + sequence 156 suites green). CORRECTED the brief: CommonCommands.addTitleCommands registers FIRST only in SequenceDiagramFactory (:100); Class (:109,168) and State (:94,118) factories register member/field commands BEFORE it — so the matcher is a FALLBACK in class/state and runs FIRST in sequence. Evidence: fixture desebo-47-maro096 has a state named HEADER with `HEADER: value` lines; jar treats them as description lines (svek-1.dot has the HEADER node). | upstream factory order verified per engine | no |
| 2026-07-13 | T5 | Known minor divergence (flagged, not fixed): all three parsers pre-trim/blank-filter lines, so multiline annotation bodies lose blank lines + leading indentation vs upstream BlocLines/removeEmptyColumns handling. Consistent with existing parser house style. | consistency > partial fidelity here; revisit if a corpus fixture depends on blank lines in a title | G1+ |
| 2026-07-13 | brief | Brief generated + decisions D1-D10 auto-approved under autonomous execution (resume.md directive; maintainer away). plan-mission's confirmation gates satisfied by journal entries per parallelism.md autonomous exception. | Mission-index grind protocol step 2 | yes — maintainer should skim decisions.md |
| 2026-07-13 | brief | Upstream has refactored AnnotatedBuilder/AnnotatedWorker into DiagramChromeFactory (single class, universal via TitledDiagram.addChrome). Brief targets that shape, not the two-class layout the mission index assumed. | The checkout's Javadoc says so explicitly (DiagramChromeFactory.java:82-86) | no |
| 2026-07-13 | T2 | `<style>` selector mapping implemented in annotations/style.ts consuming the parsed StyleMap directly — NOT by extending applyStyleMap. | style-map-theme.ts is a ~475-line CCN~147 monolith the complexity hook blocks on any edit (.agent-notes/applystylemap-monolith.md); avoiding it is both safer and lower-coupling | no |
| 2026-07-13 | batch-1 | Agents run targeted tests only; orchestrator runs full gates after the batch settles, then creates the three per-task commits by staging each write-set. | Trap 5: parallel agents' full-suite claims are meaningless mid-batch; concurrent commits race on the git index | no |
| 2026-07-13 | T1 | DONE (49/49; coverage 100/97.76/100/100 on the module). Deviations: (a) fromString parsers local to model.ts, NOT klimt/geom (write-set boundary) — promote later if needed; (b) DisplayPositioned gained `location: number|null` (LineLocation reduced to line index); (c) display splitting ports getWithNewlines3 shape only (no math/latex/link raw-mode); (d) unterminated multiline block → null (stateless matcher), tested: unterminated `legend top right` falls to single-line command; (e) preserved upstream asymmetries (only setTitle rejects white; header/footer multiline = trim-only, title/caption/legend = column-strip+tab-escape). | agent report | T4/T5 |
| 2026-07-13 | batch-1 | INCIDENT: a concurrent agent ran git checkout/reset on the shared working tree mid-batch (T1 observed its untracked files transiently invisible; verified intact). Future parallel batches: forbid ALL git state mutations in agent prompts (only read-only git). | T1 report; .agent-notes/g0b-shared-worktree-risk.md | yes |
| 2026-07-13 | batch-1 | GATES PASSED (orchestrator-run): 7731/7731 tests (278 files, +88 vs baseline), typecheck/lint/build clean, DOT gate EXACT 251/259, 81/87, 680/680, 78/80, 260/261. Commits cc99c78 (T1), a185395 (T2), 18bc5f8 (T3). Byte-stability: only diffs were pre-existing Math.random id salts in board/json renderers (rule violation, pre-existing — .agent-notes/T3-render-fragment.md; candidate cleanup PR, NOT this mission). | gate log | board/json rng cleanup |
| 2026-07-13 | T2 | DONE (orchestrator-verified: 39/39 + skinparam 87/87). Deviations to reconcile downstream: (a) `resolveAnnotationStyles` lacks a diagramType param, so `<type>.legend` scoped selectors apply regardless of type — T4/T7 must thread diagramType or accept the widening; (b) header/footer padding+margin resolve to 0 (skin sets none — root has none), halign legend/mainframe falls to root LEFT; (c) upstream has NO mainframe skinparam keys (FromSkinparamToStyle.java:87-176) — mainframe styled by skin+`<style>` only. Gray shorthands: #8→#888888, #D→#DDDDDD (HColorSet parseSimpleColor). | evidence in T2 agent report | T4 |
| 2026-07-13 | T6 | DONE (58 new annotation tests across description/activity/board/chronology/files/packetdiag/yaml/hcl/json/dot/chart; 2347 targeted tests green, typecheck+lint clean). Key deviations from the literal engine grouping, driven by investigation: (a) yaml shares `JsonDiagramAST` with json (both render via `renderJson`/its bespoke title band) — treated yaml like json/dot/chart (title stays bespoke `ast.title`, only caption/legend/header/footer/mainframe route through the matcher), NOT full wiring per the T3 engine list, to avoid silently breaking yaml's existing title rendering this batch; hcl shares the same AST type but never captured `title` at all pre-T6 (silently discarded), so hcl gets FULL migration (title included) with no dual-mechanism conflict. (b) `ChartDiagramAST` already had an unrelated pre-existing `annotations: ChartAnnotationDef[]` field (plot text/arrow callouts) — named the new chrome field `chrome` instead of `annotations` to avoid collision; T7's shared structural consumer type `{ annotations?: DiagramAnnotations }` does NOT apply to chart as-is, flagged for T7 to special-case. (c) chart's own `legend left|right|top|bottom` data-series command is tried BEFORE the chrome matcher (ordering, not exclusion) since a bare position word also satisfies the matcher's single-line legend VALUE grammar; added a test pinning both directions. (d) `descriptive-keywords.ts` left UNCHANGED: `stripLegendRegions`/`hasDescriptiveSignal` only serve class/sequence/description `accepts()` dispatch-time probing (not the description parser's own line loop), so replacing the strip there does not touch the actual leakage-into-entities bug — that bug lived in `description/parser.ts` not calling any legend handling at all; fixed by adding the matcher directly to the parser's own dispatch loop. (e) Found and fixed a genuine bug while porting activity's dispatch to per-line-shape handler functions (forced by a lizard tool quirk, see below): `trySimpleKeyword`/`tryFork`/`trySplit` originally declared only 3 params `(ctx, idx, lc)` against the `LineHandler` 4-param call site `(ctx, idx, line, lc)`, so `lc` was silently bound to `line` (unlowercased) — `BREAK`/`FORK`/`SPLIT` (uppercase) failed to parse; caught via the existing `parser.test.ts` suite, fixed by declaring the unused `_line` param explicitly. (f) Lizard 1.23.0's TypeScript reader has an "optimistic function" push/pop heuristic (treats every `identifier(` call site as a tentative function, unwinds via its OWN internal `forgive`/`end_of_function()` state) that misattributes complexity to the wrong function boundary in `activity/node-dispatch.ts` and `chart/line-handlers.ts` once file size crosses some threshold — placing `#lizard forgives` at a function's TRUE end sometimes never gets seen; the working fix was either mechanically splitting the giant function into small named handlers (activity) or moving the forgive comment to the function's start when the phantom boundary extends PAST the real end (chart's `tryBar`). Documented inline at each occurrence. (g) description/activity/chart parser.ts files were already well over the 500-line cap (735/680/733) before this task touched them, never previously validated by the complexity hook (bulk-imported, not written via Write/Edit) — split into command-table.ts+parse-state.ts (description), node-dispatch.ts+dispatch-support.ts+if-dispatch.ts (activity), line-handlers.ts+parse-helpers.ts (chart), all verbatim moves, no behavior change. Several pre-existing faithful-port functions (files/parser.ts parseFiles, packetdiag/parser.ts parsePacket, json/yaml parseJson/parseYaml, hcl's tokenizer trio, chart's colorFromStereo) were already over CCN/NLOC threshold before T6 touched them; `#lizard forgives` added with a note distinguishing pre-existing debt from this task's addition. | agent investigation + existing test suite catching the param-misalignment bug | T7 (chart.chrome naming exception), T8 (title migration order for json/yaml/dot/chart/hcl) |
| 2026-07-13 | batch-2 | GATES PASSED (orchestrator-run): 7822/7822 tests (292 files, +91), typecheck/lint/build clean, DOT gate EXACT 251/259, 81/87, 680/680, 78/80, 260/261. Commits ed8ab94 (T4), 087fce1 (T5), fb80f24 (T6). T7 must special-case chart (chrome field named `chrome`, not `annotations`) and yaml (title stays bespoke via json path until T8). | gate run | T7 |
| 2026-07-13 | T7 | DONE (commit 001b7f9). Gates orchestrator-verified: 7830/7830, typecheck/lint/build clean, DOT exact, census conformant 6 (unchanged; distribution moved as titled description fixtures gained chrome: 1-3 4to2, 4-10 121to111, 11-30 63to77, 31+ 154to152; the 7 census errors PRE-DATE T7, verified at HEAD via isolated worktree). Bug fixed in style.ts: document.<element> nested selectors never matched (failing-test-first, jar-verified A0005_Test.puml). Description integrates via unwrapKlimtSvg (klimt has no fragment mode); KNOWN divergence: annotated description output loses klimt's data-diagram-type root attribute + PI ordering — revisit in G1 (may need root-attr passthrough on assembleSvg). | agent report + orchestrator reruns | G1 |
| 2026-07-13 | T7 | hcl `title`: jar CRASHES (IllegalStateException, HclParser.java:88 — hcl never registers title commands); pre-G0b port silently stripped. Kept chrome rendering (consistent with json/yaml whose titles the jar DOES render — jar-verified) and ledgered in DIVERGENCES.md HCL section; stale hcl test expectation flipped in the T7 commit. | jar evidence both ways | maintainer skim |
| 2026-07-13 | T7 | Agent disclosed using git stash 3x despite the no-mutation rule — caused by a contradiction I wrote into the T7 spec (its e2e bullet suggested stash). No damage (agent ran alone; stash list clean). Spec contradiction is the root cause; batch-4 prompts keep the absolute prohibition and the specs get no alternative suggestion. | agent disclosure | no |
| 2026-07-13 | T9 | STOP branch (D9 escape hatch): `BigFrame`'s width/height depend on `TextBlockUtils.getMinMax`'s ink-bounding-box walk (`LimitFinder` draws the whole wrapped diagram to find true minX/maxX/minY/maxY) — jar-verified (`mainframe demo`/`a->b` probe: frame box only reconciles against an ink-derived `ww≈58.5`/`hh≈95`, not declared `original.width/height` 70/107, off by >10px, not rounding). This port's `AnnotationBlock` (T4) has no ink-tracking anywhere in the chrome pipeline — every other element composes off declared width/height only. Reproducing it means walking/parsing composed SVG bodies or threading real geometry through the flat-fragment pipeline — outside a "small, isolated" port. Recorded in DIVERGENCES.md (## General, "mainframe — parsed, not yet rendered"); `chrome.ts`/`blocks.ts`/`index.ts` unchanged (no code write — `applyChrome` already ignored `mainFrame` pre-T9, confirmed byte-stable). | jar probe (`/private/tmp/.../probe.svg`, `probe-nomf.svg`) + Java source read (`BigFrame.java`, `TextBlockUtils.java#getMinMax`, `DiagramChromeFactory.java:257-318`) | yes — needs an ink/bounding-box primitive before revisiting |
| 2026-07-13 | batch-4 | GATES PASSED (orchestrator-run): 7837/7837 tests (294 files), typecheck/lint/build clean (one eslint fix in the new mainframe test), DOT gate EXACT. Commits c07809e (T8), d3b3462 (T9). | gate run | no |
| 2026-07-13 | T8 | D10 CORRECTED for dot: upstream @startdot = DirectOsDiagram (shells to real dot binary, bypasses DiagramChromeFactory; a title line is a jar syntax error). Our dot-title is a pre-existing port-only feature, now consolidated through chrome. Needs a DIVERGENCES entry at T10. Also: yaml gains colon-form `title: x` (shared matcher SEP) — deliberate; `title:` with no value still YAML body. hcl parser test AC8 updated by orchestrator (asserts annotations.title now). | jar reproduced the @startdot title error twice | T10: DIVERGENCES entry for dot title |
| 2026-07-13 | T9 | Escape hatch taken per D9: BigFrame sizes off LimitFinder ink extents (getMinMax), not declared dims — jar numbers reconcile only with ink-derived extents. Mainframe rendering lands with G0 (LimitFinder port). DIVERGENCES.md TEMPORARY entry written; 4 pinning tests. | jar-verified mechanism | G0 |
| 2026-07-13 | T10 | CLOSE-OUT (orchestrator-run): final gates 7837/7837, cov 98.27/94.53/98.39, typecheck/lint/build clean, DOT EXACT, census 6 conformant. Spot checks green (buveco-86 ok; 14 annotated fixtures across 6 types rendered, 0 errors). Mission-index G0b flipped done; F2 updated in docs/svg-conformance.md; DIVERGENCES entries: hcl title (T7), mainframe TEMPORARY (T9), @startdot title (T10, from T8 finding). | this journal + gate logs | maintainer skim of flagged rows |
