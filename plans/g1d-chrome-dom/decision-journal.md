# G1d decision journal

| When | Iter | Decision | Why | Flag |
|---|---|---|---|---|
| 2026-07-15 | M1 | Ratchet-annotation pre-check: none of the 48 ratchet fixtures carry title/caption/legend/header/footer directives (verified by grepping all 48 `in.puml` files; one false-positive substring match in a component label, not a directive). Proceeded without stopping. | Mission brief's explicit stop condition did not trigger. | none |
| 2026-07-15 | M1 | Rejected "wrap in `<g transform>` but nest it inside the new outer `<g>`" as the design (would only close `svg[childCount]`, not the deeper `g/@transform` / structural-sibling shape jar's own cached SVGs show — verified zero `transform=` occurrences across all 19 fixtures, so entities/links are direct, transform-free siblings, not a nested wrapped group). Chose coordinate-baking (`shiftFragmentBody`) instead. | Jar's actual cached SVGs (not just the mission's prose) are the ground truth; a partial fix would leave `svg/g[childCount]` (one level deeper) open and fail the "g/@transform diffs should close" verification bar. | none |
| 2026-07-15 | M1 | Built `shiftFragmentBody` as a scoped, project-vocabulary-aware coordinate shifter (not a general SVG transform library) rather than threading an offset parameter through every diagram engine's renderer. | Full offset-threading through 10+ engines' renderers is out of a single iteration's reach; this codebase's SVG-emitting vocabulary is fully self-authored and grep-verified (only M/L/C/Q/A/Z absolute path commands, only `translate`/`rotate` transform functions), making a scoped shifter both tractable and low-risk. The mission's "thread offset into fragment builders" guidance IS followed for `buildAnnotationBlock` (chrome.ts's own builder, margin baked directly rather than wrapped) — the guidance's "not from the outside" caveat is satisfied since all shifting happens inside chrome.ts's own composition step, mirroring `DecorateEntityImage#drawU`'s architectural position exactly. | none |
| 2026-07-15 | M1 | Discovered and fixed a double-shift correctness bug during development: `buildAnnotationBlock`'s own `<g transform="translate(margin.left,margin.top)">` wrapper caused `shiftFragmentBody` to shift nested `<text>` coordinates TWICE (once via the wrapper's own composed translate, once again naively re-matching the nested text's local x/y). Fixed by eliminating the nested wrapper at the source (bake margin directly in `blocks.ts`) rather than teaching the shifter to understand DOM nesting depth. | A flat regex-based shifter cannot safely distinguish "top-level coordinate" from "coordinate nested inside an already-transformed ancestor" — the only safe fix is ensuring no nested transforms exist in chrome-composed content at all. | none |
| 2026-07-15 | M1 | `applyChrome` tracks a `decorated` boolean and returns `fragment` unchanged (no new outer `<g>`) when nothing was actually drawn (mainframe-only annotations, D9 escape hatch). | `tests/unit/annotations-mainframe.test.ts` pins byte-identical output for a mainframe-only bag; an unconditional outer-`<g>` wrap would have silently broken this D5-adjacent invariant — caught by running the full suite before declaring done. | none |
| 2026-07-15 | M1 | Named a NEW residual family ("chrome-block text styling gap": `font-family="SansSerif"` vs `sans-serif`, `fill="black"` vs hex, `<tspan>` wrapper, missing `lengthAdjust`/`textLength`) in the ledger rather than fixing it. | Out of G1d's declared scope (DOM shape, not text-attribute fidelity) — fixing it would expand this iteration beyond "chrome.ts + fragment plumbing" into `blocks.ts`'s text-rendering pipeline, a separate mechanism this mission's read-set didn't cover. Flagged for a future iteration (likely folds into G1's I2). | needs-signoff |
