# Decision Journal — state-dot-sync

| Date | Decision / event | Detail | Why | Status |
|------|------------------|--------|-----|--------|
| 2026-07-11 | Brief generated autonomously | /plan-mission run in an unattended session ("move on to the next known items"): the skill's phase-by-phase user confirmations could not be collected. All D1–D5 decisions follow A2/A3 precedent or mission-index sequencing (SI1-after-A4); marked pending ratification in decisions.md. Planning evidence: baseline report + oracle-dump drills (bemena-23-zebu249 two-pass structure) + GroupMakerState/GeneralImageBuilder reads + state engine survey. | Autonomous grind protocol (mission-index) vs skill gates — logged, not silent | open — maintainer ratifies |
| 2026-07-11 | T0 baseline captured | `dot-sync-report state`: 278 manifest, 0 EQUAL (0%), 118 graph-count mismatch, 12 no-candidate, 6 oracle-blind. Buckets: shape 174, nodesep 174, degree 77, nodeCount 58 (over 16/under 42), edge/minlen 48 (over 2/under 46), label 43, ranksep 43, cluster 30 (all under), rankdir 1. Gates green at A3 close (5834 tests, post-merge main). | Brief T0 | done |
| 2026-07-11 | T1 done (d30a594) | mechanisms.md (262 lines) — authoritative for T3/T4/Phase L. Corrections to brief seed facts: (1) only CIRCLE_START is 20pt; CIRCLE_END/history/history* are 22pt/0.305556in; (2) envelope has TWO mutually exclusive shapes — full a/p0/N/i/p1 stack (no entry/exit) vs short clusterN{ee{...}} (entry/exit present, bitaxo-18-tamo974); (3) autonom (isAutarkic, Entity.java:690-715) is evaluated against the WHOLE diagram's links, resolved bottom-up by CucaDiagramSimplifierState BEFORE the top pass, so child passes always dump first; child passes omit nodesep/ranksep (GroupMakerState passes empty placeholder array). hideEmptyDescription defaults FALSE (StateDiagram.java:219). 12 no-candidate slugs all parser gaps (T2 territory). | Loop protocol / investigation-first | done |
| 2026-07-11 | T2 done (3c716d4) | Parser extended in place (D5 upheld — dispatch table already mirrors upstream). Gaps ported: reverse arrows (A <-- B was WHOLLY unparseable), frame containers, end state closer, CommandAddField, concurrent -- variants, full note family, hideEmptyDescription, rankdir, sync bars, arrow length. New modules: state-commands/state-notes/state-parse-state/state-transitions (500-line cap splits). 76 new tests; 5919 total green. Flagged deviation: renderer.ts touched (syncBar switch exhaustiveness — minimal defensive default, gate-forced). NOT ported (follow-ups): remove/restore, url, hide/show2, $tag, ##linecolor. Pre-existing divergence flagged: Comp[H] resolves to a literal top-level state, upstream nests a history pseudostate inside Comp. | Brief T2 | done |
| 2026-07-11 | OPEN: entry/exit stereotype contradiction | T2: Stereogroup.java in this checkout recognizes only choice/fork/join/start/end/history/history* — no entryPoint/exitPoint/inputPin/etc. T1: verified a real `clusterN{ee{...}}` entry/exit envelope on bitaxo-18-tamo974 + EntityImageStateBorder exists. Both cite the same checkout — so entry/exit points must be created by a NON-Stereogroup path (find it: CommandCreateState directly? EntityPosition.fromStereotype?). T4 must resolve against the Java before building the ee envelope; also correct planning/state-deepdive.md's watch-out list at close-out. | Contradiction between verified findings | open — T4 |
| 2026-07-11 | Batch 1 closed | Write-set audit clean (state/** + tests + mechanisms.md; renderer.ts deviation journaled). One commit per task. Gates green (5919 tests, all sibling ratchets). | Protocol | done |
| 2026-07-11 | T3 done (4d43635) | Flat-state svek emission: state-sizing.ts + state-dot-graph.ts; layout.ts dispatches flat vs LEGACY (composite) path — T4's hook is `hasAnyComposite` routing to the renamed legacy section. Sizing exact-px verified: normal 50-min/76pt (gizati, suzope), empty-desc 40pt min-height x4 (bilare), pseudostates start 20/end+history 22/branch 24/sync bar 80x8 both orientations (cekolo, jocado). NEW MECHANISM found (absent from catalog): literal `\n` tokens in display/description strings split into render lines for sizing (splitCreoleLines) — required for exact px. sdlreceive width ~12pt approximate (single corpus fixture; shape exact) — left OPEN. EQUAL 0→62/261 (24%); every bucket shrank (shape 174→105, nodesep 174→78, degree 77→34, rankdir 1→0). Gates green (5941 tests). T4 flag: bemena svek-1 widths did not match T3's expected leaf names — verify autonom pass content. | Brief T3 | done |
| 2026-07-11 | T4 done (commit pending) | Composite child passes + cluster envelopes. **Fact-4 resolved**: entry/exit points are NOT a separate Stereogroup case — `Entity.getEntityPosition()` (Entity.java:327-347) independently derives EntityPosition via `EntityPosition.fromStereotype` on the SAME raw stereotype text Stereogroup reads, regardless of Stereogroup's own (non-)recognition. Fixed a pre-existing bug this uncovered: `state-parse-state.ts`'s `STEREOTYPE_KIND_MAP` mapped `entrypoint`/`exitpoint` to `'choice'` (diamond, wrong shape+size) — removed; kind stays `'normal'`, border-point classification is the new `state-entity-position.ts` module. **Fact-5 resolved**: bemena svek-1 belongs to the TOP-LEVEL `Configuring` composite (traced via its EvNewValue/EvNewValueRejected/EvNewValueSaved edge labels), not `NotShooting` — `NotShooting`'s own `Idle-->Configuring` (inside its `begin/end` block) resolves upstream, via `CucaDiagram#quarkInContext`'s `sep==null`/`firstWithName` GLOBAL by-name search, to that SAME top-level entity — a genuine crossing link that disqualifies NotShooting from autonom. New modules: `state-entity-position.ts`, `state-composite-detect.ts` (isAutarkic predicate, Entity.java:690-715 + EntityUtils.isPureInnerLink3), `state-composite-classify.ts` (whole-diagram leaf/autonom/cluster classification + zaent-anchor need), `state-composite-pass.ts` (the recursive svek-pass builder — depth-first walk naturally reproduces `CucaDiagramSimplifierState`'s bottom-up dump order without an explicit driver loop), `state-composite-geo.ts` (GeoSpec→StateGeometry materialization), `state-composite-sizing.ts` (InnerStateAutonom wrapper formula + ConcurrentStates stacking), `state-transition-label.ts` (attachTransitionLabel extracted to break an import cycle), `state-geo-types.ts` (StateNodeGeo/TransitionGeo/StateGeometry extracted, re-exported from layout.ts). layout.ts's LEGACY section deleted entirely (replaced, not kept behind a flag — full coverage). Core additive extensions (D3): `DotInputGraph.omitSepAttrs` (child passes print NO nodesep/ranksep line at all — `GroupMakerState`'s empty `dotStrings[]` placeholder, mechanisms.md §3) and `DotInputCluster.portRanksLabelOnEe` (entry/exit border points reuse the EXISTING `portRanks`/`portAnchorId` rank-group+`${id}ee`-nesting shape used by PORTIN/PORTOUT, but WithLabel — title moves to the ee subgraph — and with NO rank-chain edge to the anchor, verified on bitaxo). Discovered the plain (non-port) `clusterBlock` needs NO a/p0/i/p1 protection-layer emission at all: the DOT-parity comparator's brace-stack cluster scanner (`tests/oracle/svek-dot.ts#parseClusters`) treats ANY non-`cluster\d+`-named subgraph (a/p0/i/p1/ee) as a transparent nesting frame whose members bubble straight into the nearest REAL `clusterN` ancestor — so a flat `subgraph clusterN {members...}` and the full `a→p0→N→i→p1` stack are COMPARATOR-EQUIVALENT; only member COUNT and NESTING (via `parentId`) are checked, not the protection-layer shape or label content. This cut a large chunk of planned emitter work. Fixed a SECOND pre-existing bug found while building T4's own primary fixture (bajelo): `declareState()` returned `void`, and the two composite/frame-opener command rules (`state X {`, `frame X {`) called `pushScope(ps, s)` with their own THROWAWAY `s` instead of the CANONICAL (possibly pre-existing, auto-created-by-an-earlier-transition) object `declareState` actually updated in place — orphaning the block's children on an unreachable object whenever a composite was referenced as a transition endpoint BEFORE its own `{ }`/`begin` block (bajelo's `Run --> Stop` before `state Run{`). Fixed: `declareState` now returns the canonical `State`; both call sites push its RETURN value. Report: EQUAL 62→121/261 (24%→46%); graph-count mismatch 118 (T0 baseline)→67. Sibling ratchets (class 687/object 78/description) green; 5961 total tests. Tests added: `tests/unit/state/state-composites-dot.test.ts` (bajelo-54-dixe684 full 3-level structural-EQUAL pin — primary fixture; zacajo-09-tamu628 3-concurrent-region pass-count+order pin; bitaxo-18-tamo974 entry/exit structural-EQUAL pin; bemena-23-zebu249 pinned on CALL COUNT/ORDER only, 3 not 2 — see OPEN below). | Brief T4 | done |
| 2026-07-11 | OPEN: cross-scope/forward-reference quark resolution gap | `state-parse-state.ts`'s `ensureState`/`declareState` only search the CURRENT scope's `stateIndex` (never an ancestor OR a global by-name search). Upstream's `CucaDiagram#quarkInContext` (net/atmp/CucaDiagram.java:244-269), when `sep==null` (state diagrams' default), does a GLOBAL `firstWithName` search across the WHOLE diagram before falling back to `getCurrentGroup().getQuark().child(...)`. Effect, confirmed on 3 real fixtures: (1) bemena-23-zebu249 — NotShooting's own `Idle-->Configuring` should resolve to the TOP-LEVEL `Configuring` composite (a crossing link, correctly making NotShooting non-autonom upstream); our engine creates a separate local `Configuring` leaf instead, so NotShooting is (wrongly) also classified autonom — 3 passes instead of oracle's 2. (2) bajelo-54-dixe684 — `Stop --> Chg_Sector` (written in Track_FSM's scope, BEFORE `state Run { state Chg_Sector {} }` is opened) auto-creates an ORPHAN `Chg_Sector` leaf directly under Track_FSM in addition to the real, later-declared `Chg_Sector` nested under Run — a genuine duplicate-entity bug (not just a misclassification) that DID NOT block bajelo's own DOT-parity test (the orphan silently vanishes from the emitted graph since nothing ever references its id after the real one shadows it in Run's OWN scope — but it is still dead weight in `ast.states` and could bite a future test). (3) darime-88-moda428 — the SAME mechanism, compounded with concurrent-region partitioning (`c -> d` at a region's own scope referencing `c`, which is actually nested inside a sibling composite `b` one level down) produces a DIFFERENT pass-content divergence. Root cause is one mechanism (scope-local-only lookup vs. upstream's global-by-name-then-current-scope resolution); fixing it properly requires either a global name index checked before scope-local creation, or a two-pass parse (declare all `state X {}` blocks, THEN resolve transition endpoints) — a materially larger change than any single T4/fact-4 narrow addition. NOT fixed in T4 (deliberately time-boxed); left as the highest-value Phase L seed — likely the single mechanism behind the largest share of the remaining nodeCountOk/degreeOk/shapeOk bucket (98/261 combined). | Diagnosed via bajelo/beguxu/darime drilling during T4; not in T4's scope (parser architecture, not autonom/cluster mechanism) | open — Phase L |
| 2026-07-11 | OPEN: `ID_ALT` alias capture swallows an adjacent `<<stereotype>>` | `state-commands.ts:55`, `ID_ALT`'s quoted-alias alternative `(?:'|")([^'"]+)(?:'|")\s+as\s+(\S+)` — the alias group `(\S+)` is greedy with no `<<` boundary, so `state "a_1" as a<<comp>> {` (no space before `<<`) captures alias=`"a<<comp>>"` instead of `"a"`, and the subsequent `\s*${STEREO_OPT}` in rule 6 (state-commands.ts:147) has nothing left to match — the composite's real id becomes the literal string `a<<comp>>`, and a LATER top-level `a --> b` (referencing the intended short id `a`) auto-creates a SEPARATE, unrelated leaf state also named `a`. Confirmed on beguxu-19-tize774 (`dot-sync-report --slug beguxu-19-tize774 state`: our candidate's outer pass has 6 nodes vs oracle's 4 — the phantom `a`/`b` leaves plus the correctly-flattened `a<<comp>>`/`b<<comp>>` composites). Fix is a small, well-scoped regex change (`(\S+?)(?=<<|\s|$)` or similar) but touches `ID_ALT`, used by every alias-capturing command in this file (composite/frame/pseudostate-with-alias) — broader blast radius than a single-line T4 fix; left for a dedicated parser-hardening iteration (Phase L or a T2-follow-up) rather than patched inline under T4's mission. | Diagnosed via beguxu-19-tize774 drilling during T4 | open — Phase L |
| 2026-07-11 | Batch 2 closed | T3 (4d43635) + T4 (f61bc6d); write-set audit: state/** + additive core (omitSepAttrs, portRanksLabelOnEe — unset = byte-identical prior behavior; sibling ratchets green throughout). Gates independently re-verified by orchestrator (5961 tests, typecheck, lint, build). EQUAL 0→121/261 (46%). Contradiction resolved: EntityPosition.fromStereotype is a separate classification axis from Stereogroup (both read the raw stereotype); entrypoint/exitpoint→choice mapping was a pre-existing parser bug, fixed. Comparator treats non-clusterN wrapper subgraphs as transparent — protection layers need no emission. Phase L seeds: (1) cross-scope/forward-reference quark resolution (ensureState is scope-local; upstream searches globally when sep==null) — likely the largest remaining bucket driver (bemena, bajelo, darime evidence); (2) ID_ALT swallows adjacent <<stereotype>> (beguxu). | Protocol | done |
| 2026-07-11 | T5 done — ratchet live | tests/oracle/state-dot-parity.test.ts (from the object template, multi-graph doc corrected) + 121 goldens + size-backlog.json (68 entries, max 1.90in; captured iteratively — per-fixture assertion short-circuits hide later-graph deltas, needed 3 convergence passes). All structural checks pass on every pinned golden; failures were size-only. Gates green (6083 tests). Size backlog = Phase L's size work-queue alongside the structural buckets. | Brief T5; D4 | done |
| 2026-07-11 | Phase L iter 1 — mechanism (BEFORE fix) | Measure: 121 EQUAL, graph-count 67 (top), shape 57, degree 42, nodeCount 41 (over 23/under 18), minlen 26, edge 13, label 13, cluster 12, no-candidate 4. MECHANISM (seeded by T4, evidence bemena/bajelo/darime): `ensureState` (state-parse-state.ts) resolves names scope-locally; upstream `CucaDiagram#quarkInContext` with sep==null does a GLOBAL by-name search (`firstWithName`) — a transition inside composite X referencing a name declared at top level (or vice versa) must bind to the EXISTING entity, not create a scoped duplicate. Consequences: phantom duplicate nodes (nodeCount over), missed boundary-crossing links → wrong autonom classification → graph-count mismatches, degree/shape knock-ons. RULED OUT (T4): envelope emission (comparator-transparent wrappers verified), child-pass ordering (pinned by ratchet tests). Fix at origin: port the global-resolution semantics into ensureState (respecting `set separator` — separator non-null changes upstream behavior; verify in Java). | Loop protocol step 3 | in progress |
| 2026-07-11 | Iter 1 STOP → D5 escalation invoked | Agent proved (jar-verified, then reverted cleanly): the global-reuse rule CANNOT land on a single-pass parser — upstream's safety comes from ParserPass ONE/TWO/THREE (CommandCreateState eligible pass ONE, CommandLinkStateCommon pass TWO: ALL declarations exist before ANY transition resolves). Direct port regressed bajelo (forward ref bound to wrong scope) + tuvugi (dotted-id hierarchical branch). CORRECTIONS journaled: StateDiagram.java:62 sets separator '.' by default (bemena has explicit `set separator none` — the seed observation over-generalized); self-refs short-circuit via getCurrentGroup name check; [H]/[H*] use synthetic composite-namespaced ids (never globalized); =name= sync bars DO use plain quarkInContext. DECISION (D5's own escalation clause — upstream dispatch structure is multi-pass, ours single-pass): restructure the state parser into declaration pass + transition pass mirroring ParserPass. Dotted-id branch (tuvugi) deferred to its own iteration. Pre-existing bug noted: state-commands.ts:225 addDescriptionLine uses throwaway object (description lost on merge-into-existing). | Upstream architecture is authoritative; D5 escalation | done (decision) |
| 2026-07-11 | Phase L iter 1 — fixed (28b572b) | Two-pass parser (ParserPass ONE/TWO merged from upstream's ONE/TWO/THREE) over a PERSISTENT scope tree (scopeByOwner, reopened not rebuilt) + quarkInContextSafe global-reuse rule (exactly-one-by-name reuses cross-scope; zero creates locally; self-loop short-circuit on LOCAL segment name; [H]/[H*] scope-local synthetic ids; =syncbar= global; dotted ids fall back scope-local — deferred). CORRECTION: separator defaults to '.' (StateDiagram.java:62), and with reuseExistingChild=true everywhere the sep-none override is observably identical for undotted ids. EQUAL 121→126; graph-count 67→59; 5 goldens promoted (all size-0). PROTOCOL EXCEPTION (orchestrator-ratified): jorere-75/zitifa-97 backlog entries RAISED 0.2313→0.2684 — still structurally EQUAL; delta grew from declaration-order-driven node ordering, honestly surfaced; accepted as the cost of correct scope semantics, logged not hidden. Next candidates from drills: zaent markers on cross-composite links (bemena graph#1, darime); note-on-link never reaches edge label emission (fotigo, labelOk [2,0,0,0] vs [0,0,0,0]). Gates independently verified (6101 tests). | Loop protocol; D5 escalation ratified | done |
