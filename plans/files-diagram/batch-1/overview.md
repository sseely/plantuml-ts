# Batch 1 — AST Type Definitions

One small task. All other batches import from this file.

| ID | Description | Agent | Writes | Depends On | Done |
|----|-------------|-------|--------|------------|------|
| T1 | ast.ts — all type definitions | typescript-pro | `src/diagrams/files/ast.ts` | — | [ ] |

Run quality gates after T1. Both `npm run typecheck` and `npm run lint` must pass.
