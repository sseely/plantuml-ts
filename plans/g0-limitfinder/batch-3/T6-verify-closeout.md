# T6 — Verify + flip (ORCHESTRATOR — do not delegate the gate runs)

1. Full gates at branch head (test counts + coverage, typecheck, lint,
   build).
2. DOT gate at the post-T2 baseline — assert numerators/denominators match
   the journal's pinned numbers exactly.
3. Census before/after table (expect conformant rise from 6; the F4
   size-clean tier is the mission's SVG payoff — record which fixtures
   became conformant).
4. Exit-bar checks: the 190×65 fixture emits 190×65; vapalu/jesibe/cifaki
   dims equal the jar's; oracle-blind = elk-only counts; mainframe branch
   outcome consistent (DIVERGENCES ↔ tests ↔ code).
5. Docs: docs/svg-conformance.md F4 marked fixed (with residuals if any);
   DIVERGENCES.md § smetana "Testing consequence" updated to DONE;
   mission-index G0 flipped with evidence (incl. the NEW pinned DOT
   baseline — this becomes every future mission's gate numbers);
   plans README summary + checkboxes; journal close row.
6. Merge commit to main, push, verify main gates green post-merge.
7. Memory: save any durable orchestration lessons; update the resume-style
   baseline numbers wherever the project records them (mission-index
   snapshot section).
