# G0 decision journal

| When | Task | Decision | Why | Flag for review? |
|---|---|---|---|---|
| 2026-07-13 | brief | Brief generated + D1-D7 auto-approved under autonomous execution (maintainer typed "start G0"). Research CORRECTED F4's framing: our `ensureVisible` rounding (trunc+1) is already faithful to upstream's `(int)(x+1)` — the defect is `computeTotalDimensions`' node-box hand-scan (LAYOUT_MARGIN=12) vs upstream SvekResult's LimitFinder ink walk + (6,6) re-anchor + delta(15,15). Also: MinMax has NO delta() — delta lives on XDimension2D; the F4 doc's `getMinMax().delta(15,15)` shorthand conflated them. | mission research, jar/source-verified | maintainer skim of decisions.md |
| 2026-07-13 | T2 | DONE (orchestrator commit pending batch gates). **NEW PINNED DOT BASELINE: component 253/262 · usecase 84/90 · class 708/708 · object 78/80 · state 266/267; oracle-blind class 7, component 1, object 1, state 0, usecase 0 (elk-only).** 41/42 newcomers EQUAL; 1 structural-diff (component tojitu-03-ruto643: edgeCount/degree/minlen/cluster — queued for the description-DOT-100% mission, NOT drilled here per D4). 2 component slugs pre-excluded (kofovu-01: jar errors UnsupportedOperationException; potatu-55: jar type-routes it as CLASS). Usecase EQUAL +2 residue (sidame-35, zoriso-46) = stale 7-12 parity-report vs SI5a-era merges, verified pragma-free; zero fixtures dropped out of EQUAL anywhere (comm -23 empty on all five lists). Trap 3 attribution exact per type. | T2 report | no |
| 2026-07-13 | brief | Mainframe/BigFrame scoped as an ATTEMPT (D5): LimitFinder unblocks the geometry, but chrome operates on fragment strings LimitFinder cannot walk — branch (a) description-only implementation or (b) re-defer with updated rationale. No SVG-string extent walker this mission. | T9's G0b analysis + G0 research §6 | no |
| 2026-07-13 | batch-1 | GATES PASSED (orchestrator-run): 7886/7886 tests (295 files, +49), typecheck/lint/build clean, DOT gate at the NEW baseline exactly (253/262, 84/90, 708/708, 78/80, 266/267; blind 1/0/7/1/0). Commits: T1 + T2. T1 boundary exception accepted: the test pinning the old throwing stub now pins real behavior. | gate run | no |
| 2026-07-13 | T3 | DONE. Anchor case (b) + TWO extra mechanisms found: (1) CucaDiagram getDefaultMargins (0,5,5,0) backwards-compat margin was the actual 1px; (2) degenerate single-leaf diagrams bypass SvekResult upstream (EntityImageDegenerated delta(14,14)) — our degenerateSingleLeaf already correct, now explicitly branched. LimitFinder ellipse path has no -1 inset → literal (6,6) re-anchor illusory; NO translate applied. Census 6→12. LESSON: the a/b/c menu was under-specified — widen dims measurements past F4 to the ratchet set BEFORE concluding a case. | T3 report | no |
| 2026-07-13 | batch-2 | GATES PASSED (orchestrator-run): 7929/7929 (296 files), clean, DOT EXACT at new baseline, census 12 conformant (7 pre-existing errors). Commits: T3 + T4. | gate run | no |
| 2026-07-13 | T5 | BRANCH (b) — re-defer, updated rationale. Traced the description-side wiring per T5's own instructions before choosing: `renderDescription(geo, theme, measurer)` is the only entry point with a real `draw` callback; `geo` (`DescriptionGeometry`) carries no annotation data, and threading `ast.annotations.mainFrame` onto it (mirroring the existing `ast.seed -> geo.seed` precedent) needs `src/diagrams/description/layout.ts` — outside every branch-(a) write-set this task authorized. Independently, `styles.mainframe` (padding/margin/lineColor/lineThickness honoring skinparam/`<style>` the way title/legend/etc already do) is resolvable only in `src/index.ts`, AFTER `plugin.render()` already ran — reaching it from inside the klimt pass needs either a `SyncPlugin` contract change (`src/core/dispatcher.ts`, rippling to every engine) or a second, pre-render, description-only special case in `src/index.ts` mutating `geo` via an unsafe cast — the exact "second chrome pipeline" shape D5 scoped T5 to avoid. BigFrame's geometry formula itself was independently re-derived against `BigFrame.java` + `DiagramChromeFactory.java:257-318` and confirmed fully portable off `TextBlockUtils.getMinMax`'s raw `MinMax` (T1/T3 already ported the primitive) — only the annotation/style plumbing is missing, and it spans files outside this task's authorization. Zero code change; DIVERGENCES.md mainframe entry rewritten with the updated, narrowed rationale. | T5 trace + jar probe (unchanged from T9) | no |
