# SI5a — decision journal

| Date | Event | Decision / finding | Rationale | Status |
|---|---|---|---|---|
| 2026-07-12 | Mission opened | Rewrite `preprocessor.ts` as `TContext` + `CodeIterator` chain rather than bolting directives onto the flat line-loop | The flat loop structurally cannot express nested `!foreach`/`!while`/`!if`; upstream uses a pull-based iterator decorator chain over an execution-context stack. CLAUDE.md sanctions re-mirroring upstream structure. | locked |
| 2026-07-12 | Mission opened | Port all 76 builtins, not a corpus-driven subset | Long tail is the deliverable; an enumerated upstream list is not ambiguous scope. | locked |
| 2026-07-12 | Mission opened | Sync TIM + `IncludeStore`; async prefetch stays in `render()` | `renderSync` is public API + the browser story. Accepted divergence: over-fetch of includes inside false conditional branches. | locked |
| 2026-07-12 | Batch 2a | `StringLocated`/`LineLocation`/`TLineType` minimal shim ported to `src/core/tim/StringLocated.ts` (~130 LOC): full `TLineType` member-name union (no classifier logic), opaque `LineLocation`, and a `StringLocated` class carrying only `getString`/`getLocation`/`length`/`charAt`/`getType` — the members this batch's write-set calls. `getType()` is caller-supplied at construction rather than lazily regex-classified (the real `TLineType.getFromLineInternal` classifier is out of scope); every in-scope construction site either never reads `getType()` or already knows the classification by construction (`Eater#eatDeclareReturnFunctionWithOptionalReturn`'s synthesized `"!return "` line). | Stayed under the ~150 LOC scope-guard cap; a full classifier belongs to the iterator/TContext batch that actually needs to classify arbitrary source lines. | locked |
| 2026-07-12 | Batch 2a | `ExecutionContexts` (91-line upstream base class) ported inside `TMemory.ts`, not its own file; `ExecutionContextIf` is a narrow structural stand-in (`conditionIsOkHere()` only); `ExecutionContextWhile`/`ExecutionContextForeach` are fully opaque (`unknown`) | Both `TMemoryGlobal`/`TMemoryLocal` `extends ExecutionContexts` in upstream — a required transitive dependency of two write-set files not itself named in the write-set. `ExecutionContextWhile`/`Foreach` additionally depend on `tim.iterator.CodePosition` and `Eater` subclasses, both explicitly out of scope. Housing the base class inside the already-in-write-set `TMemory.ts` (rather than a new file) avoids expanding the write-set while staying faithful. | logged, not pre-approved — flagging for review |
| 2026-07-12 | Batch 2a | **Investigated stand-in mismatch**: widening `tim/expression/Knowledge.ts`'s `StringLocated`/`TContext`/`TFunction`/`TMemory` to the real, wider `tim/` types broke ~20 assertions across Batch 1's 139-test suite (duck-typed literals and bare `undefined` no longer satisfied the widened shapes; TS also rejects object literals against a class with private fields entirely). Reverted: `Knowledge.ts` keeps its ORIGINAL narrow stand-ins for `StringLocated`/`TMemory`/`TContext`/`TFunction` unchanged; only `EaterException` and `TFunctionSignature` (both constructed via `new X(...)`, never duck-typed) were retired to the real classes. `tim/`'s own files (`Eater`, `TFunctionImpl`, `VariableManager`) get a SEPARATE, wider canonical `TContext` (asKnowledge + executeLines + applyFunctionsAndVariables) declared in `TFunction.ts`, and the real `TMemory` from `TMemory.ts`. `EaterException`'s `location` param is typed as a minimal `{ getLocation(): LineLocation }` structural shape (not the concrete `StringLocated` class) so it accepts both narrow and real callers. | Per the mission's "investigate and report, don't bridge with an adapter/cast" instruction: the narrow stand-ins were correct minimal-structural-contract design, not a defect: nothing in `tim/expression/` calls the wider members. Widening broke tests for zero functional gain. | resolved |
| 2026-07-12 | Batch 2a | `TMemory.putVariable`'s `scope` parameter widened to `TVariableScope \| undefined` (all three declarations: interface + both implementations) | `EaterAffectation.java` passes `TVariableScope.lazzyParse(varname)`'s result directly to `putVariable` — `null`/`undefined` is a real, reachable value representing "no explicit `!local`/`!global` keyword" and drives `TMemoryLocal#putVariable`'s fallthrough resolution order. Caught while writing tests (an unscoped-assignment test would not otherwise type-check). | locked |
| 2026-07-12 | Batch 2a | Per-assignment `Log.info("[MemGlobal] Setting X")` / `"[MemLocal/...] Setting X"` trace calls in `putVariable`/`removeVariable` NOT ported; `dumpDebug` (the `!dump_memory`-triggered diagnostic) IS ported faithfully via `console.info` | Zero effect on diagram output; this project's `logging.md` argues against always-on DEBUG-level logging with no level-gating framework to gate on. `dumpDebug` itself is user-invoked output, not incidental tracing, so it is preserved. | logged, not pre-approved — flagging for review |
| 2026-07-12 | Batch 2a | `Eater.java`'s JSON literal branch (`eatExpression`'s `{`/`[` case) uses native `JSON.parse` instead of porting `net.sourceforge.plantuml.json` (a full boxed JSON AST library, `minimal-json`) | `minimal-json` implements standard JSON grammar; native `JSON.parse` is a faithful mechanical substitute, not a special case worth preserving byte-for-byte. Consistent with `Token.ts`'s existing `JsonValue` type (Batch 1). | locked |
