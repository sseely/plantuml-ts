# SI5b+E2r decision journal

| When | Task | Decision | Why | Flag |
|---|---|---|---|---|
| 2026-07-14 | brief | Interactive plan-mission with maintainer-confirmed decisions D1-D9 (capture-all/publish-audited, four packages incl. -all meta sans GPL, verbatim hard constraint, sync Decompressor port, stored-block PNG + data-URI pass-through, skinparam-owned sprite registry). | chat rulings 2026-07-13/14 | no |
| 2026-07-14 | batch-1 | GATES PASSED (orchestrator-run): 8045/8045 (301 files, +36), typecheck/lint/build clean, DOT FROZEN exact, vendor --verify clean (34587 files). T1: split manifests (root index + per-bundle, ~3.5MB committed; single file would be ~5MB). T3 divergence: alias-cycle guard (upstream would hang). T2 new hook lesson: new Array<T>() generics trip lizard's <>-misparse — avoid the pattern. No CI test workflow exists at all (only docs.yml) — T9/T10 note. | agent reports + gate run | no |
| 2026-07-14 | batch-2 | GATES PASSED (orchestrator-run): 8153/8153 (305 files, +108), clean, DOT FROZEN exact. Seams for T7 to reconcile: (a) T5 sprite-raster types against {grayLevels,pixelAt}, T4 shipped {grayLevel,getGray} — one-line adapter per T5's header note; (b) T6 SpriteDimsLookup.get() vs T4 SpriteRegistry.getSprite() — name-bridge at the join; (c) measurement lookups not yet fed from ast.sprites (T7/T9 wire it). T5 divergence: upstream scales sprites via AWT bilinear (unportable) → natural-size PNG + scaled image dims (T10 DIVERGENCES). T6: creole.ts untouched (over cap); atoms in creole-atoms.ts. New hook lessons: bitwise ops +2 CCN apiece in lizard's TS parser; ?? likewise. | agent reports + gate run | T7 |
| 2026-07-14 | batch-3 | GATES PASSED (orchestrator-run): 8182/8182 (307 files, +29), clean, DOT FROZEN exact. T7 write-set expansion accepted (real join point = buildTextBlock; klimt UImage+driver were the genuine missing primitives). T8 judgment calls transcribed: bootstrap subpath exports alias+target together; tupadr3 index-only (multiple partial BundleDatas sharing a name would overwrite in stdlibStore); peerDep ^0.1.0 + devDep file:../.. (npm workspaces doesn't auto-link root by name — verified E404); root files:[dist]. FLAGS: cloudogu has no upstream LICENSE file (MIT claim = README only — maintainer must confirm before publishing @plantuml-ts/stdlib); C4 default <$person> sprite unresolved (no bundled sprite named person) — T9 drill item. | agent reports + gate run | cloudogu license; T9 |
| 2026-07-14 | T9 | Harness wiring: assets-backed StdlibStore (`scripts/stdlib-assets-store.ts`, re-exported `tests/helpers/stdlib-assets-store.ts`) wired into `scripts/dot-sync-report.ts#ourInputs` (SCOPED to `STDLIB_WIRED_TYPES = {component, usecase}` only -- wiring it universally regressed 2 CLASS-tagged fixtures, see mechanism row below) and `tests/oracle/description-parity.ratchet.test.ts`. Measured baseline (harness wiring alone, no src/ fix yet): component 262/262 EQUAL, usecase 89/90 EQUAL -- 5 of the 6 target fixtures (xusuxe-62-guba767, fariba-82-xolu802, kofuca-08-pafi749, ruziru-69-xixo434, bootstrap-0) were ALREADY structurally EQUAL from resolution alone; only vivido-49-nisu863 failed (nodeCount 2 vs oracle 3). | plans/si5b-stdlib/batch-4/overview.md T9 Step 0-1 | no |
| 2026-07-14 | T9 | MECHANISM 1 (vivido-49-nisu863, entity-code collision): `parseNameSection` (`src/diagrams/description/parse-helpers.ts`) called `stripUrl(rest.trim())` unconditionally BEFORE quote extraction, stripping an inline `[[url]]` creole link even when embedded WITHIN a quoted display (`rectangle "text[[url]]"`). Two `rectangle` labels differing only inside their embedded `<$database*0.31>` vs `<$database>` link collapsed to the SAME stripped display text -> same entity CODE -> one dropped node (candidate nodeCount 2 vs oracle 3). Java: `CommandCreateElementFull.java:84,88` (`CODE_WITH_QUOTE`/`DISPLAY` = a single lazy-quoted match consumed WHOLE) + `:113` (`UrlBuilder.OPTIONAL` positioned strictly AFTER that alternation in the regex concat -- can only ever match text OUTSIDE the quotes). Ours (before): `parse-helpers.ts` old `parseNameSection` line 1 (`let remainder = stripUrl(rest.trim())`). Ours (fix): added `splitLeadingQuote` (quote-boundary-aware split) so `stripUrl` only runs on the TAIL after a leading quote's close, never inside it; quoted content (including embedded `[[...]]`) now survives verbatim into id/display. Ruled out: cleanId dedup (traced -- RE_DQ_ONLY branch never calls cleanId; parseNameSection alone already produced distinct ids once un-stripped, confirmed via isolated repro before touching layout.ts). Pinned by `tests/unit/description/parse-helpers.test.ts`'s new `parseNameSection` describe block (5 cases: embedded-link survival, no-dedup, trailing-URL-after-quote still stripped, trailing-URL-before-alias still stripped, bare-id trailing URL still stripped). | diagnosis (drilldown + Java read) + TDD | no |
| 2026-07-14 | T9 | MECHANISM 2 (vivido/xusuxe class of bug, keyword-scan-window defeat): once `!include` genuinely resolves a vendored bundle, a multiline `sprite $name [WxH/N] { ... }` body (30-50 ASCII-art lines) is inlined BEFORE the real diagram content -- pushing `title`/`rectangle`/etc past `SCAN_LINE_LIMIT=20` in `hasDescriptiveSignal`/`hasDescriptiveElement` (`src/core/descriptive-keywords.ts`). For vivido-49-nisu863 this meant `class`'s decline guard (`class-dispatch.ts:252`, `if (hasDescriptiveSignal(declLines)) return false`) never fired, so `classPlugin` (registered before `descriptionPlugin`, `index.ts`) claimed the block and rendered it via the wrong, simpler entity model (2 uniform-height nodes instead of 3 sprite-sized ones) -- confirmed via `registry.resolve` returning `plugin.type='class'` before the fix, `'description'` after. Java: sprite is a regular diagram COMMAND (`command/CommandSpriteFile.java` et al, `CommonCommands.java`), never TIM-preprocessor-stripped, but upstream's per-`@startuml` type resolution runs the FULL grammar dispatch (not a fixed-line-count heuristic), so a 50-line sprite body never defeats it -- our port's SCAN_LINE_LIMIT heuristic is the actual divergent piece, not sprite handling itself. Ours (fix): exported `isSpriteMultilineOpenLine`/`isSpriteMultilineCloseLine` from `src/core/sprite-commands.ts` (reusing the existing `MULTILINE_START_RE`/`END_RE`, no duplicated grammar); added `stripSpriteRegions` in `descriptive-keywords.ts`, composed with the existing `stripLegendRegions` via `stripNonContentRegions`, applied before the `SCAN_LINE_LIMIT` slice in both `hasDescriptiveSignal` and `hasDescriptiveElement`. Ruled out: dedup at parse layer (parseDescription/layoutDescription in isolation already produced 3 correctly-sized nodes -- the bug was ONLY in which plugin claimed the block, not in the description engine itself). Re-measured after fix: usecase 89->90/90 EQUAL, component held 262/262, class/object/state held EXACT (708/708, 78/80, 266/267) -- confirms MECHANISM 2's fix is scoped correctly (SCAN_LINE_LIMIT applies identically regardless of stdlib wiring). | diagnosis (registry.resolve probe) + TDD | no |
| 2026-07-14 | T9 | HARNESS-WIRING REGRESSION (caught, reverted before commit): universally wiring the stdlib store into `ourInputs` for ALL 5 dot-sync-report types (not just component/usecase) flipped 2 CLASS-tagged fixtures (bidusa-22-jutu505, ruliki-78-biji661) from a legitimate 0-vs-0 EQUAL (both sides skip graphviz -- our side previously via `StdlibNotBundledError` before layout, per `class-dot-parity.test.ts`'s own doc comment) to a real graph-count mismatch (oracle 0, ours 1) once the `!include <tupadr3/...>` actually resolved and surfaced a SEPARATE, out-of-scope divergence in the class engine's single-leaf/degenerate-graph detection for icon-only-field diagrams. Disposition: NOT fixed (out of scope -- class is FROZEN this mission) -- `ourInputs` scoped to `STDLIB_WIRED_TYPES = new Set(['component','usecase'])` instead, restoring class 708/708, object 78/80, state 266/267 EXACT. Slugs: bidusa-22-jutu505, ruliki-78-biji661 (both remain un-ratcheted, un-touched, exactly as before this mission). | diagnosis (before/after diff scan) | no |

## graphviz-ts layout crash on cluster + cross-boundary-edge topology (xusuxe-62-guba767)
- Mechanism: `graphviz-ts` (vendored, pinned via .tgz -- not in this mission's write-set) throws `TypeError: Cannot read properties of undefined (reading 'info')` deep inside `ctx.layout(g, 'dot')`, re-wrapped as `RenderError('RENDER_ERROR')` at the catch site, when laying out xusuxe-62-guba767's specific shape: one 4-node cluster (`cluster0`: jenkins/scm/smeagol/nexus) plus 2 external nodes (k8s, developer) and 5 labeled edges, one of which (`jenkins->k8s`) crosses the cluster boundary. `.info` is graphviz-ts's per-node/edge/subgraph internal-data slot (mirrors graphviz C's `ND_info`/`ED_info`/`GD_info`), uninitialized somewhere in the dot-engine's cluster-boundary-edge path for this topology (Java/C origin not traced further -- third-party, out of write-set). Ours: `src/core/graph-layout.ts:298` (`layoutInputObserver?.(input)`) fires BEFORE the crashing `render(b.graph,...)` call at line 311, so the captured `DotInputGraph` (and therefore `dot-sync-report`'s structural-EQUAL classification) is UNAFFECTED -- xusuxe-62-guba767 legitimately counts toward component's 262/262 DOT-EQUAL numerator. The crash only blocks a FULL render (`renderSync`/the description-parity SVG ratchet, which asserts `expectNoErrorDiagram`).
- Ruled out: (a) our DotInputGraph construction -- the captured graph is well-formed (4-node cluster with a label, 5 edges with labels/minlen, no obviously invalid shape); (b) my two source fixes above -- xusuxe was ALREADY DOT-EQUAL before either fix, so this crash is orthogonal to both; (c) jsdom/vitest-vs-node environment difference -- reproduces identically under plain `tsx` (Node) and under vitest+jsdom.
- Disposition: blocked-on graphviz-ts (third-party, pinned .tgz dependency, outside this mission's write-set) -- NOT added to `oracle/goldens/description/` (the ratchet's `expectNoErrorDiagram` assertion cannot pass while this stands). DOT-structural-EQUAL credit stands (counted in the component 262/262 target); full-render/SVG parity remains blocked pending a graphviz-ts fix or upstream report, tracked here rather than silently dropped.
- Slugs: xusuxe-62-guba767
