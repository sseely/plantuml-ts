# G4 decision journal

| When | Iter | Decision | Why | Flag |
|---|---|---|---|---|
| 2026-07-20 | S0 | Wrote a NEW `render-fixture-state.ts` instead of reusing `render-fixture-class.ts`/`render-fixture.ts` | State has its own dedicated upstream engine (`statediagram/`) and its own dedicated port pipeline (`parseState`/`layoutState`/`renderState`) — confirmed via `.claude/catalog.md` and direct source inspection before writing any code, unlike G3/O0's object dispatch (verified object has NO separate engine upstream before reusing the class helper) | |
| 2026-07-20 | S0 | Made zero code changes to `svg-parity-survey.ts` | The script already dispatches generically via `renderSync`'s production registry and already accepts `--out <path> <type...>` (additive since G2/N0) — confirmed by reading the file before assuming a change was needed, rather than blindly adding a state-specific branch that would have been dead code | |
| 2026-07-20 | S0 | Explicitly SKIPPED the optional stretch fix (mechanism 1, the SVG root shell) despite it being the single highest-reach family (271/271) | Verified via direct hand-probe (not just the census's aggregate table) that landing mechanism 1 alone would not move the census's zero-diff count off 0 — mechanisms 2 (no `<g>` wrapping) and 4 (document-margin gap) independently block EVERY fixture, confirmed on the simplest possible corpus member (`jocela-05-niba392`, 1 state, 0 transitions). Per this iteration's own instruction ("skip if it isn't clearly bounded"), landing an unverifiable change (0/271 before, 0/271 after) was rejected in favor of naming all four mechanisms precisely for S1 | |
| 2026-07-20 | S0 | Diagnosed FOUR mechanisms via direct probing/reading rather than three or fewer | The census's own family table stops at the root-`<svg>` level for every fixture (mechanism 2's `childCount` short-circuit in `compareSvg`), so the aggregate table alone could not distinguish "shell" from "wrapping" from "arrowhead mechanism" from "layout margin" — each was isolated by choosing a DIFFERENT minimal-confounding-factor fixture (trivial 1-state for margin; nested-composite for wrapping/shell; direct string search across 8 sampled fixtures for the marker-vs-inline-polygon arrowhead difference) rather than reported as one undifferentiated "SVG structure is wrong" finding | |
| 2026-07-20 | S0 | Sampled 16 fixtures by hand (exceeding the mission's own 15-fixture floor) spanning every major state feature (every pseudostate stereotype, 3-level nesting, concurrent regions, both history forms, 3 note forms, URLs, skinparam cascades, LaTeX bodies) | Confirmed the 4-mechanism signature is IDENTICAL across all 16 despite maximal feature diversity — this is load-bearing evidence for the ledger's own claim that "no 17th mechanism exists in this sample" and that the corpus's real per-feature diversity is currently invisible below the root (mechanism 2), not merely asserted from the aggregate census numbers | |
| 2026-07-20 | S0 | Wrote the state ratchet's AC3-deferred test assertion differently from `object.golden.ratchet.test.ts`'s own `expect(parity.fixtures).toHaveLength(0)` | Copying it verbatim would have been WRONG for state: `parity-state.json` is a real, populated 271-entry survey (unlike object's O0, which never generated an unsurveyed placeholder at all), so the correct assertion is `parity.fixtures.length > 0` (survey ran) AND `manifest.fixtures.length === 0` (nothing pinned yet) — caught by actually running the survey and inspecting its contents before writing the test, not by pattern-matching the object file blindly | |
| 2026-07-20 | S1 | Landed all four S0 mechanisms in one iteration rather than splitting across S1/S2 | Each mechanism reused ALREADY-BUILT machinery (`assembleDocumentShell`, `ExtremityArrow`/`place`, class's own `renderer-group.ts`/`layout-ink-extent.ts` shape) via thin state-specific adapters — none required new algorithm design, only jar-verified wiring, so the combined lift stayed within one iteration's budget despite covering 4 named items | |
| 2026-07-20 | S1 | Did NOT force mechanism 5 (state box/shape content fidelity) into this iteration despite it being the ONLY remaining blocker for zero-diff | Unlike mechanisms 1-4, mechanism 5 has no already-built machinery to adapt — it requires a genuine per-shape content rewrite (divider line, exact colors/stroke-widths, jar's real text-layout convention, per-node color-override threading) comparable in scope to G2's own multi-iteration classifier-box work. Confirmed via direct probe that a PARTIAL fix (e.g. just the divider line) would not reach zero either — the box's own attribute-level diffs sit one layer further down, hidden by the SAME childCount-short-circuit pattern mechanism 2 itself exhibited. Diagnosed per diagnosis.md (mechanism/origin/causal-chain/ruled-out) and queued for S2 rather than partially landed under time pressure | |
| 2026-07-20 | S1 | Wrapped ALL composite (`children.length > 0`) states as `class="entity"` uniformly, despite jar-verified evidence (`bajelo-54-dixe684`) that some composites wrap `cluster` instead | The `autonom`-vs-non-autonom classification that determines this split lives entirely inside `state-composite-pass.ts`'s internal `GeoSpec.kind` and is never threaded onto the public `StateNodeGeo` this mission's write-set is scoped to render from — plumbing it through is a genuinely separate, unscoped write-set expansion (a new geometry field), not a same-iteration wrapping-logic fix. `entity` is correct for the common (autonom) case; named as a residual rather than guessed at differently per-fixture | |
| 2026-07-20 | S1 | Fixed transition `stroke-width` (1.5→1) and pseudo-endpoint `<path id>` values (`__initial__`/`__final__`→`*start*`/`*end*`) while landing mechanism 3, though neither was one of S0's four named mechanisms | Both are directly adjacent, single-line corrections inside the SAME function (`renderTransitionWrapped`) being rewritten for mechanism 3, and both attributes are exact-match (non-numeric, non-`data-*`) fields `compareSvg` compares byte-for-byte — leaving them wrong would have silently blocked every transition-bearing fixture from ever reaching zero, for a reason unrelated to any of the five named mechanisms. Treated as "fix violations in the same file" (`pr-workflow.md`), not scope creep | |
| 2026-07-20 | S2 | Threaded per-line measured text (`headerLines`/`bodyLines`) onto `StateNodeGeo` at LAYOUT time rather than giving the renderer its own `StringMeasurer` | Mirrors the class engine's own `ClassifierGeo.rows[].width` precedent exactly — the renderer stays a pure, DOM-free, measurer-free function (project architecture constraint); a shared `state-sizing.ts#buildStateGeoTextFields` builder avoids the flat and composite pipelines independently re-deriving the same per-kind (normal/history/deepHistory) dispatch | |
| 2026-07-20 | S2 | Substituted a real U+00A0 NBSP (not a plain space) for an empty captured body line, after confirming byte-for-byte against the jar fixture's raw UTF-8 | Guessing "a space" first and moving on would have looked plausible (textLength would still be non-zero) but was WRONG — verified via direct byte inspection (`\xc2\xa0`) before writing the fix, per diagnosis.md's "instrument before hypothesizing"; matches the class engine's own already-documented NBSP-substitution precedent, not a new invented mechanism | |
| 2026-07-20 | S2 | Added a `historyLabelText`-based fallback to `renderHistory` for the `headerLines === undefined` case (hand-built test geometries) | Without it, every pre-existing hand-built-geometry unit test for history/deepHistory would need to switch from asserting real label content to asserting no label at all — a worse test suite, not just a passing one. Mirrors `renderer-box.ts#renderNormal`'s own pre-existing unmeasured-fallback convention (added in the SAME iteration) rather than inventing a second pattern | |
| 2026-07-20 | S2 | Updated (not preserved) two pre-existing tests whose names asserted stale/buggy behavior as a requirement (`layout.test.ts`'s "TransitionGeo preserves original [*] from/to"; several `renderer.test.ts` color/shape assertions) | Both assertions encoded behavior now PROVEN wrong against the jar oracle (byte-verified, not a style preference) — per testing.md, a test asserting incorrect behavior is a bug in the test, not a contract to preserve; updated with a doc comment naming the jar evidence rather than silently deleted or left red | |
| 2026-07-20 | S2 | Diagnosed composite box rendering (mechanism 6) via direct raw-SVG inspection of `bajelo-54-dixe684` BEFORE attempting any fix to S1's named items 3/4 | Per diagnosis.md's "instrument before hypothesizing" — S1's own framing ("entity-vs-cluster wrap split", "composite ink rule") turned out to be a symptom-level description of a much larger, different mechanism (the box's own DRAWING CONVENTION, not just its wrap class or ink margin); landing a fix against the wrong diagnosis (e.g. tweaking `addStateBoxInk`'s composite branch) would not have moved any fixture toward zero and would have consumed the remaining iteration budget on the wrong target | |
| 2026-07-20 | orchestrator (between S2/S3) | graphviz-ts 0.1.26071415→0.1.26072013 per maintainer; all five docs/graphviz-issues repros verified byte-identical to real dot → tracker boxes checked. Verification exposed that the residual "gvts-blocked" drift on couple fixtures was OURS: `graph-layout.ts#addNodes` hardcoded `shape:'box'`, discarding `circle`/`point` (proven A/B on bunuce-10-vere519 — all-box reproduces the exact 87.2373 drift; honoring circle matches jar's 87.18). Fix: `layoutShape` mirror of `svek-dot-emit#nodeLine` + point→`width=.01` (new `graph-layout-build.ts`, split for the 500-line cap). Class census 294→303 (+11 pins incl. bunuce/bosiki, ratchet 305 tests); description/object/state censuses unchanged; DOT structural gate frozen exact; one state size-backlog allowance (fotuje-06-fifa085) bumped 3.238626→3.241979 in — composite outer size derives from now-more-faithful inner layout; true fix is S3's mechanism 6. |
| 2026-07-20 | S3 | Landed the composite box's real 3-4-layer shape (mechanism 6) for the AUTONOM (`entity`-wrap) case only, deliberately leaving the non-autonom `cluster` case on the pre-S3 dashed-rect fallback | Direct raw-SVG inspection of `bajelo-54-dixe684`'s own `Track_FSM.Run` showed the cluster path uses a DIFFERENT header-height source (graphviz's own cluster-label sizing, 19px, not `InnerStateAutonom`'s MARGIN formula, 24px) — landing both in one iteration would have required exposing graphviz cluster bounding boxes (a library/`graph-layout.ts`-level change with its own cross-diagram-type census-proof requirement per the mission brief's fenced sub-item 4), genuinely unbounded; the fallback path was already correct for this case (unchanged, non-regressing) so scoping to autonom-only was the smaller, verifiable unit | |
| 2026-07-20 | S3 | Split `state-composite-pass.ts`'s `buildPlainAutonomSpec`/`buildAutonomSpec`/`resolveAllAutonomPasses` into a NEW `state-composite-autonom.ts` (accepting a circular import with `state-composite-pass.ts`/`state-composite-concurrent.ts`) rather than restructuring to avoid the cycle | The 500-line file-cap hook BLOCKS edits to over-cap files (mission's own hard boundary), and `state-composite-cluster.ts`/`state-composite-concurrent.ts` already establish the SAME circular-import pattern with `state-composite-pass.ts` (both import `resolveMember`/types from it while it imports back from them) — confirmed via direct inspection this is safe in ESM because every cross-module call happens inside a function body, never at module-eval time, before adding a third instance of the same established pattern | |
| 2026-07-20 | S3 | Implemented mechanism 7's width/height fix (`tightContentDimension` reuse), jar-verified it improved 2 fixtures, then REVERTED it after `npm test -- --run` showed it regressed 2 already-pinned `size-backlog.json` entries past their own allowance | `oracle/goldens/state/size-backlog.json` is an explicit tighten-only hard boundary in this iteration's own instructions — no exception for "verified improvement elsewhere." Kept the `tightContentDimension` export and the full diagnosis (mechanism/origin/causal-chain/ruled-out) in the ledger so S4 does not have to re-derive it, but did not land a change that violates a named hard boundary, even a well-understood and partially-correct one | |
| 2026-07-20 | S3 | Diagnosed a THIRD sub-component (child position-offset, ~7px consistent gap) as likely required to land ALONGSIDE mechanism 7's width/height piece, rather than attempting it standalone this iteration | Direct `layoutGraph()` probe confirmed a single-node child pass's own raw node position is `(0,0)`, not pre-shifted by `SvekResult`'s own `moveDelta(6,6)` — the observed ~7px gap does not match that constant cleanly, meaning a correct derivation needs more instrumentation than this iteration's remaining budget allowed; per diagnosis.md, named the uncertainty explicitly (ruled out `moveDelta(6,6)` as the sole cause) rather than guessing a magic constant to force a fix | |
| 2026-07-20 | S3 | Did NOT attempt to independently jar-verify the composite ink-extent rule (item 2) byte-exact, despite strong algebraic evidence (`coteta-47-mare883`'s document-size delta matches its composite box's own width delta EXACTLY) that no separate ink-rule bug exists | No corpus fixture currently has a mechanism-7-correct composite box (mechanism 7 itself was reverted), so a true zero-residual confirmation is impossible until mechanism 7 lands — recorded the algebraic evidence as a strong but explicitly NOT-YET-CONFIRMED finding rather than overstating verification status the corpus cannot currently support | |
| 2026-07-20 | S4 | Re-derived mechanism 7's fix from scratch (ink-extent-aware `SvekResult#calculateDimension()`) rather than resurrecting S3's own reverted `tightContentDimension`-based trial | Hand-derivation against jar's real SVG for `coteta-47-mare883` proved S3's own naive-geometric trial was off by EXACTLY the leaf-state-box ink rule's `-1` min-corner asymmetry (mechanism 4's own `addStateBoxInk`) — the position-offset residual S3 left unexplained (~7px, not the expected 6px) and the width/height gap are the SAME root cause, not two independent problems; landing the ink-aware version closed BOTH in one mechanism instead of two | |
| 2026-07-20 | S4 | Excluded arrowhead ink from `computeSvekResultGeometry`'s own walk (`includeArrowheadInk:false`) rather than root-causing `transitionArrowheadInk`'s over-reach bug | The bug is in ALREADY-BUILT, ALREADY-verified-elsewhere code (mechanism 3, S1) — jar-verified the anomaly is real (self-loop/back-edge cases produce ~2-3x the expected ink span) but tracing it to an exact `ExtremityArrow`/`rotate-point.ts` line was out of this iteration's budget; scoping the workaround to ONLY the new composite-sizing call site (leaving the document-level mechanism-4 call site's `true` behavior completely unchanged) bounds the risk to zero for every already-pinned fixture | |
| 2026-07-20 | S4 | Reverted a same-iteration attempt to measure edge-label ink width inline (`buildLabelInkPoints`, `ctx.measurer`) in favor of a `Math.max(geometry, result)` floor | Jar-verified the label-width estimate was directionally correct on `bunade-42-fudu910` (closed most of a 21-fixture regression) but OVERSHOT on `beguxu-19-tize774` (a different, single-labeled-edge fixture) — net WORSE than the simpler floor alone (7 failures vs 4). Per diagnosis.md, a fix that helps one case and hurts a different case without a unifying, verified mechanism is not yet a real fix — reverted rather than landed with a known counter-example, and named for S5 | |
| 2026-07-20 | S4 | Extended scope to fix `ConcurrentStates`' own separator-gap formula (mechanism 8, `state-composite-concurrent.ts`/`state-composite-sizing.ts`) rather than special-casing mechanism 7 to avoid concurrent-region composites | The mission's own hard boundary ("must not regress any size-backlog entry") left no in-scope-only path — mechanism 7's own correct fix for a nested composite (`toutou9`) unavoidably cascades into whatever consumes that composite's now-accurate size, and `resolveAllAutonomPasses`' firing-order-decoupled architecture (mission A4 Phase L iter 17, load-bearing precedent) has no cheap way to detect "this composite's ancestor is a concurrent-region owner" without a larger redesign. A direct read of `ConcurrentStates.java` (not a guess) found the REAL formula differs materially from the S1-era placeholder, and fixing it resolved every regression AND improved dozens of already-pinned fixtures well beyond mechanism 7's own scope — the broader, principled fix was less risky than a narrower, unverified special-case | |
| 2026-07-20 | S4 | Tested `CONCURRENT_SEPARATOR_GAP=0` in isolation BEFORE also switching to ink-based per-region sizing, and used the isolated-experiment result (2 NEW regressions) to prove both pieces are required together | Per diagnosis.md's "instrument before hypothesizing" — confirms the `ConcurrentStates.java` reading was applied completely and correctly (gap alone is insufficient, matching the source's own `inner.calculateDimension()` call for region dimension) rather than assuming a partial understanding was enough because it moved metrics in the right direction | |
| 2026-07-20 | S4 | Diagnosed a `parity-state.json` regen run showing `xuzapa-55-xoli880` (an already-pinned fixture) as `dotEqual:false`/`timeout` as a survey-tool/system-load artifact rather than a real regression, and re-ran the survey in isolation to confirm | The fixture's own `AC1` byte-exact-against-golden ratchet test (a DIFFERENT, more direct check) never stopped passing throughout — process-list inspection found 2 concurrent, stale `svg-parity-survey.ts` invocations competing for CPU/JVM resources (leftover from earlier background bash calls), explaining the elevated timeout rate (130+/271 in the worst run); killing the stale processes and re-running singly produced a clean 0-timeout survey, confirming the earlier result was noise, not signal | |
| 2026-07-20 | S4 | Computed EVERY `size-backlog.json` entry's actual current delta (not just the composite-bearing subset mechanism 7/8 directly touched) before tightening | A composite-wrapper/concurrent-stacking fix's blast radius is not obviously scoped to "only composite fixtures" — `maxSizeDeltaIn`'s own sorted-array comparison can shift for fixtures whose OWN content didn't change, purely from a DIFFERENT node's size changing elsewhere in the same DOT graph (already observed once this iteration, `bemena-23-zebu249`/`jorere-75-peja265`/etc.'s identical `0.32397...` values tracked back to a shared underlying pattern) — auditing the full 138-entry set (not a sample) was the only way to guarantee the tighten pass was itself `new <= old` everywhere, per the hard "tighten-only, verify" boundary | |
| 2026-07-20 | S5 | Chose composite internal-transition nesting (mission's own named primary scope) as the FIRST mechanism to land, ahead of any 1-3-bucket-specific "cheap" candidate initially visible | Sampling the 1-3/4-10 buckets FIRST (per this iteration's own instruction) showed `svg/g[1][childCount]` (alone or with `svg/@height`/`@width`/`@viewBox`) is by far the dominant signature, reaching 129/271 fixtures at the EXACT classic pattern alone — orders of magnitude larger reach than any other single candidate found during sampling, confirming it as the single highest-leverage fix despite NOT being individually "cheap" to implement (a real architectural restructuring, not a one-line formula tweak) | |
| 2026-07-20 | S5 | Restructured `StateNodeGeo` to carry its OWN `transitions` field (tree-shaped) rather than threading a `parentId`/scope tag onto a still-flat `TransitionGeo[]` | A tree-shaped attachment matches jar's OWN real document structure exactly (edges are direct children of the SAME `<g>` their owning pass's other content lives in) and lets `renderNodeWrapped`'s EXISTING recursive wrap logic render them with zero new dispatch code — a flat array + parentId lookup would have required a SECOND recursive walk (build a parentId→children index, then interleave during rendering) to reconstruct the SAME tree shape this field already IS | |
| 2026-07-20 | S5 | Kept `computeStateDocumentDims`/`computeStateInkShift`/`computeSvekResultGeometry`'s own public signatures COMPLETELY unchanged (still `states`+`transitions` params) rather than threading the new tree-shaped transitions through every ink-computation call site | `addNodeInk`'s existing recursion into `node.children` was ALREADY the exact right place to ALSO recurse into `node.transitions` (same coordinate space, same "walk the tree" shape) — extending that one function's own recursion covers every call site's ink coverage identically to the pre-S5 `outTransitions`-accumulator behavior, with zero signature churn across `state-composite-autonom.ts`/`state-composite-concurrent.ts`/`layout.ts`'s own call sites | |
| 2026-07-20 | S5 | Changed `StateUidPlan.edgeUid` from an array (parallel to a single flat list) to a `Map<TransitionGeo, string>` keyed by object identity | Transitions now live in MULTIPLE arrays (top-level + every composite node's own `.transitions`) once mechanism 9 lands — a single array-index scheme can no longer identify "the Nth transition" meaningfully across that many arrays. Object-identity keying works cleanly because every `TransitionGeo` is a freshly-constructed object per render pass (never pooled/reused), so there is no risk of two logically-different transitions colliding on the same key | |
| 2026-07-20 | S5 | Updated (not preserved) `layout.test.ts`'s two pre-existing composite-internal-transition tests, which asserted the transition appears in `result.transitions` (the top-level array) | Both assertions encoded the OLD, now-proven-incorrect flat-sibling behavior mechanism 9 deliberately replaces — per testing.md, a test asserting behavior that is now KNOWN wrong (jar-verified) is a bug in the test, not a contract; updated to assert the NEW, correct per-node attachment, with a doc comment naming the jar evidence (`bajelo-54-dixe684`) rather than silently deleted | |
| 2026-07-20 | S5 | Flipped `buildTopLevelPass`'s own `specs` order (`[...pseudoSpecs, ...specs]` → `[...specs, ...pseudoSpecs]`) despite direct evidence (`nelupe-49-xova546`) that jar's TRUE ordering rule is creation-index-based, not simply "real before pseudo" | Verified SAFE (zero-risk) against all 13 S4-pinned fixtures first — none use a top-level `[*]` transition, so `pseudoSpecs` is always empty for them, making this reorder a pure no-op for every currently-pinned fixture. The fix IS correct for the common single-composite-plus-one-pseudo-pair shape (jar-verified via `bajelo-54-dixe684`'s own structure) even though it does not close the harder multi-real-entity case — landed the PARTIAL, safe, real improvement rather than withholding it while a full creation-index solution (out of this iteration's scope) remains unbuilt | |
| 2026-07-20 | S5 | Implemented the explicit background `<rect>` mechanism (12) empirically, from jar SVG bytes across all 11 non-default-background fixtures, after a targeted Java source search failed to locate the exact draw call | Per this project's own established "jar-verified, source line not found" precedent (mechanism 3's own `transitionArrowheadInk` sub-bug, S4) — the mechanism's SHAPE is unambiguous and confirmed across every available sample (11/11 match, 2/2 default-background samples confirm no false positive), meeting the bar for a real, evidence-based fix even without a pinned Java call site. Logged the specific classes searched (ruled out) so a future iteration does not repeat the same unsuccessful search | |
| 2026-07-20 | S5 | Diagnosed BOTH the concurrent-region separator-line gap (mechanism 13) and the per-region pseudo-node id collision (mechanism 14) via a full jar XML pretty-print of `nelupe-49-xova546`, but explicitly did NOT land either this iteration | Both are genuinely NEW rendering features (a not-yet-implemented dashed-line draw call; a scope-id re-derivation touching `buildConcurrentRegionPass`'s own `addLocalPseudoNodes`/`addLevelEdges` calls) rather than "fix an existing formula" — per this iteration's own "cheapest first" instruction, forcing two new, moderately-scoped features into an already-large iteration (5 other mechanisms already landed) risked rushing verification on the CONCURRENT-region family specifically (only 18/271 fixtures, but each fixture is structurally complex) — named both precisely (including the EXACT scope-id parameter that needs to change) so S6 can land them without re-diagnosing | |
| 2026-07-20 | S5 | Did NOT start the mission's own secondary scope item (transition `path/@d` routing/positioning) despite it being named explicitly in the task | Mechanism 9's own scope (the mission's PRIMARY, explicitly-named target) plus its 5-mechanism unmasking cascade (10/11/12 landed, 13/14 diagnosed) consumed the full iteration budget — verified via the census/ratchet/DOT-gate re-runs required after EACH mechanism landing (5 full verification passes this iteration, each taking real wall-clock time). Named the specific fixture/edge showing the clearest `path/@d` divergence (`gopumi-11-pise779`'s `[*]-to-s0_start`) so S6 has a concrete starting point rather than a cold restart | |
| 2026-07-20 | S6 | Sampled ALL 29 (not just >=20) of S5's own near-zero 1-3-bucket fixtures individually, plus 10 from 4-10, BEFORE choosing which mechanism to land first, explicitly testing the mission's own suggested "mechanism-19 cheap subset FIRST" ordering against the data | The full sample showed `svg/g/g/path/@d` (mechanism 19) NEVER appeared as the SOLE or DOMINANT diff on any near-zero fixture -- every occurrence co-occurred with a larger blocker (mechanism 16 or the id-numbering gap) -- so mechanisms 13/14 (already fully diagnosed in S5, 3 direct near-zero unlocks confirmed) were the genuinely cheapest-first target, not an assumption carried over from the task's own suggested default ordering | |
| 2026-07-20 | S6 | Landed the concurrent-region `moveDelta` position-offset fix (mechanism 7's own formula, never wired into `state-composite-concurrent.ts`) as PART OF mechanism 13's own change, not as a separately-numbered mechanism | It is not a new algorithm -- `state-composite-autonom.ts#buildPlainAutonomSpec` already computes and applies the IDENTICAL `computeSvekResultGeometry` `dx`/`dy` via `shiftDotLayoutResult` for the plain-composite case (S4); the concurrent path's own `regionInkDim` already computed the SAME geometry object but discarded `dx`/`dy`, using only `width`/`height`. Renaming to `regionInkGeometry` and reusing the already-exported `shiftDotLayoutResult` closed a gap in an EXISTING formula, not a new mechanism requiring its own diagnosis-from-scratch | |
| 2026-07-20 | S6 | Exported `shiftDotLayoutResult` from `state-composite-autonom.ts` for reuse in `state-composite-concurrent.ts`, accepting the resulting circular import (`autonom.ts` already imports FROM `concurrent.ts`: `buildConcurrentAutonomSpec`/`buildConcurrentRegionPass`) | S3's own decision journal already established this EXACT circular-import pattern as safe in ESM for this file pair (every cross-module call happens inside a function body, never at module-eval time) -- reusing an ALREADY-accepted pattern rather than duplicating the function body or restructuring module boundaries under this iteration's own time budget | |
| 2026-07-20 | S6 | Added a SECOND `resliceRegions`-shaped helper (`resliceStateRegions`) in `layout.ts`, duplicating ~10 lines from `state-composite-geo.ts#resliceRegions`, rather than importing the original | `layout.ts` and `state-composite-geo.ts` are DIFFERENT layers (document-level document-margin shift vs per-composite materialization shift) operating on the SAME `StateRegionGeo` shape by coincidence, not by shared responsibility -- a needless cross-module dependency for a 10-line pure function was judged worse than the small duplication, matching this codebase's own established precedent for splitting file-cap-driven modules that share small helpers (state-composite-cluster.ts/state-composite-concurrent.ts already duplicate small helpers rather than share them across the SAME kind of layer boundary) | |
| 2026-07-20 | S6 | Did NOT attempt `addStateBoxInk`'s max-corner asymmetry fix (`bilare-19-fufe539`'s 1px rounding) despite deriving the EXACT one-character change that closes it algebraically | The function is an ALREADY jar-verified, universally-reused ink formula (every leaf state box in the 271-fixture corpus) whose asymmetry is explicitly documented as DELIBERATE in its own S4-era doc comment -- changing it on the strength of ONE fixture's algebra, without budget this iteration to verify the full corpus/`size-backlog.json` blast radius (a hard tighten-only boundary), was judged too risky. Named precisely for a future iteration with budget to run the full verification | |
| 2026-07-20 | S6 | Re-diagnosed `niveno-60-tiro789`'s queued "background theme-resolution bug" (S5's own framing) by reading upstream's actual `skin/debug.skin` resource file BEFORE attempting any fix | Per diagnosis.md's "instrument before hypothesizing" -- the queue's own one-line description ("background theme-resolution failure") would have led directly to a wrong, narrow fix (e.g. patching background-color resolution alone); the real gap is a WHOLE unimplemented `skin <name>` directive (a multi-property override file: FontSize, RoundCorner, LineThickness, BackGroundColor, stereotype/title/header blocks), confirmed by reading the actual jar-loaded skin file rather than trusting the prior iteration's own compressed summary | |
| 2026-07-20 | S6 | Re-sampled mechanism 16 (entity-vs-cluster wrap dimension) with a SECOND fresh fixture (`gojuja-90-pune699`) beyond the single `decede-10-buvu414` sample, before re-confirming (not re-deriving from scratch) S1/S3's own "unbounded, needs graphviz cluster-bbox" conclusion | The two samples' cluster margins DIFFER (16px for a 50w rect child vs 24px for a 20w circle anchor) -- if they had MATCHED, that would have been strong evidence for a fixed-constant fix (tractable, worth landing); since they diverge in a content-shape-dependent way, this confirms (rather than merely repeats) the graphviz-native hypothesis with a genuinely independent data point, avoiding the risk of a false "still unbounded" conclusion drawn from too small a sample | |
| 2026-07-20 | S7 | Chose RAW `creationIndex` values directly (`ent%04d(idx)`), not dense re-packing over a sorted merge (G2's own `class/renderer-uid.ts#buildClassUidPlan` precedent) | Verified via `xoravu-40-gebe122` (a `remove`-directive fixture): since `ParseState.creationCounter` increments for EVERY jar tick — including the CONC-phantom-group tick and a `remove`d entity's OWN tick, stamped during parsing BEFORE `filterRemovedEntities` excludes it — the SURVIVING items' raw values already carry jar's real gap with zero reconstruction. G2's own dense-merge-with-phantom-entries machinery exists because ITS creationIndex stamping is NOT a full 1:1 replay of every jar tick (some port-side creation events genuinely never happen, e.g. a package-endpoint stub); state's threading IS a full 1:1 replay (verified byte-exact on 5 independent samples), so porting G2's more complex scheme would have been unnecessary extra surface area | |
| 2026-07-20 | S7 | Fixed sibling DOCUMENT ORDER (`sortSpecsByCreationIndex`) and the composite-pipeline `<path id>` bug (`buildLevelTransitionGeos`/`svgEndpointId`) in the SAME iteration as mechanism 10's own id-VALUE fix, rather than diagnosing-and-deferring per the mission's own "cheapest first" default | Both were discovered WHILE jar-verifying mechanism 10's own fix on `nelupe-49-xova546` (not a separate investigation) and are the SAME underlying "true creation order" concept the mission's own task framing names — `toutou9` sorting before its own region's pseudo anchor is not a new algorithm, it is mechanism 10's own ordering corollary; the `<path id>` bug is a one-line-per-call-site data-plumbing fix (read the ALREADY-resolved endpoint off `acc.edges` instead of the raw AST `Transition`), not a new mechanism requiring its own diagnosis-from-scratch. Deferring either would have left `nelupe`/`pevene` still showing FALSE id/ordering noise that would have obscured this iteration's own required evidence (which sample category is blocked by WHICH mechanism) | |
| 2026-07-20 | S7 | Did NOT thread `creationIndex` through `state-composite-cluster.ts#buildConcurrentRegionLeaf`'s own synthetic region-as-node id (a CLUSTER-classified composite WITH concurrent regions) | This combination (cluster classification + concurrent regions together) was outside the mission's own 5 required sample categories (plain, nested composite, concurrent regions, removed entities, `[*]` in multiple scopes — all 5 exercised the AUTONOM concurrent case, `combineConcurrentPasses`, never this rarer cluster-classified variant) and it is UNVERIFIED whether jar's real tick for this case shares sub-pattern (a)'s `GroupType.CONCURRENT_STATE` phantom mechanism or is something else — named precisely for a future iteration rather than guessed | |
| 2026-07-20 | S7 | Switched 6 pre-existing `toEqual` assertions on `Transition` objects (`state-global-resolution.test.ts`/`state-dotted-id.test.ts`) to `expect.objectContaining` rather than hardcoding the new `creationIndex` tick value | Per testing.md/diagnosis.md: these tests assert scope/resolution mechanics (which entity a transition binds to), not id-numbering fidelity — hardcoding the exact tick value would over-specify a test that isn't about that field, and would silently break (false-failure) the moment an UNRELATED, correct parser change shifted tick counts elsewhere in the same fixture. `objectContaining` preserves the test's own original intent (asserting `from`/`to`/`length`) while staying agnostic to a field the test was never about | |
| 2026-07-20 | S7 | Split `state-parse-state.ts` (521 lines) and `state-composite-pass.ts` (532 lines) once this iteration's additive edits pushed both over the 500-line cap | Per this mission's own hard boundary ("split when you first touch one") -- extracted the pure, stateless pseudostate/stereotype classification helpers (`state-pseudokind.ts`) and the pseudo-anchor-id/creation-order-sort helpers (`state-composite-pseudo.ts`) into NEW files, re-exported from the original modules so every pre-existing importer keeps working unchanged (mirrors the SAME re-export pattern this codebase already used for prior file-cap splits, e.g. S3's `state-composite-autonom.ts`). Pure moves, zero behavior change -- verified via unchanged census (16/271), unchanged DOT gate (267/267), and unchanged test count (9973/9973) before and after | |
| 2026-07-20 | S8 | Instrumented the exact fixture's svek DOT (`nelupe-49-xova546`'s pinned `svek-3.dot`) through BOTH real `dot -Tplain` AND a minimal `layoutGraph()` probe with IDENTICAL node/edge structure BEFORE writing any fix | Per this iteration's own instruction ("if graphviz-ts matches real dot, the bug is ours") -- the probe showed graphviz-ts's raw spline endpoint (`y=44.51`) fell ~11.5px short of the target node's boundary (`y=56`) while real dot's own spline (via `-Tplain`, same input) reached to within 0.27px of it; tracing WHY led directly to `graph-layout-build.ts#addEdges`'s existing `manualArrowheads` flag (already landed for class in G2 N29) -- state's own `DotInputGraph` construction sites never set it, despite state having switched to inline-polygon arrowheads in mission G4 S1 mechanism 3, the SAME switch that made class need the flag | |
| 2026-07-20 | S8 | Landed `manualArrowheads: true` at all 3 of state's `DotInputGraph` construction sites (`state-dot-graph.ts#buildDotGraph`, `state-composite-pass.ts#runPass`/`buildTopLevelPass`) in ONE change, not incrementally per-site | All 3 sites are the SAME mechanism (state's own arrowhead-rendering convention vs graphviz-ts's default arrow-reservation), not 3 independent bugs -- landing one site at a time would have required 3 separate jar-verification passes for a single root cause already fully derived and verified against real `dot -Tplain` before any code was written | |
| 2026-07-20 | S8 | Rewrote `renderer.ts#buildPathD` to mirror `class/renderer.ts#buildPathData` exactly (1+3n bezier-chain passthrough, straight-`L` fallback), deleting the pre-S8 Catmull-Rom smoothing entirely rather than keeping it as a fallback | Direct inspection of `layoutState()`'s own raw output (BEFORE any rendering) showed `TransitionGeo.points` is ALREADY a well-formed `1+3n` bezier spline for every real dot-layout-driven transition (verified via a probe script, not assumed) -- the Catmull-Rom re-interpolation was discarding correct data and inventing 2-3x too many segments (jar-verified: `nelupe`'s `*start*s7_2-to-chat1`, jar draws ONE segment, pre-S8 port drew THREE). Kept ONLY as a straight-`L` fallback for point lists that AREN'T `1+3n` (hand-built test geometries) -- exactly matching class's own established fallback, not a new invention | |
| 2026-07-20 | S8 | Updated 3 pre-existing `renderer.test.ts`/`renderer-nested-transitions.test.ts` assertions that encoded the OLD Catmull-Rom contract (`M 10,20` with a space; a bare 3-point list producing a smoothed curve) rather than leaving them failing or deleting them | Per testing.md: these tests documented buildPathD's INTERNAL branch behavior, not diagram-level conformance -- the OLD behavior they asserted was the bug this iteration fixed, so the tests needed updating to the NEW (jar-matching) contract, not preservation. Added 2 NEW tests in their place asserting the real 1+3n passthrough shape byte-exact (`C5,30 5,55 5,85`, accounting for the arrowhead trim) | |
| 2026-07-20 | S8 | Used a numeric-tolerant assertion (0.01px, matching `compare.ts`'s own established conformance bar) in `state-manual-arrowheads.test.ts`'s `nelupe` acceptance test, not exact string equality | jar's own computed interior bezier control points for this specific edge (`71.35`, `80.49`) differ from graphviz-ts's own (`71.3548`, `80.4888`) by a few thousandths of a pixel -- both engines agree on the topology (ONE segment) and both endpoints match to within the SAME tolerance this project already uses everywhere else for pinning eligibility (`conformant && dotEqual`); demanding byte-exact equality in a NEW unit test would have imposed a stricter bar than the project's own established ratchet criteria | |
| 2026-07-20 | S8 | Fixed `kilato-12-laso661`'s missing diamond-polygon closing point (`renderer-pseudostate.ts#closeDiamondPoints`) via a LOCAL post-hoc string patch on `diamond()`'s own output, not by adding a `closed?` parameter to `core/svg.ts#diamond` | `core/svg.ts` is shared with `activity`/`chronology` (both also call `diamond()`) and is OUTSIDE this mission's write-set ("Nothing else") -- even an additive, default-`false` parameter would touch a file this mission is not authorized to modify. The local patch is fully contained in `src/diagrams/state/renderer-pseudostate.ts` (in write-set) and does not change `diamond()`'s own behavior for its other 2 callers at all | |
| 2026-07-20 | S8 | Diagnosed (jar-verified, 2 fixtures: `lalava-26-zosi801`, `tegali-39-molu382`) but did NOT land a fix for the CONCURRENT-region bare-name numbering gap (`CONC1`/`CONC2` should be a DIAGRAM-GLOBAL sequence across ALL composites, not per-composite-local -- jar's `StateDiagram#concurrentState` -> `getUniqueSequence2(CONCURRENT_PREFIX)` is a single shared counter) | Confirmed via direct evidence: `tests/oracle/svg-conformance/normalize.ts` strips ALL `data-*` attributes (including `data-qualified-name`) before comparison, so ONLY the rendered `<path id="*start*CONC<n>-...">` string is actually diff-checked -- narrowing the fix's true blast radius to `renderer.ts#localScopeName` alone, not the internal scope-id/qualified-name machinery. Landing it needs a NEW diagram-global counter threaded from parse time (mirroring the `creationIndex`/`pseudoCreationIndex` S7 precedent) through to render time, a moderately-scoped new mechanism affecting exactly 2 known fixtures -- named precisely (including which normalize.ts exclusion narrows its scope) rather than rushed under this iteration's remaining budget, which was prioritized toward the higher-value items the task named next | |
| 2026-07-20 | S8 | Attempted, then abandoned, chasing `state-composite-cluster.ts#buildConcurrentRegionLeaf`'s own unthreaded `creationIndex` gap (S7's own named item) this iteration | Located the only 2 known corpus fixtures combining cluster-classification WITH concurrent regions (`jijuze-43-ceva131`, `zecivu-62-pagu681`, via a direct `classifyDiagram` probe over the full corpus) -- both show 5-6 diffs dominated by UNRELATED, larger sizing/childCount mechanisms (viewBox/height off by 25-138px), meaning the creationIndex threading could not be independently jar-verified in isolation on either fixture this iteration. Per diagnosis.md, a fix cannot be landed without a clean fixture to verify the mechanism against -- deferred rather than guessed | |
| 2026-07-20 | S8 | Declined to chase `<<meblue>>` `StateBorderColor<<X>>` stereotype-scoped skinparam (S6/S7's own named item, `semala-31-joji042`'s sole remaining blocker) this iteration despite it being explicitly named as a next-priority option | Confirmed via direct source read (`core/skinparam.ts`) that stereotype-qualified skinparam keys are deliberately discarded to `unknown[]` for EVERY property except `classBorderThickness<<X>>` (G2 N51, the one modeled precedent) -- landing `StateBorderColor<<X>>` requires extending `resolveSkinparam` (`core/skinparam.ts`) and adding a new `Theme` field (`core/theme.ts`), BOTH outside this mission's write-set ("Nothing else"). Named the exact precedent to mirror (G2 N51's `classBorderThicknessByStereo` mechanism) for a future iteration authorized to touch those shared files | |
| 2026-07-20 | S9 | Sampled ALL 31 near-zero fixtures individually plus 25 from the 4-10 bucket BEFORE choosing which mechanism to land, rather than jumping straight to the task's own priority-2 suggestion (`StateBorderColor<<X>>`) | Per this iteration's own instruction ("sample ALL of them first") -- the full sample surfaced a MUCH richer attribution table than the task's own hint list implied (notes-never-render as a 15-fixture family, the `<style>` cascade widening to 3 sub-families, 2 brand-new single-fixture root causes), evidence that would have been missed by jumping straight to the suggested target without independently verifying the landscape first | |
| 2026-07-20 | S9 | Landed `StateBorderColor<<X>>` ONLY, deliberately excluding the sibling `stateBackgroundColor<<X>>`/`stateFontColor<<X>>`/`stateFontSize<<X>>` keys despite `laferu-31-tice836` needing all three and the marginal code-shape cost looking small | `FontSize<<X>>` is not a pure render-time concern like border color -- it changes MEASURED text width, meaning it would need threading through `state-sizing.ts`'s LAYOUT-time `buildStateGeoTextFields` call (which currently receives a single diagram-wide `theme.fontSize`, not a per-node override), a materially larger and unverified change than a stroke-color lookup. Landing border-color alone kept the blast radius to render-time-only (STATE geometry/dimensions unchanged, `state-dot-parity.test.ts`'s size-backlog ratchet stayed 268/268 throughout with zero risk) | |
| 2026-07-20 | S9 | Threaded `StateNodeGeo.stereotype` through BOTH `GeoSpec` 'state' AND 'autonom' variants (not just 'state'), and through `materializeAutonom` (not just `materializeSpecs`'s leaf branch) | `semala-31-joji042`'s own blocked entity (`state a<<meblue>>`) is a COMPOSITE with concurrent regions, which routes through the 'autonom' `GeoSpec` variant and `materializeAutonom`, not the leaf 'state' path -- verified this BEFORE writing any code (traced `a`'s own classification via `hasLocalContent`/`concurrentRegions.length > 0`) rather than assuming the leaf path alone would suffice, which would have left the actual target fixture unfixed | |
| 2026-07-20 | S9 | Did NOT add `stereotype` threading to the 'cluster' `GeoSpec` variant (`state-composite-cluster.ts#materializeCluster`) | A non-autonom cluster-classified composite already stays on its pre-mechanism-6 dashed-rect fallback shape (S3's own deliberate, unchanged decision) -- its renderer path (`renderer-composite-box.ts#renderCompositeFallback`) has no border-color call site that would consume the field, and no known corpus fixture combines a stereotype-scoped skinparam with a cluster-classified composite, so adding the field there would be unverifiable dead plumbing | |
| 2026-07-20 | S9 | Diagnosed a mid-iteration `parity-state.json` regen showing `semala-31-joji042` as `verdict: diverged` (despite `dotEqual: true` and a byte-exact `DeterministicMeasurer` census pass) as a bash-tooling race, and re-ran the survey once cleanly to confirm, rather than chasing a phantom code bug | Per diagnosis.md's "instrument before hypothesizing" -- ran `renderSync` (the REAL production entry point) directly first and confirmed `stroke="#0000FF"` renders correctly, which is inconsistent with a genuine `@stroke` divergence under a DIFFERENT measurer (measurer choice cannot affect a non-text color attribute) -- pointing at tooling, not rendering. Traced to two overlapping background `svg-parity-survey.ts` invocations from an earlier confused polling attempt (a stale prior-run file existed on disk while the real survey was still mid-flight in a separate shell); killed the stray processes and re-ran singly, confirming `conformant`/`dotEqual: true` cleanly | |
| 2026-07-20 | S9 | Investigated the state hyperlink (`[[url]]`) mechanism (task's own priority-5 suggestion) far enough to discover it needs URL INHERITANCE + a separate anchor-reference (`[[{alias}]]`) resolution path + a NEW `State.url` AST field, then explicitly declined to land any part of it this iteration | The task's own framing ("class already renders URLs... mirror it") undersold the complexity -- direct inspection of `kenuci-20-cane702`'s jar SVG showed `S.a.b` (no own url) inherits `a`'s title/href, NOT `S`'s, meaning a naive "wrap in `<a>` if `node.url` is set" port would be WRONG on the very fixture used to justify "mirror it". Rather than land a partial, verifiably-incomplete mechanism, deferred whole with the concrete evidence recorded so S10 does not have to re-discover the inheritance rule from scratch | |
| 2026-07-20 | S9 | Did NOT attempt the bare `StateBackgroundColor`/`StateBorderColor` (`ELEMENT_BUCKET_SNAMES` 'state' addition) despite confirming it is a genuinely one-line, low-risk change mirroring G3/O1's `'object'`/`'map'`/`'json'` precedent exactly | Re-surveyed all 5 known bare-form fixtures and confirmed EVERY one is dominated by a larger, unrelated bug (mechanism 16's cluster-wrap gap on 4 of them, `xexika`'s own unrelated arrow-marker gap on the 5th) -- meaning the fix's real improvement could not be jar-verified end-to-end (flip any fixture to zero, or even show a clean before/after diff-count delta isolated from the masking bug) on any sample this iteration. Also identified an unresolved scoping question (which of `resolveStateFill`'s 6 call sites should consume the new tier, given initial/final/fork/join have their OWN distinct default colors per an existing doc comment) that would need its own verification pass -- named precisely for S10 rather than landed on inspection-only confidence | |
| 2026-07-20 | S10 | Folded notes into the SAME `StateNodeGeo` array (`kind: 'note'`) instead of a parallel `StateGeometry.notes: NoteGeo[]` field mirroring class engine's own shape | State's `creationIndex`/`sortSpecsByCreationIndex`/`buildStateUidPlan` machinery (S7) is precise enough to reproduce jar's real interleaved document order for free once notes carry a `creationIndex` too — a parallel array would need a SEPARATE document-order/uid-interleave mechanism (class engine's own `notesByHost` heuristic exists BECAUSE class lacks state's exact creation-index threading), which would be strictly more code to build and verify for a WORSE (heuristic, not exact) result | |
| 2026-07-20 | S10 | Derived and implemented the "GMN quark-name" dual-tick mechanism (an ATTACHED note burns one extra `creationIndex` tick before its own) from direct Java source reading (`CommandFactoryNoteOnEntity.java:327`'s `getUniqueSequence("GMN")` + `reallyCreateLeaf`'s own second tick) BEFORE writing any code, then verified the resulting tick counts against 3 independent fixtures' own `id="entNNNN"` values (gedude/xodazu/kujuzo) | Per diagnosis.md and S7's own precedent (creationIndex work always needs fixture-sequence cross-verification, not just Java-reading) — the tick-burn is invisible in rendered output UNLESS you know to check `id=` numbering specifically, so verifying it against real fixture id gaps (not just "the code compiles") was required to trust it | |
| 2026-07-20 | S10 | Did NOT thread an actual `GMN<n>` string anywhere in the port (only the tick-burn count) | Confirmed via direct read of `tests/oracle/svg-conformance/normalize.ts` that `data-*` attributes (including `data-qualified-name`) are stripped before comparison entirely — so the STRING has zero observable effect on conformance, only the TICK COUNT (which shifts subsequent `id=` numbering) does. Building a full quark-name generator for a value nothing ever compares would be unrequested scope | |
| 2026-07-20 | S10 | Scoped this iteration's note rendering to freestanding + attached-to-state (opale-merged) only, explicitly deferring `note ... on link` (6 fixtures), creole/table note content (`fatupo`), `#color`/stereotype overrides on notes, and the composite pipeline's own note materialization | Direct fixture sampling (`fotigo`/`jaxuxe`/`kupexa`/`vateco`/`xupefu`/`tumaba`) showed `note on link` is a STRUCTURALLY DIFFERENT shape (embedded in the transition's own `<g class="link">`, no host `<g class="entity">` at all) — not a small extension of the attached-note opale mechanism just landed. Landing it in the same iteration would have meant building a third, unverified shape under time pressure rather than one well-verified one | |
| 2026-07-20 | S10 | Reused `../class/note-opale.ts`'s pure geometry functions directly (import, no state-local copy) for the Opale zigzag-notch shape | Confirmed the module is entirely diagram-agnostic (byte-exact port of `Opale.java`, no class-specific types beyond a `NoteGeo` type import used only in an unrelated helper this iteration doesn't call) BEFORE reusing it — mirrors `state-render-colors.ts`'s own pre-existing precedent of importing from `../class/` for shared geometry/color helpers, avoiding a ~150-line duplicate port of already-verified geometry | |
| 2026-07-20 | S10 | Fixed `state-note-layout.ts#measureNote`'s note-sizing formula (font size 13 not `theme.fontSize`, margins `6+15`/`5*2` not the old `NOTE_HPAD*2+NOTE_FOLD`/`*1.4`) even though it feeds the FROZEN DOT-structural gate's own node sizing | Verified via `dot-sync-report.ts` (before AND after) that state's structural-EQUAL count stayed EXACTLY 267/267 — the gate measures graph TOPOLOGY (node/edge presence, not exact width/height numbers), so correcting a node's SIZE to match jar cannot move it. Confirmed the derivation against `labono`'s own `textLength=108.4688 -> width=129.4688` (exactly `+21`) and `gedude`'s 2-line `height=36` (exactly `2*13+10`, no `*1.4`) before writing the fix, not by assumption | |
| 2026-07-20 | S10 | Pinned `kilato-12-laso661` alongside the 4 notes-family fixtures despite it having NO note in its source at all | Re-derived independently (via `git stash` A/B against the pre-S10 tree) that it was ALREADY zero-diff before any S10 change — a pre-existing, previously-undiscovered zero-diff fixture surfaced by this iteration's own fresh full-census re-run (not caused by any S10 mechanism). Verified `dotEqual: true, verdict: conformant` in the freshly regenerated `parity-state.json` (AC3) before pinning, same bar as the genuine notes-family pins | |
| 2026-07-20 | S10 | Landed the bare `state`-element `ELEMENT_BUCKET_SNAMES` addition (S9's own named "cheap, may not unlock any fixture" item) scoped to 4 of `resolveStateFill`'s 7 call sites (plain leaf box, composite box, choice, history/deepHistory) via a NEW `resolveStateFillBucketed` sibling function, leaving initial/final/fork/join/syncBar on the unchanged plain `resolveStateFill` | Mirrors the exact scoping S9 named as the open question (`state-render-colors.ts`'s own doc comment already documented which kinds have DISTINCT default colors) — re-verified via the SAME re-survey S9 already did that all 5 known bare-skinparam fixtures remain masked by unrelated bugs (mechanism 16 / arrow-markers), so this lands as a proven, additive, non-regressing mechanism with NO fixture reaching zero from it alone this iteration, exactly as S9's own queue entry anticipated | |
| 2026-07-20 | S11 | Attempted `note ... on link` (task's own priority-1 item), diagnosed and DERIVED a jar-verified position formula (5px fixed offset from the edge's own line, vertically centered on the edge midpoint), implemented full render wiring (geo type, geometry attach, ink-extent, render shape, DOM-order), then REVERTED it entirely before committing | Wiring the render made the childCount/DOM-structure correct but exposed a DEEPER, unrelated pre-existing bug: `graph-layout-build.ts`'s edge `label` attribute is fed to graphviz-ts as RAW TEXT (`attrs.label = a.label`), so graphviz-ts computes its OWN rank-gap reservation from its internal Times-font LUT measurement of the string — our real `labelWidth`/`labelHeight` override (`state-dot-graph.ts#edgeLabelAttrs`) is only ECHOED back into the edge result, never fed into the actual layout call. This under-reserves vertical rank separation whenever the real note/label height (23px, 13pt font + margins) differs from graphviz-ts's own guess, which cascaded into WRONG `State1` box position too (16.5px off), not just the note. Verified via a direct probe (`buildDotGraph`→`layoutGraph` raw output) that this is INDEPENDENT of the `measureLinkNote` formula fix (labelWidth/Height overrides genuinely never reach graphviz-ts's real layout call) — confirmed via `grep` that `graph-layout-build.ts` never emits `labelWidth`/`labelHeight`/an HTML-table label to the DOT text. Landing the wiring regardless would have moved `vateco-92-pece508` from a 9-diff to a 39-diff fixture with ZERO new pins — a real quality regression per-fixture, not the mission's own established "mixed-direction unmasking" pattern (which always came with an offsetting zero-diff gain). Per diagnosis.md ("no fix before a stated mechanism" / "do not guess to make progress"), reverted cleanly (`git show HEAD:<path>` restore, verified `git diff --stat` empty) rather than commit a formula I could not verify byte-exact, and re-scoped to a cheaper, fully verifiable item instead | |
| 2026-07-20 | S11 | Re-scoped from note-on-link to `ceruzi-77-give569`'s pseudostate stroke-color over-application bug (S9-named, already root-caused: `renderer-pseudostate.ts` applied a `#color` override to BOTH fill and stroke on start/end pseudostates; jar applies it to fill only) after the note-on-link revert | Per this iteration's own "cheapest first" priority and per `code-principles.md`'s scope discipline — the fix was ALREADY root-caused by S9 with an exact call-site pinpoint (`renderInitial`/`renderFinal`'s `stroke: fill` should be `stroke: PSEUDO_ANCHOR_COLOR`), a 2-line change with a clear TDD test (fill/stroke divergence under an override), zero interaction with the DOT-graph/layout machinery (pure render-time color resolution), and directly landed on the mission's own priority queue ("pseudostate stroke over-application (ceruzi)") | |
| 2026-07-20 | S11 | Landed `renderInitial`/`renderFinal`'s stroke-color fix (`stroke: fill` → `stroke: PSEUDO_ANCHOR_COLOR`) TDD-first (new `tests/unit/state/renderer-pseudostate.test.ts`, 4 tests, 2 of which failed red before the fix) | Per testing.md — wrote the failing test asserting jar's real behavior (fill overridden, stroke unchanged) BEFORE touching `renderer-pseudostate.ts`, confirmed red, then made the minimal 2-line change to turn it green, jar-verified against `ceruzi-77-give569` (0 diffs) and a second, independently-surfaced fixture exercising the SAME bug (`gepoti-01-sasi356`, `<<end>>` x2 with `#Red`/`#Green`) found by the fresh full census re-run after landing | |
| 2026-07-20 | S12 | Traced the exact library mechanism BEFORE writing any fenced-item code: read jar's cached svek DOT for a labeled edge (`bajelo-54-dixe684/svek-2.dot:11`), confirmed it uses the SAME HTML-table label shape (`FIXEDSIZE="TRUE" WIDTH=".." HEIGHT=".."`) `svek-dot-emit.ts#labelTable` already emits for the DOT-text channel, then read graphviz-ts's bundled `dist/index.js` runtime source (not just its `.d.ts`) to confirm `isHtmlValue()`/`applyLabel()`/`isTblFixed()`/`sizeTableInner()`'s exact `zeroContent` branch, and ran a standalone 2-node/1-edge probe script BEFORE wiring into `addEdges` | Per this iteration's own step-1 instruction ("determine whether the library can accept an explicit edge-label size") and diagnosis.md's "instrument before hypothesizing" — confirmed empirically (not by reading docs alone) that an HTML-table label with FIXEDSIZE/WIDTH/HEIGHT reserves the EXACT declared box regardless of cell content, via a probe showing `rankGap` tracked the declared HEIGHT (52.5 -> 59 -> 136 for HEIGHT 15/23/100) while two different-length PLAIN-text labels produced an IDENTICAL rank gap (52.5, both) | |
| 2026-07-20 | S12 | Used `String.fromCharCode(1)` to build the HTML-string marker prefix rather than a literal control character in source, after a literal single control-character string got silently stripped to an empty string by the file-write pipeline on the first attempt (confirmed via byte-level inspection of the written file) | The raw U+0001 (SOH) control byte is not reliably round-trippable through this session's own tool-call serialization — `String.fromCharCode(1)` produces the IDENTICAL runtime string via a portable, ASCII-only source representation, verified via the SAME probe script before wiring into production code | |
| 2026-07-20 | S12 | Landed the HTML-table label injection in `graph-layout-build.ts#addEdges` (extracted into a new `applyEdgeLabelAttrs` helper to keep CCN under the complexity hook's threshold), TDD-first (2 new tests in `tests/unit/core/graph-layout.test.ts` asserting the rank-gap now tracks the REAL `labelHeight` override, not graphviz-ts's own Times-LUT guess) | Per testing.md — wrote the RED test (asserting a rank-gap for `labelHeight=150` is more than 100 greater than for `labelHeight=15`) BEFORE the fix, confirmed it failed (52.5 vs 152.5) against the pre-change code, then implemented and confirmed GREEN | |
| 2026-07-20 | S12 | Discovered mid-verification (via `npm test -- --run`) that the fenced-item landing regressed 12/268 `state-dot-parity.test.ts` size-backlog entries (all composite fixtures whose dominant content is 1-2 short internal labeled transitions) — root-caused to a PRE-EXISTING, S4-named "composite-internal-labeled-transition ink under-count" bug (`state-composite-autonom.ts#buildPlainAutonomSpec`'s `Math.max(geometry.*, result.*)` floor) whose `result.*` operand is EXACTLY the raw `layoutGraph()` canvas size the fenced item changes | Per diagnosis.md's "trace the divergence to where behavior first departs, not where it surfaces" — instrumented `bajelo-54-dixe684`'s graph-2 wrapper height directly (before: candidate 4.5833in vs oracle 5.5278in, delta 0.9444 = the size-backlog's OWN pinned allowance; after: candidate 4.5486in, delta 0.9792, WORSE) rather than accepting the symptom at face value. Traced the mechanism precisely: graphviz-ts's OLD default-14pt-Times guess for the internal transition's real (smaller, ~11pt) label text happened to OVER-reserve rank gap relative to the real font, which the pre-existing floor bug was silently benefiting from; feeding the REAL (smaller) label height removed that accidental over-reservation, shrinking the ALREADY-undercounting floor further | |
| 2026-07-20 | S12 | Verified the fenced item was architecturally correct and caused ZERO visible regression/gain in the SVG census (`state` 46/271 exact same SET before/after, `class` 303/718, `object` 22/80, `description` 48/355 all unchanged, DOT gate frozen EXACTLY `262/262 - 90/90 - 708/708 - 78/80 - 267/267`) BEFORE deciding whether to revert | Per this iteration's own explicit protocol ("run ALL four censuses + the full test suite... if any protected set shrinks, revert and report the mechanism instead") — confirmed the size-backlog regression (12 fixtures, `state-dot-parity.test.ts`, a protected `npm test -- --run` gate) had NO offsetting SVG-census gain this iteration (no currently-pinned fixture exercises a real guard/action label whose size diverges enough to matter, and `note ... on link` — the mechanism's own intended beneficiary — was not re-wired this iteration), unlike every prior mission mechanism-landing iteration's own "mixed-direction unmasking with a net positive" pattern | |
| 2026-07-20 | S12 | REVERTED the fenced item in full (`git show HEAD:src/core/graph-layout-build.ts`, `git show HEAD:tests/unit/core/graph-layout.test.ts` restore, `git diff --stat` verified empty) rather than land it with the size-backlog regression, or attempt an in-iteration fix of the adjacent S4-named ink-under-count bug | The write-set for `oracle/goldens/state/size-backlog.json` is explicitly "tighten-only" (hard constraint, not a judgment call) — widening it to accommodate the regression was not an option. A full fix of the adjacent ink-under-count bug (`attachTransitionLabel`'s placement formula needs the label's REAL measured width/height reconciled, per its own S4 doc comment) was already attempted once in S4 and reverted for overshooting a different fixture — re-attempting it under this iteration's remaining budget, without a fresh fixture-by-fixture jar-verification pass, would have repeated exactly the "guess to make progress" diagnosis.md prohibits | |
| 2026-07-20 | S12 | Investigated "composite-scoped notes" (S10 item 4) in depth — traced the exact two injection points needed (`materializeCluster`/`materializeAutonom` in `state-composite-geo.ts`, both already have the right `posMap`/`edgePosMap` available) — but did NOT implement it this iteration | A targeted corpus grep (`note` and `{` co-occurring in the SAME `.puml`) found only 4 fixtures in the 271-fixture `dot-cache/state` corpus with this combination: 2 are `note ... on link` (blocked on the fenced item's own future landing), 1 (`joleju-94-maru748`) is a 3-level concurrent-region nesting case far more complex than the materialization gap alone, and 1 (`dajipi-09-doki542`) also needs state/note HYPERLINKS (a separately-named, unbuilt S8/S9 item) to reach zero. With ZERO fixtures reachable by this mechanism ALONE this iteration, and non-trivial implementation risk (`materializeAutonom`'s own note injection would add ink into the SAME `computeSvekResultGeometry` ink-extent walk the fenced item's own regression just demonstrated is fragile), deferred with the exact injection points named for a future iteration that can budget proper jar-verification | |
| 2026-07-20 | S12 | Investigated the "note `#color` parser gap" (S10/S11 queue item, originally framed as a small `NOTE_COLOR` regex fix) via a real fixture render (`kujuzo-76-bavi505`) BEFORE scoping the fix, and discovered the TRUE gap is larger: state's `#color` resolution has NO gradient/`Paint` support at all (a raw `#red` + separator + `yellow` gradient token leaks straight into `fill="..."` unresolved, confirmed via direct SVG output inspection) | Per diagnosis.md — the task's own queue text ("NOTE_COLOR regex non-capturing") undersold the scope; verifying against a real fixture before coding avoided landing a fix that would have looked complete (grammar capture wired through) but still produced visibly wrong output for any gradient-bearing note/state, silently leaving a WORSE-looking half-fix in place | |
| 2026-07-20 | S12 | Scoped this iteration's landed work to SOLID `#color` overrides only (reusing `resolveStateFill` verbatim, the SAME fill-only precedent mechanism 20/23 already established for state boxes/pseudostates), explicitly deferring gradient/`Paint` support for BOTH note fill and state-box fill as a separate, larger, not-yet-built mechanism | Full `Paint` threading needs `parseColor`/`paintToSvg` (`core/paint.ts`, already used by `description`/`class` note stereotype-cascade) wired through EVERY state renderer call site that currently consumes a plain `string` fill (box, composite-box, pseudostate ellipses/diamonds, note) plus a `<defs>`-collection mechanism for the emitted `<linearGradient>` — a multi-file mechanism comparable in scope to mechanism 21 itself, not a one-line grammar fix. Landing the SOLID-color subset alone is correct, additive, and independently jar-verifiable (`fatupo-62-bemu777`'s note fill: `fill="#FFFFFF"` byte-exact against jar for `note right of X #FFF`, even though the SAME fixture stays far from zero-diff due to the separate, still-unbuilt creole/table content gap) | |
| 2026-07-20 | S12 | Made `NOTE_COLOR_CAPTURE` a SEPARATE constant from the existing non-capturing `NOTE_COLOR`, used ONLY in the 4 entity-note rules (10/11/13/14), leaving `note on link`'s rules (12/12b) on the unchanged non-capturing `NOTE_COLOR` | `NOTE_COLOR` is REUSED across both the entity-note family AND `note on link`'s own regexes — making it capturing globally would have shifted `NOTE_ON_LINK_RE`/`NOTE_ON_LINK_MULTI_RE`'s own downstream `match[N]` indices for a feature (`note on link` `#color`) not in this iteration's scope, an unrelated and unverified risk avoided by the separate constant | |
| 2026-07-20 | S12 | Converted `addFreestandingNote`'s trailing 2 positional params (`creationIndex`, `color`) into a single `opts?` object after the complexity hook flagged the 6-positional-param version (PARAM limit) | Mirrors `addNote`'s own pre-existing `opts: {...}` bundling pattern in the SAME file — a one-call-site refactor (only `state-commands-notes.ts`'s single-line freestanding rule + `finalizePendingNote` call it), not a new pattern introduced to the codebase | |
| 2026-07-20 | S13 | Re-landed S12's fenced item (`graph-layout-build.ts#addEdges`'s HTML-sized edge-label injection, `htmlSizedLabel`/`HTML_LABEL_MARK`) exactly as S12 derived it, independently re-verified via a fresh probe script (plain-text label rank gap identical regardless of text length; HTML label rank gap tracks declared HEIGHT) before writing production code | Matches this iteration's own task order: land the fenced item first, TDD, before touching the adjacent ink-under-count bug — re-deriving independently (rather than trusting S12's ledger prose alone) caught nothing new, confirming S12's own derivation was already correct | |
| 2026-07-20 | S13 | Extended `TransitionGeo` with a NEW `labelInk` field (the REAL graphviz-computed label center + measured box, `DotLayoutResult.edges[].labelX/Y/labelWidth/labelHeight`) SEPARATE from the existing `label` field (this port's own render-position approximation, `attachTransitionLabel`'s perpendicular-offset-from-routed-midpoint formula) | A same-iteration probe proved the two positions diverge substantially for multi-edge composites (`bemena-23-zebu249`'s `Configuring`: real graphviz labelX=317.94 vs `attachTransitionLabel`'s own x=51.83, a ~266px gap) — reusing the render-position field for ink purposes would have coupled two independently-verified-different quantities; keeping them separate let each attempt be tested in isolation | |
| 2026-07-20 | S13 | Tried THREE distinct label-ink formulas for `computeSvekResultGeometry`'s ink walk (full box from `attachTransitionLabel`'s render position; full centered box from real `labelInk`; centered box scaled to half-width/half-height) before concluding none is viable | Full box from render position fixed the "0.244904" multi-edge family (bemena/jorere/ketibo/pajefo/xepafa/zitifa, 6 fixtures) but overshot and REGRESSED 10 previously-fine fixtures (beguxu/bunade/dulixa/fadupe/fojisi/gifasa/kejabo/kujaju/mosigo/nuboca). Full box from real `labelInk` position fixed 0 of the 13 originally-failing fixtures (same exact PASS/FAIL set as doing nothing) while INTRODUCING a genuinely NEW regression (`zacajo-09-tamu628`, concurrent regions, PASS 0.1166 -> FAIL 0.1583) not present in the (a)-only baseline. Half-scaled box fixed bemena (0.2866->0.1303, PASS) but broke bunade (0.0141->0.1560, FAIL) and worsened zacajo further (0.2932) — each variant traded one regression set for a different one, never net-improving | |
| 2026-07-20 | S13 | Root-caused the WHY behind the formula instability: compared jar's REAL rendered `<text>` position (from the cached golden SVG, `bemena-23-zebu249`'s own `lnk16` "EvNewValueSaved" glyph: local x=255.86, textLength=111.475, right edge 367.335) against a naive centered box built from graphviz-ts's real `labelX`/`labelWidth` (center 317.94, half-width 60.025, right edge 377.965 -- off by 10.6px) | Two independent, compounding gaps: (1) jar's real `SvekEdge.java` label-PLACEMENT algorithm is NOT simply "centered on graphviz's virtual label-node position" -- it diverges by several px in ways not characterized this iteration; (2) this port's own `StringMeasurer` measures "EvNewValueSaved" ~7-8px WIDER (120.05px) than jar's real Java font metrics (textLength 111.475px) -- a systematic text-measurement calibration gap that compounds any box-based ink formula, since the SAME over-measured width feeds BOTH graphviz's own label-reservation position AND the ink box itself | flag: text-measurement calibration gap is a NEW, cross-cutting finding -- likely affects every diagram type's edge-label sizing, not state-only, needs its own dedicated investigation before any future label-ink-box attempt |
| 2026-07-20 | S13 | REVERTED (a)+(b) in full back to the exact S12 HEAD commit (`git show HEAD:<path>` restore on all 8 touched `src/` files + the 1 touched `tests/unit/core/graph-layout.test.ts`, `git diff --stat` verified EMPTY before proceeding), per this iteration's own explicit protocol ("if the final state widens ANY entry [or, as found here, flips a passing entry to failing], revert both and report the mechanism instead") | (a) cannot be landed without (b) fully compensating for its own 12-13-fixture `state-dot-parity.test.ts` regression (S12's own finding, reconfirmed this iteration byte-for-byte identical: bajelo/bemena/jaxebo/jorere/ketibo/mifuti/nimana/pajefo/pesita/rovese/xepafa/zitifa, PLUS zacajo newly discovered as an (a)-only-adjacent near-miss that only THIS iteration's (b) attempts tipped over); since no tested (b) formula achieves that compensation without a NEW regression, landing (a) alone (or with any of the 3 tried (b) variants) would fail the mission's own protected `npm test -- --run` gate. This is the THIRD independent attempt at this exact mechanism (S4's inline-measurer attempt; S12's fenced item; this iteration's 3 formula variants) to hit the SAME wall -- strong evidence a simple geometric-box ink formula cannot match jar's real `SvekEdge.java` label-placement algorithm without either porting it directly or first closing the text-measurement calibration gap named above | |
| 2026-07-20 | S14 | Landed mechanism 25 (CONC-region bare-name global numbering) as a SEPARATE translation table (`ast.concurrentGlobalIds`, `ParseState.concurrentGlobalCounter`), NOT by changing `concurrentRegionScopeId`'s own numbering | `concurrentRegionScopeId(ownerId, n)`'s existing owner-local number is a load-bearing internal dedup key shared by 4 call sites (`ctx.resolvedRegions` map keys, `__init_`/`__final_` scoped pseudo-anchor ids, `noteScopeId`) — changing its number source risked desyncing build-side vs lookup-side keys. A parallel map populated by a SEPARATE `cpt2`-mirroring counter, consumed ONLY at render time by `localScopeName`, is strictly additive and could not affect internal lookup correctness | |
| 2026-07-20 | S14 | Threaded `concurrentGlobalIds` through `StateGeometry` via a single merge at the end of `layoutState` (`{ ...applyStateDocumentMargin(raw), concurrentGlobalIds: ... }`) rather than through every flat/composite/margin-shift helper | The map is never mutated or read during layout — only carried. A single end-of-pipeline merge avoids touching `layoutFlat`/`layoutComposite`/`applyStateDocumentMargin`'s own signatures for a value none of them consume | |
| 2026-07-20 | S14 | Landed mechanism 26 (`<<sdlreceive>>` folded-frame shape, `EntityImageState2`/`USymbolFrame#drawFrame`) as a NEW `renderSdlReceive` in `renderer-box.ts` dispatched from `renderShape`'s `'normal'` case by `stereotype?.toLowerCase()==='sdlreceive'`, rather than branching inside `renderNormal` itself | The shape is structurally unrelated to `EntityImageState` (no divider line, top-left-anchored label not centered, an extra fold-notch `<path>`, unwrapped in the DOM) — a separate function keeps `renderNormal` a faithful, unmodified port of `EntityImageState` alone, matching this codebase's existing one-function-per-upstream-class convention (`renderEmptyDescription`'s own sibling precedent) | |
| 2026-07-20 | S14 | Corrected `state-sizing.ts#measureSdlReceive`'s previously-`documented-as-unverified` ~12pt width gap by tracing `BodyEnhanced1#getMarginX()=6` (LEFT+RIGHT via `TextBlockUtils.withMargin`) rather than guessing/curve-fitting a constant | The prior doc comment explicitly flagged the formula as unverified against the corpus (1 fixture only) — per diagnosis.md, traced the ACTUAL Java call chain (`USymbolFrame.asSmall` -> `BodyFactory.create2` -> `BodyEnhanced1` -> `BodyEnhancedAbstract#getMarginX`) instead of empirically curve-fitting `+12` to the single known fixture, so the fix generalizes to any future `<<sdlreceive>>` fixture, not just `cekolo-21-gini183` | |
| 2026-07-20 | S14 | Assessed but did NOT land: composite-scoped notes (S10/S12 item 4, `xupefu`/`tumaba`/`dajipi`/`joleju`), maruju's json-table rendering, the `<style>`-cascade `stateDiagram`/`activityBar.fork/.join` families, and `stateBackgroundColor<<X>>`/`FontColor`/`FontSize` (`laferu-31-tice836`) | Composite-scoped notes: re-confirmed (fresh per-fixture check) all 4 known fixtures are STILL cross-blocked by unrelated, separately-named mechanisms (note-on-link x2, 3-level concurrent nesting, hyperlinks) — landing would be UNVERIFIABLE against any corpus fixture, violating diagnosis.md's evidence requirement. Json-table rendering: root-caused (no row-drawing infra exists at all, `renderJson` reuses `renderNormal` verbatim) but is a multi-hour feature comparable in scope to mission's own notes/composite-box mechanisms, not a bounded fix. `<style>` cascade family: the `stateDiagram`-ancestor tier (RoundCorner/Shadowing/BackgroundColor/LineColor/FontColor) and the cross-diagram-type `activityBar {.fork/.join}` selector are BOTH genuinely unbuilt subsystems (mirroring class's dedicated `style-cascade-class.ts`, which is outside this iteration's write-set) — not a small extension. `laferu`: BackgroundColor/FontColor alone would still leave the fixture non-zero because `stateFontSize<<X>>` changes MEASURED text width/box size, requiring per-state font-size threading through `state-sizing.ts`'s measurement functions (a layout-time change, not a render-time color swap) — all three fields are load-bearing together, not independently landable | flag: all four items are legitimately larger-than-"cheap" despite appearing in the non-blocked queue; queued precisely for a future iteration with matching budget |
| 2026-07-21 | S15 | Sampled the FULL `<<entrypoint>>`/`<<exitpoint>>` corpus family (20 fixtures via a full-corpus grep) rather than trusting S14's own 1-fixture sample before deciding whether to attempt the render shape | S14 named the item without checking for a mechanism-16 cross-block; a fresh `hasBorderPointDescendant` code read plus a render probe on the named target (`lulozu-10-bopu547`) proved the owning composite ALWAYS takes the parked non-autonom `cluster` fallback — confirming landing the ellipse shape alone would reach zero on ZERO fixtures, before spending any implementation time on it | |
| 2026-07-21 | S15 | Landed `o-->`/`x-->` (mechanism 27) even after confirming it produces 0 net pins (the fixture's own self-loop routing residual, unmasked by fixing childCount) | The machinery is genuinely correct, jar-verified byte-exact for its own scope (decoration geometry + the id-separator bug it also surfaced), TDD, and non-regressing — matches the mission's own established S10/S12 precedent for landing verified-but-non-pinning infrastructure rather than leaving a parsed-but-dead `Transition.crossStart`/`.circleEnd` field in place | |
| 2026-07-21 | S15 | Derived `ExtremityArrowAndCircle`/`ExtremityCircleCross`'s rendered geometry EMPIRICALLY (byte-matching jar's own SVG numbers) rather than trusting a literal translation of the Java source's own `angle - PI/2`/`angle + PI/2` rotation composition | A first literal-translation attempt at the algebra produced numbers that did NOT match jar's own ellipse/polygon coordinates; re-deriving from the OBSERVED bytes (which point-for-point matched treating the shape as "identical to plain ARROW, rotated by `angle` directly") was faster and more reliable than debugging the Java's own multi-step angle-adjustment chain across two collaborating classes (`ExtremityFactoryArrowAndCircle`'s `angle - PI/2` feeding the constructor's own `angle + PI/2`) | |
| 2026-07-21 | S15 | Ported `ExtremityArrowAndCircle`/`ExtremityCircleCross` as STATE-LOCAL pure functions over `core/svg.ts` primitives rather than extending `core/svek/extremity/link-decor.ts`'s `LinkDecorName` union | That file's own doc comment explicitly documents both decors as UNREACHABLE from the description-diagram grammar it serves, and the file itself is outside this mission's write-set (`src/core/svek/**` not listed) — mirrors this codebase's established per-module-duplication convention (`renderer-composite-box.ts`'s own `compositeHeaderPath`, `renderer-pseudostate.ts`'s own diamond-closing patch) | |
| 2026-07-21 | S15 | Fixed the `Link#idCommentForSvg` id-separator bug (`-to-` vs bare `-`) in the SAME commit as mechanism 27, rather than deferring it | Discovered WHILE jar-verifying mechanism 27 against `xexika`'s own self-loop (`id="foo-foo"` vs the port's `id="foo-to-foo"`) — the SAME diagnosis session, the SAME 2-line fix location (`buildTransitionInnerMarkup`'s id-building expression), and the fixture cannot be jar-verified byte-exact without it. Splitting it into a separate task would have re-required re-deriving the exact same context | |
| 2026-07-21 | S15 | Landed `stateBackgroundColor<<X>>`/`stateFontColor<<X>>` (mechanism 28) even though it produces 0 net pins on the one known fixture | Mirrors S9's `StateBorderColor<<X>>` mechanism almost line-for-line (same regex/accumulator/graphOverride shape, same theme-field pattern, same render-time precedence-tier consumer) — genuinely bounded per this iteration's own instruction to "re-check whether cheap now", jar-verified correct on the 2 fields it covers (background fill + text fill both byte-match jar), and sets up `stateFontSize<<X>>` (S16+ item) to zero the fixture with no further re-derivation needed | |
| 2026-07-21 | S15 | Did NOT thread `stateFontColor<<X>>` into `renderEmptyDescription`/`renderUnmeasuredFallback`/`renderSdlReceive`'s own hardcoded text-fill calls, only `renderNormal`'s two `renderTextLines` calls | `laferu-31-tice836` (the only known fixture) exercises `renderNormal`'s ordinary headerLines path only — threading the other 3 call sites would be unverified-against-any-fixture scope creep, matching diagnosis.md's evidence requirement | |
| 2026-07-21 | S15 | Wrote `tests/unit/skinparam.test.ts` additions FIRST (the conventional home for a `core/skinparam.ts` test, matching `statebordercolor<<X>>`'s own existing test location), then caught the write-set violation via a `git status --short` audit before finishing, reverted via `git show HEAD:` restore, and re-homed the equivalent 5 tests into a NEW `tests/unit/state/state-skinparam-cascade.test.ts` | The mission's write-set explicitly lists `tests/unit/state/**` and `tests/unit/core/**`, NOT the top-level `tests/unit/` directory `skinparam.test.ts` actually lives in — a hard boundary, not a judgment call. Re-homing (rather than skipping the tests) preserves TDD coverage of the new parsing branches while staying strictly inside the declared write-set | |
| 2026-07-21 | S15 | Traced `<style>` `activityBar {.fork/.join}`/`arrow {LineColor/HeadColor}` to `core/style-map-theme.ts#applyStyleMap` as the exact (and only) merge point, then stopped without attempting a workaround inside the write-set | Confirmed by reading EVERY existing diagram-type-specific `<style>` selector handler (json/yaml/hcl's own `arrow`/separator overrides) — all three are hardcoded `if` blocks inside `applyStyleMap` itself, with NO generic per-diagram-type dispatch mechanism a write-set-compliant file could hook into. Any workaround (e.g., re-deriving the override inside `render-fixture-state.ts` or `renderState` itself) would either duplicate `applyStyleMap`'s own selector-matching logic outside its established location (a maintainability regression) or require a NEW `styleMap` parameter threaded through `layoutState`/`renderState`'s public signatures AND `src/index.ts`'s production dispatch (also outside the write-set) — both correctly out of scope, named precisely rather than attempted | |
| 2026-07-21 | S15 | Root-caused `maruju-55-soko478`'s json-table content to full shape precision (rx=2.5, title+2-divider+per-cell layout) via direct golden-SVG inspection, but did not implement it | `state-json-sizing.ts#MeasuredJsonState` already exists (proven correct for DOT sizing, `dotEqual:true`) but exposes only aggregate width/height, no per-row/per-cell positions — implementing the render shape would require a genuinely new row-layout pass, comparable in scope to mechanisms 5/6/21 (box/composite-box/notes), not a bounded fix within this iteration's remaining budget | |
| 2026-07-21 | S15 | Resolved a recurring `svg-parity-survey.ts` overlapping-invocation tooling hazard (same class as S9/S14) via `pkill -f svg-parity-survey` + a `git status --short` corruption check + exactly one clean re-run, twice | The script only writes its `--out` file atomically at the very end of a full run, so a killed mid-run process cannot corrupt the existing file (verified via `git status --short` showing an empty diff before each re-run) — `svg-conformance-census.ts` (independent of `parity-state.json`) was used for every authoritative census number this iteration regardless, so the incident had zero effect on any reported result | |
