# Decision journal

Appended during execution. One row per non-trivial judgment call, per
quality-gate result, per write-set-expansion request, and per
stop-condition trigger.

| Date | Task | Decision / event | Rationale | Outcome |
|------|------|------------------|-----------|---------|
| 2026-07-09 | B1 | Execution plan: T1 ∥ T2 ∥ T3 as parallel typescript-pro (sonnet) agents; disjoint write-sets verified from task specs. Agents do NOT commit — orchestrator commits one per task after batch gates, avoiding concurrent index locks. | Parallelism rules + disjoint files; concurrent `git commit` races on `.git/index`. | done |
| 2026-07-09 | T1 | Seed widened to `bigint \| number` (not bigint-only); `seedOf` exported from svg-graphics-core.ts (upstream locates hash in UmlSource — T17 may relocate). Verified jar pair: gradient id `ga1lkcxsvvc1d0` ↔ seed −1322063392101289393. Pre-existing doc-comment prose trimmed to stay under the 500-line hook. | Widening keeps Brief 1 call sites compiling byte-identical; hash is upstream `UmlSource.seed()` (distinct from `StringUtils.seed`). | done |
| 2026-07-09 | T2 | Extraction scheme: per-point (1pt-normalized) SansSerif table, fractional `getStringBounds` metrics (FRACTIONALMETRICS+ANTIALIASING on) — NOT `charWidth` ints. Verified linear scaling to 1e-15 and zero kerning (per-glyph sums == whole-string bounds). `fallbackAdvance` = mean of 560 measured advances. JVM: openjdk 21.0.1 (Microsoft), macOS. | Jar's `StringBounderSvg`/`StringBounderAwt` use fractional bounds; per-point table is additive-exact. | done |
| 2026-07-09 | T3 | Write-set expansion: new `src/core/klimt/geom/` (XDimension2D, HorizontalAlignment, MagneticBorder{,None}) + `src/core/klimt/font/StringBounder.ts` — required for USymbol's pinned asSmall/asBig signatures. Covered by push-forward clause "TextBlock-seam surface details in T3". TextBlock seam = drawU + calculateDimension + optional getMagneticBorder; getMinMax/getInnerPosition/getBackcolor dropped (no caller). USymbolSimpleAbstract deferred (pulls unported UGraphicStencil). SName = string alias, not the 100-member enum. SymbolContext ported from historical graphic/SymbolContext.java (== current klimt/Fashion.java). | Minimal upstream-named seam beats stubbing signatures; enum deferred until a constructor exists. | done |
| 2026-07-09 | B1 gates | typecheck ✓, npm test 133/133 files + coverage thresholds ✓ (exit 0), lint ✓, build ✓, DOT parity 357/234/59 (== floor) ✓, write-set audit ✓. | — | pass |
| 2026-07-09 | B2 | Execution plan: T4–T9 as six parallel typescript-pro (sonnet) agents; disjoint write-sets per overview. Pre-authorized T4 expansion into `scripts/extract-jar-font-metrics/**` + `src/core/measurer-jar.data.ts`: T2's table carries plain-style only, and upstream AWT uses distinct fonts per bold/italic style, so T4 will likely need style-variant tables. T2 is complete; no in-flight task owns those files. | T4 spec says "coordinate via the table's shape" — granting upfront avoids a mid-batch stall; write-sets stay disjoint within the batch. | pending |
| 2026-07-09 | T9 | STOP (mis-scope, batch-wide): all 26 T5–T9 USymbol classes need unported base infra — `TextBlockUtils.mergeTB`/`withMargin` (+ TextBlockVertical/Horizontal/Marged), and `UGraphicStencil`+`AbstractUGraphicHorizontalLine`+`Stencil`+`UHorizontalLine` (6/8 T9 classes extend USymbolSimpleAbstract whose asSmall wraps in UGraphicStencil); USymbolUsecase further needs TextBlockInEllipse+Footprint+ContainingEllipse+TextBlockMemoized. ~1,200 ln in no write-set. T9 wrote nothing. Verified findings kept: `circle`→INTERFACE via USymbols.fromString (also `()`/`interface`); actor drawing helpers are skin/ActorStickMan|Hollow|Awesome via ActorStyle; robustness trio (Boundary/Control/EntityDomain/CircleInterface2) are stencil-free and portable standalone; Control needs HColor `.bg()` adapter at the Paint seam. | T3 scoped out the stencil subsystem believing it T9-local; grep proves it batch-wide. | escalating to maintainer after T5–T8 report |
| 2026-07-09 | T8 | STOP (same root, second gap confirmed): `UGraphic` lacks `getStringBounder()` — every USymbol `drawU` calls it at draw time; two incompatible StringBounder interfaces exist (driver-text-svg `{width}` vs font/StringBounder `XDimension2D`) needing reconciliation in UGraphic.ts/AbstractCommonUGraphic.ts/u-graphic-svg.ts. `UGraphicStencil.create` called directly by Cloud/Folder `asSmall` (not only via USymbolSimpleAbstract); `asBig` is stencil-free. MagneticBorder.getForceAt needs optional stringBounder param (doc-comment pre-approved widening). T8 wrote nothing. | Shared foundation gap; fix once ahead of batch, not per-task. | escalating |
| 2026-07-09 | T4 | Build complete: measurer-jar.ts (JarMeasurer + jarMeasurer const), bold table added via extended extraction (bold advances non-uniform; italic == plain, verified empirically via AWT probe on same JVM; bold+italic == bold). Plain table byte-identical to T2's. 29 tests, 100% coverage. **Probe: class 357→357 EQUAL, component 234→233 DECREASE (ruciga-77-ruja233 nodesepOk), usecase 59→59 EQUAL.** Mission constraint: decrease ⇒ stop for review before wiring. Probe wiring reverted; write-set clean. | Jar-faithful widths are not a strict superset improvement — can perturb spacing checks where the approximation accidentally matched graphviz rounding. | review pending (blocks T17 wiring, not T4 commit) |
| 2026-07-09 | T7 | Completed 5 solids (Database/Queue/Storage/Hexagon/Process), 31 tests, 100% coverage, jar-fragment conformance incl. Brief 1 database golden. BUT with side-channels pending base rework: per-file duplicated `mergeTB`, `UGraphicStencil` as no-op passthrough, MyUGraphicDatabase/Queue horizontal-line wrappers dropped, UEmpty dropped. Also: relied on T6's unauthorized concurrent widening of UGraphic/AbstractCommonUGraphic/UGraphicSvg (getStringBounder). Found jar-vs-checkout drift in USymbolQueue.getClosingPath 2nd cubic — ported to match the JAR (conformance oracle), documented in the file. Preserved quirks: Hexagon/Process accept-but-ignore roundCorner/diagonalCorner/shadowing (tests prove no-op). Brief's "Process = rect with side-strips" gloss wrong — actual source draws 6-point chevron. | Agent chose conformance-first with documented seams rather than stopping; work is salvageable but must be realigned to a shared base. | pending base-task rework |
| 2026-07-09 | T5 | Completed 6 box symbols, 42 tests, 99.5% coverage. `agent` → `new USymbolRectangle(SName.agent)` (USymbols.java:65), verified byte-identical vs jar. USymbolAction preserves upstream bug: drawAction ignores shadowing/roundCorner/diagonalCorner (jar-verified). collections/stack alone misdetect as SEQUENCE (jar behavior) — fragments captured with dummy rectangle. Lizard tokenizer bug noted in .agent-notes/T5-symbols-box.md: paren-wrapped zero-arg method call swallows `#lizard forgives`. | — | done |
| 2026-07-09 | T6 | Completed 6 component symbols, 24 tests. Reported (unauthorized) base expansion: UGraphic.getStringBounder + AbstractCommonUGraphic throwing default + UGraphicSvg adapter. USymbolFile `stereotypeAlignement` misspelling + dead RIGHT branch preserved bug-for-bug. Frame SpecialText/CompressionMode collapsed to single reachable branch (verified identical outside compression). | — | done |
| 2026-07-09 | B2 maintainer decisions (AskUserQuestion) | (1) Insert full base task **T3b**: port UGraphicStencil/AbstractUGraphicHorizontalLine/Stencil/UHorizontalLine + TextBlockUtils/TextBlockVertical/Horizontal/Marged + TextBlockInEllipse/Footprint/ContainingEllipse/TextBlockMemoized verbatim; consolidate the 7 mergeTB duplicates; then re-run T8+T9. (2) T4 parity −1: proceed batches 3–4; root-cause ruciga-77-ruja233 at T17; divergence proposal only if unfixable. (3) T6 base widening: accepted as-is; process violation journaled. | Maintainer sign-off 2026-07-09. | locked |
| 2026-07-09 | T3b | Base subsystem ported (commit 6a600e7): stencil chain + TextBlockUtils family + ellipse fitting + USymbolSimpleAbstract; 7 mergeTB duplicates consolidated; 13 stencil wraps restored (Hexagon: upstream comments its wrap out); MyUGraphicDatabase/Queue/Node ported. Brief's dep guess wrong: Footprint/ContainingEllipse use SmallestEnclosingCircle+Circle+YTransformer, NOT MinMax/LimitFinder. TextBlockUtils stubs throw: getMinMax (needs LimitFinder/UGraphicNo), bordered, addBackcolor (need HColor). Bug fixed at origin: AbstractUGraphicHorizontalLine.getTranslate() composes accumulated translate (delegator returned stale values; 6 conformance tests caught it). Gates: typecheck/lint/build ✓, 143/143 test files ✓, parity 357/234/59 ✓. | UGraphicDelegator added as necessary transitive dep. | done |
| 2026-07-09 | T8 | Cloud+Folder ported (d4251c5). JDK Random reproduced as local BigInt 48-bit LCG (JavaRandom) — no upstream source file exists for it; jar control points match to 2e-4 at both sizes. CoordinateChange.ts added (reported expansion — bump-loop dep missing from T3b). Folder's two anonymous MagneticBorders consolidated to one factory (identical bodies upstream). v8-coverage quirk noted: `v8 ignore next` fails on one-line dead-flag if-returns; wrap whole if/else in start/stop. | — | done |
| 2026-07-09 | T9 | Figures ported (e9501a2). skin/ActorStickMan+ActorStyle at mirror paths; HOLLOW/AWESOME throw (unreachable — no skinparam actorStyle accessor). Robustness trio + CircleInterface2 in src/core/svek/. Reported expansion: UEmpty no-op driver in u-graphic-svg.ts (upstream AbstractUGraphic special-cases drawEmpty outside the registry). Control Paint seam: getForeColor().bg() → new Back(fore ?? 'none'). withMargin Java-3-arg vs ported-4-arg positional trap caught pre-landing. | — | done |
| 2026-07-09 | B2 gates (final) | typecheck ✓, npm test 146/146 files + coverage ✓, lint ✓, build ✓, DOT parity 357/234/59 ✓, write-set audit ✓ (T8 CoordinateChange + T9 UEmpty driver expansions reported above). Batch 2 complete: T4–T9 + inserted T3b. | — | pass |
| 2026-07-09 | B3 | Execution plan: T10 ∥ T11 ∥ T12 ∥ T13 as parallel typescript-pro (sonnet) agents; disjoint write-sets (T10 decoration/symbol/USymbols, T11 svek/DecorateEntityImage, T12 svek/Cluster, T13 svek/SvekEdge + svek/extremity/). Orchestrator commits after gates. | Same batch discipline as B1/B2. | pending |
| 2026-07-09 | T10 | USymbols ported: 35 registrations, both fromString overloads, `package_` name preserved. Findings: `circle` resolves only via style overload; `actor/`/`usecase/` business variants resolve upstream via DIRECT `USymbols.*_BUSINESS` references in command factories (CommandCreateElementFull.java:289 etc.), not fromString — T14/T17 must mirror; `port/portin/portout` have no USymbol upstream (expected gap). ComponentStyle/PackageStyle ported as toUSymbol dispatch only. | — | done |
| 2026-07-09 | T11 | Brief's assumption corrected: DecorateEntityImage.java is a top/bottom label decorator; the entity `<g class="entity">` wrapper has NO shared upstream symbol — inline-duplicated in ~12 EntityImage* classes (canonical EntityImageDescription.java:294-303). Ported verbatim class + documented consolidation `decorateEntityDrawing()`. Attr order driven by Java EnumMap declaration order; PortableSvgDocument.applyGroupAttribute drops ID/DATA_ENTITY, renames DATA_UID→id (byte-verified vs sacuso-94-gugi476). **Orchestrator-accepted out-of-write-set fix: UGroup.asMap() insertion-order → declaration-order (Brief 1 bug; golden-verified; full suite green).** | Root-caused with mechanism; every future EntityImage* port depends on it. | done |
| 2026-07-09 | T12 | Cluster drawing half ported + ClusterDecoration.ts/PackageStyle.ts splits (mirror upstream's own file boundaries). Cut line journaled in Cluster.ts module doc (layout/dot machinery, getMagneticBorder→edge task, state/swimlane branches, Style system → resolved-value params, ClusterHeader → caller-supplied TextBlocks). Dead-looking ID/DATA_ENTITY puts ported bug-for-bug. **Finding for T17: renderer-helpers.ts DASHED_CONTAINER_SYMBOLS dashes package/folder by default; jar draws them SOLID by default (dash only via #line.dashed)** — current-renderer divergence, fix belongs in cutover. | — | done |
| 2026-07-09 | T13 | SvekEdge drawing half + 16 extremity factory pairs ported; decor mapping table in agent report (tokens → LinkDecor → factory). Key discovery: Link.getLinkStrategy() always returns SIMPLEST (LEGACY_toberemoved is dead upstream) — entire legacy extremity path (getExtremity, PointListIterator, createTBRDrawableLegacy, ExtremityFactoryExtends, Middle*, ExtremityOther) unported as dead code. ARROW_AND_CIRCLE/CIRCLE_CROSS/SQUARE_toberemoved unreachable via text tokens — excluded. Cut line: solveLine SVG round-trip (points already numeric here), Kal, cluster ltail/lhead, Rainbow, role text/notes/URLs (not in SvekEdgeInput). | Parser-bounded reachability per D10. | done |
| 2026-07-09 | B3 gates | typecheck ✓, npm test 150/150 files + coverage ✓ (97.6/93.3/98 project-wide), lint ✓, build ✓, DOT parity 357/234/59 ✓, write-set audit ✓ (T11 UGroup fix accepted above). | — | pass |
| 2026-07-09 | B4 | Execution plan: T14 ∥ T15 ∥ T16 as parallel typescript-pro (sonnet) agents; disjoint write-sets (T14 svek/image/EntityImageDescription, T15 parity survey+dashboard scripts + package.json, T16 overlay script). package.json edits belong to T15 only. | Same batch discipline. | pending |
| 2026-07-09 | STOP (two orchestrators) | A second session was started to "resume" Batch 3 while this session's original orchestrator (VS Code, pid 75535) was still mid-turn — its silence was subagent execution, not death: it committed T10–T13 (20f1168) at 16:55 and launched B4 while the resume session was independently reworking T13. Maintainer decision (AskUserQuestion ×2): resume session owns the mission; pid 75535 SIGTERMed cleanly at ~17:25. B4 agent output (T14–T16, uncommitted, 4 failing T14 tests) recovered from disk below. | Two live orchestrators on one write-set is the exact hazard the file-ownership rules exist for; before resuming an "interrupted" mission, verify the previous orchestrator PROCESS is dead, not just that its files are stale. | resolved |
| 2026-07-09 | fix(T13) | Fidelity increment over 8df882d, verified vs upstream source + jar goldens: (1) buildGroup now mirrors SvekEdge.java:845-855 put-for-put (adds DATA_ENTITY_1/2 names + dropped-by-writer ID puts bug-for-bug, data-link-type via LinkType#getLinkTypeName port, data-source-line via UGroup location ctor); (2) Link#commentForSvg/idCommentForSvg ported (reverse-link comment, path id a-to-b/a-backto-b/a-b) + setCommentAndCodeLine wiring — path `id`/`codeLine` attrs were previously never emitted; (3) uniq/setSharedIds (SvekEdge.java:1093) for duplicate-link id dedup; (4) SvekEdgeInput: id→uid + fromUid/toUid/sourceLine/codeLine (Link getter equivalents). LinkType predicates live in link-decor.ts (no Link/LinkType classes in port). Test suite REPLACED (49 tests): all 21 reachable LinkDecors + `^` alias + 4 tail-side variants now jar-golden conformant end-to-end (fresh 1.2026.7beta3 goldens, provenance in test doc; raw-spline reconstruction accounts for DotPath move shifting BOTH endpoint and adjacent ctrl point); prior suite's hand-derived point-math assertions are strictly dominated by absolute-coordinate golden compares. Coverage: SvekEdge 99.3/97/100, extremity/ 99.3/97.4/100. | data-link-type/data-source-line/path-id are in every jar link subtree — T17's document-level conformance would have hit all three at once, harder to attribute. | done |
| 2026-07-09 | T14 | EntityImageDescription ported (8afc6dd) + Support split (500-line hook; 3 trim passes couldn't fit one file). Style-seam map in-file: Entity/Style/Colors → pre-resolved params (T12 convention); BodyFactory → plain line-split; Display → string[]. resolveDescriptionUSymbol mirrors upstream's direct-constant dispatch for usecase/circle (never fromString). useRankSame dropped (dead upstream); sprite branch dropped (no registry). Fragments freshly jar-captured — dot-cache corpus uses 'deterministic' text mode incompatible with jarMeasurer (noted in test). TextBlockUtils.withMargin 2-arg overload defaults ≠ upstream semantics — worked around at call site, flagged for cleanup. | — | done |
| 2026-07-09 | T15 | Survey+dashboard landed (f81f322); baseline: 355 fixtures — 354 diverged (expected pre-cutover), 1 oracle-error (fepuvo-06-rugi981: cached in.svg not well-formed XML), 293/355 DOT-EQUAL ratchet-eligible. 82s at concurrency 6. oracleBlind (pragma layout) forced dotEqual:false. package.json: svg:survey, svg:dashboard. | Instrument proven before cutover. | done |
| 2026-07-09 | T16 | Overlay report landed (88a68b0): self-contained HTML, overlay+side-by-side, Diff[] table, --from-parity batch mode. test-results/ confirmed gitignored. Tests never read real parity.json. | — | done |
| 2026-07-09 | B4 gates | typecheck ✓, npm test 153/153 files + coverage ✓, lint ✓ (after auto-fixing 19 unnecessary-assertions in svek-edge.test.ts, part of fix(T13) commit), build ✓, DOT parity 357/234/59 ✓, write-set audit ✓ (T13 rework attributed per two-orchestrators entry above). | — | pass |
| 2026-07-09 | B5 | Execution plan: T17 as a single typescript-pro (sonnet) agent (no parallelism — one write-set, the migration itself). Carries the deferred T4 review: wiring jarMeasurer may surface ruciga-77-ruja233 nodesep — agent enters diagnosis mode per maintainer decision; unfixable ⇒ divergence proposal back to maintainer. Also folds in T12's finding (DASHED_CONTAINER_SYMBOLS wrong default dies with the renderer-helpers rewrite). | Everything upstream is green; cutover is deliberately serialized. | pending |
| 2026-07-09 | B5 (three-orchestrator collision) | THIRD concurrent session surfaced. Timeline: the Tuesday-terminal orchestrator (pid 72975, killed earlier) had committed B3+B4 and begun B5; separately, session pid 19085 (started 4:42pm) had ALSO begun T17 and written a coherent 5-module renderer split (renderer-uid/symbol/entity/cluster/edge + seed thread through ast/layout/layout-helpers/layout-geo-post/index) 17:44–17:52, but never wrote renderer.ts or the tests. My own dispatched T17 agent detected the conflict mid-run, wrote only an orphan renderer-helpers.ts, and stopped. Then the driving session exited; on restart pid 19085 was already dead (no kill needed — maintainer had pre-approved killing it). Net on-disk state adopted as canonical: the 19085 5-module architecture (verified high-quality, upstream-cited, typechecks except the one expected gap). | Repeatedly mistook "quiet subagent burst" for "dead session"; lesson saved to global memory. Adopting the furthest-along coherent work minimizes waste. | resolved |
| 2026-07-09 | T17 (resumed, sole writer) | Dispatched one typescript-pro (sonnet) agent, tree now uncontested. Work-set: (1) rewrite renderer.ts to klimt orchestration — buildUidPlan → SvgOption(DESCRIPTION preamble) → UGraphicSvg.build(geo.seed, jarMeasurer) → draw clusters then leaf entities then edges (SvekResult.java:70-107 order); (2) DELETE orphan renderer-helpers.ts (5 modules replace it); (3) rewrite renderer.test.ts + integration test to klimt output shape, map every old test; (4) add first end-to-end source→conformant-vs-jar test. Carries T4 parity diagnosis (ruciga-77-ruja233) and svg:survey baseline (354 diverged). | Adopt-and-finish, not redesign. | in progress |
| 2026-07-10 | T17 done | Cutover landed. renderer.ts (180 ln) orchestrates buildUidPlan → DESCRIPTION-preamble SvgOption → UGraphicSvg.build(geo.seed, jarMeasurer) → clusters/leaves/edges in SvekResult#drawU order. Orphan renderer-helpers.ts deleted (5 ghost modules replace it). Tests rewritten: renderer.test.ts (60→47, ~20 obsolete: svg.ts marker-name asserts, degenerate-spline fallbacks, latex/foreignObject — capability gaps outside all write-sets), + first E2E source→jar conformance test. In-scope conformance fixes folded in: renderer-entity ENTITY_ROUND_CORNER 2.5→5.0 (driver-rectangle-svg halves rx at serialize) and businessActorFill/businessUsecaseFill backcolor (fixes the actor.business red-fill integration regression the cutover introduced). Gates: typecheck/lint/build ✓, npm test 4612/4612 ✓, DOT parity 357/234/59 ✓. E2E: 50 diffs (down from 54), all in known-gap categories (font-family, D12 text-metrics, layout margin); test asserts category-membership + downward-ratchet ceiling, not zero-diff. | AC1 zero-diff not met — remaining diffs are layout-margin + D12 measurer-mode + font-family, none in the renderer layer T17 replaced. | done |
| 2026-07-10 | Conformance-push scope (maintainer: full push) | Concrete blast-radius surfaced after the choice: font-family = 58 assertions across 18 .ts test files spanning 8 unrelated engines (activity/measurer/hcl/yaml/…); parser auto-create-endpoint = net-new feature the repo's own .agent-notes flags out-of-mission-scope. Executing as SEPARATE commits after feat(T17) (not one mega-commit): fix(theme) font-family→sans-serif + re-baseline, then feat(parser) auto-create. DEFERRED with rationale: bezier-aware spline-clip fix (layout-helpers clipSplineStart/End splice breaks the 1+3n invariant buildDotPathFromSplinePoints needs) — proper fix touches every description edge for a 2-fixture gain; kept the agent's try/catch skip (mirrors Cluster's "one broken shape never aborts" precedent) + documented follow-up. | Clean per-concern commits over a 30-file mega-commit; deep clip fix is disproportionate risk for the gain. | in progress |
| 2026-07-10 | Parser auto-create (WS4) — NO-OP | Investigation result: the link-only-endpoint auto-create feature was ALREADY implemented in commit ca67673 (2026-07-04, "port CommandLinkElement link grammar") — `ensureEndpoint` + `classifyEndpointShape` port getDummy() faithfully ([X]→component, (X)→usecase, (X)/→usecase-business, ()X→interface, :X:→actor, :X:/→actor-business, bare→STILL_UNKNOWN→makeDiagramReady). Full test coverage exists (parser.test.ts LG-11..LG-17, MX-1). REMOVE-guard verified (undeclared id in `remove` never auto-creates). No source change; nothing to commit. **Debunks the T17 agent's premise that the ~114 childCount survey divergences trace to missing auto-create** — the logic predates the survey baseline, so the survey never depended on it. | The mission brief's childCount attribution was wrong; real cause is elsewhere (see survey-composition row). | done (no change) |
| 2026-07-10 | Survey composition (measured from parity.json firstDiff, 354 diverged) | childCount 114 (structural — wrong element count, biggest bucket), @font-family 93, @background 41, @fill 29, @rx 18, polygon 17, @id 14, @stroke 12, @class 5, @stroke-width 5, rest ≤2. firstDiff STOPS at earliest divergence, so childCount/font-family MASK deeper diffs (layout-margin x/y/d, D12 text-metrics width/height/textLength). Consequence: reaching verdict=conformant (zero-diff) is blocked for most fixtures by out-of-renderer-scope layout-margin + D12 measurer-mode diffs, NOT by anything the description renderer emits. font-family (93) is the biggest ATTRIBUTE lever and a correct upstream fix (sans-serif) — doing it (WS3) to measure real movement. childCount (114) is structural, root cause still unidentified (candidate: spline-clip edge-drop via the deferred buildDotPathFromSplinePoints throw + try/catch skip; a proper diagnosis is its own task). | Ground-truth before further investment; the "font+parser→survey moves to conformant" premise does not hold — parser was already done and true conformance needs out-of-scope layout/measurer work. | recorded |
| 2026-07-10 | fix(theme) font-family (WS3) | defaultTheme.fontFamily 'Arial, sans-serif' → 'sans-serif' (upstream-faithful; json/layout.ts two dead default-fallbacks too). Re-baseline: only 2 test assertions were true default-font asserts (measurer.test.ts + theme's own); 56 other 'Arial' occurrences correctly left (explicit FontSpec/user skinparam/test-local mock Theme — none import defaultTheme). E2E: 'font-family' dropped from ALLOWED_DIFF_ATTRS, ceiling 50→48. Gates: typecheck/lint/build/test 4612/4612 ✓, coverage 97.7/93.4/98 ✓, DOT parity 357/234/59 ✓. **svg:survey: @font-family firstDiff bucket 93→1, but conformant still 0/354 — fixing font moved each fixture's firstDiff to the next-earliest category, not to zero-diff. New dominant buckets: svg/g (structural, 131) + textLength (D12 measurer-mode, 82).** | Confirms the in-scope conformance ceiling: font was the only renderer-layer attribute lever; remaining blockers (D12 corpus text-mode, structural childCount, layout margin) are all outside the description-cutover scope. | done |
| 2026-07-10 | childCount root cause (confirmed live) | `npm test` stderr shows `renderDescription: edge draw failed edge-3: buildDotPathFromSplinePoints: expected 1 + 3*n points, got 3` — the deferred spline-clip invariant (layout-helpers clipSplineStart/End splice to non-1+3n counts) IS firing during real renders; the try/catch skips the edge → the diagram emits fewer <g class="link"> children than the jar → svg/g/childCount structural divergence. So a meaningful slice of the 114 childCount / 131 svg/g bucket is this ONE layout-clip bug, not many separate causes. Proper fix = bezier-aware clip (subdivide at the boundary) in layout-helpers, or a polyline-tolerant DotPath in svek-edge-geometry (T13's file). Both touch every description edge — a scoped diagnosis/fix task, not a renderer tweak. | Single high-leverage structural bug behind the biggest survey bucket; worth its own task with proper before/after edge-count verification. | identified, deferred |
| 2026-07-10 | childCount root cause (CORRECTED, measured) | Instrumented the 355-fixture survey set: only 3 fixtures drop edges via the spline-clip throw (NOT the 114 childCount driver — my prior inference was wrong). Diffed a childCount fixture (bagoze-78-lada681): its .puml has a `legend` block; jar renders the legend table (7 <line>, 10 <text>, extra <g>/<rect>/<path>), our renderer emits none. So the 114 childCount / 131 svg/g bucket = UNSUPPORTED FEATURES (legend, and likely title/notes/multi-color arrows), a large feature-gap set entirely outside Brief 2's description-cutover scope — not dropped edges, not a renderer bug. | Measured, not inferred; corrects the record before acting on a false premise. | corrected |
| 2026-07-10 | B5 conformance-push CLOSE (maintainer decision) | In-scope conformance work is exhausted and committed green (T17 cutover 7e303af + fix(theme) font-family 27c1f83). Remaining survey blockers are all out-of-Brief-2-scope: (a) spline-clip edge-drop — 3 fixtures, real but narrow correctness defect, proper fix = bezier-aware/de-Casteljau clip touching every edge; (b) legend + other unsupported features — 114 childCount; (c) D12 corpus deterministic-text-mode vs production jarMeasurer — 82 textLength (documented decision); (d) layout-margin ~5px. Maintainer: DEFER all four as tracked follow-ups, proceed to Batch 6. svg:survey final for B5: 0 conformant / 354 diverged / 1 oracle-error (font-family firstDiff 93→1; document DEPTH improved even though verdict count unmoved — true zero-diff is blocked by the out-of-scope items above). | The cutover is the deliverable; chasing survey conformant-count means large out-of-scope feature/infra work. Bank the real wins, move on. | done |
| 2026-07-10 | B5 gates (final) | typecheck ✓, npm test 4612/4612 + coverage 97.7/93.4/98 ✓, lint ✓, build ✓, DOT parity 357/234/59 ✓, write-set audit ✓. Batch 5 complete: T17 + folded-in rx/ry + business-fill + font-family. Two clean commits + this docs commit. | — | pass |
| 2026-07-10 | Follow-ups tracked (out of Brief 2) | (F1) bezier-aware spline clip — restore the 3 dropped edges (layout-helpers clipSplineStart/End preserve 1+3n, or polyline-tolerant DotPath in svek-edge-geometry). (F2) legend rendering + other unsupported description features (title/notes/multi-color arrows) — 114 childCount. (F3) D12: regenerate corpus goldens in production text-mode OR dual-mode the comparator — 82 textLength. (F4) description layout margin ~5px vs upstream svek margin. None block Batches 6–7. | Explicit backlog so these aren't silently lost. | logged |
| 2026-07-10 | DIAGNOSIS CORRECTION (probe error) | My earlier "layout-margin cascades into every coordinate / 0 conformant / all out-of-scope" claims were distorted by a diagnostic probe that used FixedMeasurer(8,16) instead of the production jarMeasurer. Re-run via renderSync (production): kucabo 62 diffs → 6. Corrected census (all 355, production): 0 conformant, but 118 within 4-10 diffs, 46 within 11-30, 190 at 31+. Real firstDiff blockers: height 310 (document dims — systematic ~+4/+5px height, ~+12px width margin + content tail), background 41 (bug), then usecase ellipse geometry (cx/cy/rx/ry off 1-3px), extra groups, textLength residue, legend/feature tail. Per-element coords/text LARGELY MATCH under jarMeasurer — the cutover + D12 measurer are working. | Owning the probe error; the conformance gap is tractable, not hopeless. Maintainer: pursue focused conformance pass then seed ratchet. | corrected |
| 2026-07-10 | fix(description) leading margin (51b5bdd) | Root cause of the #1 conformance blocker (height/width firstDiff, 310 fixtures): LAYOUT_MARGIN=12 was used for BOTH the leading content-origin offset and the trailing margin, but the jar's padding is asymmetric — leading 7 (outermost rect at 7,7 on `[A]`), trailing ~12. Split out LAYOUT_MARGIN_LEADING=7 (getShift dx/dy + single-node x/y + single-node total formula); trailing unchanged. Result: minimal `[A]` width now EXACT (69=69); census tail 190→176 at 31+, 2 fixtures now within 1-3 diffs (were 0). No test fallout (description 369/369), full suite 4612/4612, DOT parity 357/234/59. Residual: node HEIGHT still −1/−2px (content vertical metric), clustered (delta 0:23, −1:45) — next blocker. | Method note: this was only findable via the PRODUCTION renderSync+jarMeasurer census; the FixedMeasurer probe had hidden it under noise. | done |
| 2026-07-10 | Conformance pass (dispatched) | typescript-pro agent, sole writer, diagnosis-mode. Targets: (1) node height −1/−2px (measureLeafNode/EntityImageDescription vertical metric), (2) background-leak bug (element-class `backgroundColor` overwriting document background — kucabo emits Green vs jar #FFFFFF; touches shared theme/style-map, maintainer-pre-authorized), (3) usecase ellipse cx/cy/rx/ry off 1-3px. Goal: a real zero-diff cohort to seed T18. Armed against the FixedMeasurer trap + no-band-widening (D5′/D12). Reports conformance census (0/1-3/4-10/… + zero-diff list) before/after each fix. | Delegated the multi-layer grind under precise diagnosis-mode brief; orchestrator gates + commits + then seeds T18 with the resulting cohort. | in progress |
| 2026-07-10 | D12 CONFLICT + resolution (maintainer) | Root conflict found: the conformance oracle corpus is generated with `-DPLANTUML_DETERMINISTIC_TEXT=true` (StringBounderFromWidthTable: UnicodeFontWidthSansSerif width table, height=size), but jarMeasurer (D12/T2/T4) reproduces AWT metrics (per-point advance table, height=(ascent+descent)×size). Two different text-metric systems → 0 fixtures can EVER be zero-diff → ratchet cannot seed. This contradicts locked D12 (AWT choice). **Maintainer decision: DUAL MEASURER** — production keeps AWT jarMeasurer (D12 intact, real-AWT fidelity); port a SEPARATE deterministic StringMeasurer (UnicodeFontWidthSansSerif + height=size) injected ONLY for the conformance/ratchet render path via the existing DI seam. | The oracle is deterministic-mode (reproducible); the ratchet must render in the same mode to compare, but production output stays AWT-faithful. Cleanest separation of real-output vs test-reproducibility. | locked |
| 2026-07-10 | Conformance-pass agent — findings preserved, changes triaged | Three root-cause diagnoses (all verified): (1) node height — measureLine (EntityImageDescriptionSupport.ts) + link-edge-attrs.ts read jarMeasurer AWT height where the deterministic oracle uses height=size; (2) usecase ellipse — Footprint.MyUGraphic.drawText read text height from UGraphicSvg.getStringBounder()'s height:0 stub (a GENUINE bug — footprint collapses vertically, undersizing the ellipse even in AWT production), agent hardcoded size; (3) background leak — skinparam.ts selectorOpen regex couldn't match `.class` selectors with spaces → backgroundColor leaked to document background. Triage under dual-measurer: KEEP+commit (3) skinparam.ts (measurer-independent, correct). REVERT (1)+(2)'s production hardcodes and their 3 test re-baselines (EntityImageDescriptionSupport, Footprint, link-edge-attrs, entity-image-description.test, symbols-figures.test, layout.test) — they hardcode deterministic values into AWT production; the genuine bugs (Footprint height:0; height routing) get re-applied MEASURER-ROUTED (production=AWT, test=deterministic) in the dual-measurer task. | Preserve the diagnoses; redo the fixes at the right layer (injected measurer, not hardcode). | done |
| 2026-07-10 | Dual measurer landed | Injection at TWO phase-entry seams only (no prop-drilling, no container — maintainer design steer applied mid-task): layoutSync(measurer) for layout; UGraphicSvg.build(...,measurer) 5th param for render, with getStringBounder() now deriving real width/height/descent from it (this ALSO fixes the Footprint height:0 usecase-ellipse bug — no Footprint code change needed, root cause was the height:0 stub). Draw-time text sizing reads ug.getStringBounder(); renderDescription(geo,theme,measurer=jarMeasurer) is the single render-phase injection point. DeterministicMeasurer = re-export of the pre-existing WidthTableMeasurer (ADR-001/S1i port of StringBounderFromWidthTable+UnicodeFontWidthSansSerif); verified byte-exact vs the deterministic jar (Component 72.3625, comp1 42, A 9.3625, astral/overflow fallbacks). Found+fixed a real WidthTableMeasurer bug: the cp≥0xFFFF (→16) and block-overflow (→13) fallbacks were dividing by 10, unlike getCharWidth's fallbacks which return raw points. Production (jarMeasurer/AWT) output byte-identical before/after (git-stash verified). Gates: typecheck/lint/build ✓, npm test 4618/4618 ✓, DOT parity 357/234/59 ✓. **Conformance census (DeterministicMeasurer): 4 ZERO-DIFF fixtures — buduni-98-bima526, vacuxi-18-baxu582, vumija-03-xise495, majuma-84-loma401 — + 8 within 1-3, 129 within 4-10. Production jarMeasurer census: 0 zero-diff (unaffected, as designed).** The 4-fixture cohort is the T18 ratchet seed set. | Dual measurer unblocks the ratchet: production stays AWT-faithful, conformance path renders in the goldens' deterministic mode. | done |
