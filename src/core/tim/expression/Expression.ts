/**
 * Upstream is an intentionally empty placeholder class with no fields or
 * methods, and no known callers within `tim/expression/`. Preserved
 * verbatim — not simplified away, not given speculative content.
 *
 * @see ~/git/plantuml/src/main/java/net/sourceforge/plantuml/tim/expression/Expression.java
 */
export class Expression {}
