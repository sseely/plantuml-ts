/**
 * Empty marker class, ported verbatim. Grepping the full upstream
 * `net/` tree finds no reference to `TMode` outside this file -- it is
 * dead code upstream too, not something this port trimmed. Preserved per
 * this project's don't-refactor-while-porting discipline.
 *
 * @see ~/git/plantuml/src/main/java/net/sourceforge/plantuml/tim/TMode.java
 */
export class TMode {}
